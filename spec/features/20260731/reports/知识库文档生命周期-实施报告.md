# 知识库文档生命周期实施报告

## 1. 实施摘要

- 实施范围：T001、T002 已完成；本轮按用户调整后的包结构关闭 T003 及其必要前置 T031，并补齐全部基础实体的 Domain Service。
- 授权证据：用户明确指令“参考 KbUserDomainService.java 补充其他 entity 的 domain service”，并确认“已经初始化 MYSQL 脚本，直接用 fons4cloud-mysql MCP 验证，不需要用 Docker 验证”。
- 当前结论：
  - T001 已完成。仓库根保持非 Maven、非 Node 项目；后端和前端可在各自项目根独立构建。
  - T002 已完成。V4 设计产物已重新渲染并完成内部复验，用户于 2026-07-31 明确确认 UI 通过。
  - T031 已完成：Flyway 依赖与迁移目录已移除，唯一结构事实来源为项目级 `sql/init-schema.sql`；只读数据库 MCP 已确认初始化结构生效。
  - T003 已完成：11 张表、11 个实体、11 个 Mapper、11 个 Domain Service 接口及实现均已闭合；静态契约、泛型绑定、Spring Service 注解和 Maven 回归测试通过。
  - 当前新增 Java 文件已统一声明 `@author hongqy`，并由结构契约测试持续约束。
- 未实施范围：T007 已完成；T009～T030、T032、T035～T036 未执行；T004、T005 已按用户明确决定豁免关闭，详见 §18。

## 2. 变更文件

### 2.1 T001 项目骨架

- `.gitignore`
- `fons4ai-rag2okf-service/pom.xml`
- `fons4ai-rag2okf-service/src/main/java/com/fons/cloud/ai/rag2okf/Rag2OkfApplication.java`
- `fons4ai-rag2okf-ui/package.json`
- `fons4ai-rag2okf-ui/package-lock.json`
- `fons4ai-rag2okf-ui/index.html`
- `fons4ai-rag2okf-ui/src/main.ts`

### 2.2 T002 原型复验

- `docs/product-design/prototypes/v4/*.png`（由现有原型重新渲染）
- 未修改业务页面代码。

### 2.3 T031/T003 实现

- 保留用户在 `fons4ai-rag2okf-service/pom.xml` 中新增的 Fons4Cloud/Fons4AI 技术栈。
- 沿用用户已移除 Flyway 的 POM，不恢复 Flyway 依赖或自动迁移能力。
- 使用唯一项目级手工初始化 SQL：`fons4ai-rag2okf-service/sql/init-schema.sql`。
- 沿用用户调整后的包结构：`domain/entity/` 下 11 个持久化实体，`domain/mapper/` 下 11 个 MyBatis-Plus Mapper。
- 更新 `KnowledgeSchemaContractTest` 与 `KnowledgePersistenceMySqlIT`：两者读取同一份项目级 SQL，集成测试通过 JDBC 执行脚本，不再引用 Flyway。
- 参考既有 `KbUserDomainService`，新增其余 10 个实体对应的 Domain Service 接口及 `ServiceImpl` 实现。
- 新增 `DomainServiceContractTest`，验证 11 组实体、服务接口与实现的泛型绑定、继承关系和 `@Service` 注解，并扫描 Java 文件作者注释。

## 3. TDD / 前置失败证据

- 骨架任务没有业务行为，采用构建契约作为最小验证闭环。
- RED-1：首次设计为仓库根 Maven 聚合项目，与用户确认的独立项目边界冲突；相关错误骨架已删除，并通过 CR-006 修正设计与任务。
- RED-2：修正后的首次后端构建能够识别独立 POM，但私服缺少 `fons4cloud-auth-satoken:1.0.0` 与 `fons4ai-rag-langchain:1.0.0`。
- GREEN：仅安装同工作区所需的 Fons4Cloud/Fons4AI 模块到本机 Maven 仓库后，后端独立打包成功；前端类型检查、测试入口和生产构建成功。
- 当前无业务单元测试；前端 Vitest 入口以 `--passWithNoTests` 验证测试工具链可识别。业务测试由后续任务补齐。
- RED-3：用户移除 Flyway 后，首次使用 JDK 21 定向编译测试失败于 `KnowledgePersistenceMySqlIT` 的 `org.flywaydb.core` 残留引用，失败行为与 T031/T003 预期一致。
- GREEN-3：测试切换为读取并通过 JDBC 执行 `sql/init-schema.sql` 后，静态结构契约测试通过，用户调整后的生产包与测试源码编译通过。
- RED-4：新增 `DomainServiceContractTest` 后，测试准确失败于缺少 `KbWorkspaceDomainService` 等服务，以及新增 Java 文件缺少 `@author hongqy`。
- GREEN-4：补齐 10 组 Domain Service 接口与实现、统一作者注释后，2 项领域服务契约测试与 1 项 SQL 静态契约测试全部通过。
- 数据库 Gate：用户明确以已初始化 MySQL 的只读 MCP 验证替代 Docker；MCP 已确认 `rag2okf` 数据库的 11 张表、关键列、默认值、唯一/组合索引和乐观锁字段生效。

## 4. 验证结果

| 验证项 | 命令/检查 | 结果 |
| --- | --- | --- |
| 后端独立构建 | 在 `fons4ai-rag2okf-service` 执行 `mvn -DskipTests package` | 通过，生成 `target/fons4ai-rag2okf-service.jar` |
| 前端类型检查 | 在 `fons4ai-rag2okf-ui` 执行 `npm run typecheck` | 通过 |
| 前端测试入口 | 在 `fons4ai-rag2okf-ui` 执行 `npm run test:unit` | 通过，无测试文件 |
| 前端独立构建 | 在 `fons4ai-rag2okf-ui` 执行 `npm run build` | 通过，生成 `dist/index.html` |
| 根目录边界 | 检查仓库根 `pom.xml`、`package.json` | 均不存在 |
| 禁用依赖 | 扫描 Maven 依赖树中的 Auth Service API、Communication API、Dubbo、Easy-ES、RHLC、Kafka | 无匹配 |
| V4 原型渲染 | 执行 `docs/product-design/prototypes/v4/render-prototypes.cjs` | 通过 |
| 亮暗主题检查 | 检查登录、注册、文档列表、文档详情、个人中心、设置页 | 通过内部复验；用户明确确认 UI 通过 |
| 用户 POM 独立构建 | 在后端项目根执行 `mvn -DskipTests package` | 通过 |
| 用户 POM 依赖树 | 检查 Sa-Token、Redisson、MySQL/MyBatis、ES、MinIO、SkyWalking、LangChain 和禁用项 | 技术栈均已解析；无禁用依赖 |
| T003/T031 RED | `mvn -Dtest=KnowledgeSchemaContractTest test` | 按预期失败：测试源码仍引用已移除的 Flyway API |
| T003 GREEN | `mvn -Dtest=KnowledgeSchemaContractTest test` | 通过：1 项测试，无跳过 |
| Domain Service RED | `mvn -Dtest=DomainServiceContractTest test` | 按预期失败：缺少 10 组服务和作者注释 |
| Domain Service GREEN | `mvn -Dtest=DomainServiceContractTest,KnowledgeSchemaContractTest test` | 通过：3 项测试，无跳过 |
| T003 最小回归 | `mvn test` | 通过：3 项测试，无跳过 |
| T003 MySQL MCP | 查询 `rag2okf` 的表、列、默认值和索引 | 通过：11 张表；11 个 `version`、11 个 `deleted`；`parse_status=NOT_STARTED`；邮箱唯一索引存在；禁用邮箱验证字段为 0；任务幂等组合索引正确 |
| T003 结构计数 | 统计 `init-schema.sql`、实体、Mapper | 11 张表、11 个实体、11 个 Mapper |
| T031 静态残留 | 扫描 POM、源码与 SQL 中的 Flyway、`db/migration`、旧 V1 路径 | 无运行依赖或路径引用；仅保留禁止 `flyway_schema_history` 的负向断言 |
| Java 作者注释 | 扫描 `src/main/java` 与 `src/test/java` | 通过：缺失数量为 0 |

## 5. Evidence Bundle

- 产物证据：
  - `fons4ai-rag2okf-service/target/fons4ai-rag2okf-service.jar`
  - `fons4ai-rag2okf-ui/dist/index.html`
  - `fons4ai-rag2okf-service/target/dependency-tree.txt`
  - `fons4ai-rag2okf-service/target/dependency-tree-user-pom.txt`
  - `fons4ai-rag2okf-service/sql/init-schema.sql`
- 结构证据：仓库根不存在 `pom.xml` 与 `package.json`。
- 原型证据：`docs/product-design/prototypes/v4/` 中登录、注册、知识库、文档列表、文档详情、个人中心、设置页均有亮暗主题评审图。

### 5.1 服务验证矩阵

| 对象 | 构建 | 启动 | 健康检查 | 外部依赖 |
| --- | --- | --- | --- | --- |
| Rag2OKF 后端 | 通过 | 未执行，归属 T020 | 未执行，归属 T020 | MySQL 初始化结构已由只读 MCP 确认；其他依赖未连接验证 |
| Rag2OKF 前端 | 通过 | 未执行，页面实现尚未授权 | 不适用 | 不适用 |

## 6. 评审与门禁

- UI Gate：通过。确认依据为用户明确回复“UI通过”。
- T003/T031 MySQL Gate：通过。依据为用户确认已执行初始化脚本，以及 `fons4cloud-mysql` 只读 MCP 对 `rag2okf` 实际结构的查询结果。
- Spec Review：实施方已按 T003/T031、用户调整后的包结构和验证方式完成自检；独立 Spec Review 待执行。
- Code Review：待独立评审。
- 人工 Gate：DDL 执行确认已由用户提供，实际结构已由只读 MCP 复核；本轮没有外部系统写入。
- 发布结论：T001、T002 已完成；尚未实现业务功能，不具备业务发布条件。

### 6.1 Harness 执行说明

- 使用 `fons4ai-sdd-implement` 执行已授权任务。
- 架构冲突通过 CR-006 进入变更流程后再恢复实现。
- 未发现需要生成 Harness 上游反馈单的问题。

## 7. AC 覆盖情况

- T001 负责的项目边界与构建入口已具备。
- T002 相关页面信息架构、视觉产物和人工 UI Gate 均已完成。
- 其余 AC 依赖后续业务、数据、安全和运行态任务，本次不宣称完成。

## 8. 质量检查

- Java 使用 21，Spring Boot 使用 3.5.8。
- 后端直接依赖本地 Sa-Token 认证能力，不依赖 Fons4Cloud Auth Service API 或 Dubbo 认证接口。
- 根目录不参与 Maven/Node 构建。
- 前后端独立生成 JAR 与 `dist/`，符合后期 Nginx 静态托管方案。
- `.gitignore` 排除 Maven、Node 和 Vite 构建产物。

## 9. DDL 执行状态

- 执行型初始 DDL：`fons4ai-rag2okf-service/sql/init-schema.sql`，包含 11 张新表。
- 用户确认已初始化 MySQL；只读 MCP 已确认 `rag2okf` 中 11 张实际表及关键结构生效。
- 应用启动链路没有配置 Flyway 或 Spring SQL 自动初始化；初始化 DDL 由部署责任方手工执行，自动化环境中的 JDBC 执行仅作为可选隔离验证。
- MCP 为只读连接，本轮没有执行 DDL/DML，也没有写入 Mapper CRUD 测试数据；基础 Mapper/Domain Service 通过编译与结构契约验证，写入型业务集成由后续应用任务覆盖。

### 9.1 初始化 DML / Seed

- 本次不适用，没有初始化业务数据。

## 10. 数据变更

- 无数据库、Elasticsearch、对象存储或 Redis 真实数据变更。
- 本机 Maven 仓库安装了验证所需的同工作区 Fons4Cloud/Fons4AI 模块；前端生成本地依赖锁文件。

## 11. S2 风险门禁

- 本次未实现认证业务、数据权限或发布流程，不触发对应 S2 验证闭环。
- 整体项目 S2 门禁仍由 T022、T028、T029 等任务关闭。

## 12. 长期知识影响

- “仓库根只承载项目资料，后端与前端分别作为独立构建根”属于长期架构事实。
- 该事实已写入技术设计 D-011、CR-006 和任务规划；本次不自动更新项目知识汇总。

## 13. 风险与后续动作

- UI Gate 已关闭，T017～T019、T026～T027 不再受 T002 阻塞，但仍需满足各自依赖并获得后续实现授权。
- 内部 Fons4Cloud/Fons4AI 模块当前未全部发布到私服；新环境构建前需要发布这些制品，或在 CI 中先构建相应源码模块。
- 后端启动、健康检查和真实外部依赖闭环属于 T020，不应由 T001 的构建成功替代。
- T003/T031 原 Docker 阻塞已由用户批准的 MySQL MCP 验证路径解除，不再要求 Docker 作为完成前置。
- `KnowledgePersistenceMySqlIT` 仍可作为未来 CI 的可选写入型隔离测试，但不属于当前默认 Surefire 测试集。
- POM 观察项：Spring Boot 3.5.8 BOM 实际统一 Elasticsearch Java Client 为 8.18.8；Fons4Cloud 锁模块仍传递 Redisson 3.24.3。二者当前编译通过，但 Redisson 与 Spring Data Redis 3.5 的运行兼容性必须在 T020/T024 验证。
- POM 观察项：`fons4cloud-common-skywalking` 自带普通 `logback.xml`，测试启动时出现 `app.name_IS_UNDEFINED` 和 `springProperty` 解析警告；应在 T020 的正式运行配置中关闭该配置缺口。

## 14. T033/T037 模型配置结构候选实现

- 实现确认依据：用户回复“按推荐执行”，确认先完成 T033/T037 的完整初始化结构候选，之后一次性重建开发库并使用 MySQL MCP 复核最终 14 表。
- 本轮范围：执行 T033、T037 的候选代码与完整初始化 SQL；未执行 T004、T005，也没有连接或修改已初始化的 11 表 `rag2okf` 数据库。
- 变更内容：
  - `sql/init-schema.sql` 增加 `kb_model_connection`、`kb_model_profile` 与 `kb_model_binding`，完整初始化结构当前为 14 张表。
  - 新增三组实体、Mapper、Domain Service 接口和 `ServiceImpl`，以及 `ModelBinding` 领域模型；API Key 仅映射为 `apiKeyCiphertext` 与 `apiKeyNonce` 的 `byte[]`，不新增明文 Key 字段。
  - `kb_model_binding` 只保存知识库、用途、模型档案和非敏感配置；不复制 owner/API Key，且用途不使用数据库 CHECK。
  - 更新 `KnowledgeSchemaContractTest` 与 `DomainServiceContractTest`，验证 14 表、模型连接/档案业务唯一键、密文字段、用途唯一绑定、固定知识库模型字段为零，以及 14 个实体对应的 Domain Service。
- TDD 证据：
  - RED：`mvn -Dtest=DomainServiceContractTest,KnowledgeSchemaContractTest test` 失败，原因是两个模型表及其实体/服务尚不存在。
  - RED：T037 的同一聚焦测试再次失败，原因是 `kb_model_binding` 及绑定实体/服务尚不存在。
  - GREEN：补齐最终结构后，同一聚焦命令通过，3 项测试无失败、无跳过。
  - 最小回归：`mvn test` 通过，3 项测试无失败、无跳过；Java 作者注释扫描缺失数为 0，初始化 SQL 表数为 14。
- DDL 状态：14 表 `init-schema.sql` 是尚未执行的完整初始化候选；现有 `rag2okf` 仍为经先前 MCP 确认的 11 表结构。按照用户确认的策略，下一步由用户一次性重建开发库，再用 MCP 复核最终结构。Agent 未执行 DDL/DML。
- 任务状态：T033、T037 均保持未完成，阻塞签名为 `验证|T033,T037|最终初始化结构执行确认|用户确认延后到候选 14 表完成后一次性重建并复核`；分类为外部 Gate。
- Review：实施方自检通过；独立 Spec Review、Code Review 待执行。人工 Gate：开发库重建与最终 14 表 MCP 复核待用户执行后进行。
- 长期知识影响：是，涉及用户级 Provider 连接、加密凭证和模型档案持久化边界；本轮不自动写入项目知识库。

## 15. T033/T037 最终初始化结构验证

- 人工 Gate：用户已确认以最新 `fons4ai-rag2okf-service/sql/init-schema.sql` 重建本地 `rag2okf` 数据库；本实施方未执行 DDL 或 DML。
- MySQL MCP 只读复核：`rag2okf` 中恰有 14 张 `kb_*` 表；14 张表均具备 `version` 与 `deleted` 列。
- 模型安全结构：`kb_model_connection.api_key_ciphertext` 为 `VARBINARY(2048)`、`api_key_nonce` 为 `VARBINARY(32)`，不存在明文 API Key 列；连接与模型档案的业务唯一键均已生效。
- 用途绑定结构：`kb_model_binding` 具备 `(knowledge_base_id, usage_type)` 唯一键、`(model_profile_id, status)` 与 `(knowledge_base_id, status)` 索引；不含 `owner_user_id` 或 API Key 字段，知识库表也不含固定聊天/向量模型列。
- Outbox 结构：`kb_outbox_event` 具备事件幂等唯一键 `event_key` 与待投递扫描索引 `(status, next_run_at)`。
- 结论：T033、T037 的代码、初始化 SQL 与实际 MySQL 结构一致，任务状态更新为完成。T004/T005 仍按用户既定范围保持未执行。

## 16. T006 本地认证、Sa-Token 会话与工作空间隔离

- 授权与边界：用户明确授权执行 `T006`；按 CR-010 仅实现已有本地账号的登录、会话、当前资料与工作空间授权，不新增注册接口，也不创建账号、个人 Workspace 或成员关系。
- 本地认证：新增 `LocalAccountRepository`、`EmailNormalizer`、`PasswordHasher` 与 `AuthenticationRateLimiter` 端口；`AuthenticationApplicationService` 对邮箱执行 trim/小写规范化，在密码匹配前检查频控，并把账号不存在、密码错误和 DISABLED 账号统一收敛为无会话的认证失败。Sa-Token loginId 固定为 `kb_user.user_key`。
- 密码与频控：`SpringSecurityPasswordHasher` 使用 `DelegatingPasswordEncoder`；`RedisAuthenticationRateLimiter` 以带服务端 salt 的 SHA-256 hash 构造邮箱/IP Redis 键，不写入明文邮箱或 IP，分别使用 15 分钟和 10 分钟窗口。
- API 与资料：新增 `POST/DELETE /auth/session`、`GET /auth/csrf`、`GET/PATCH /users/me`。资料更新白名单仅包含展示名、头像与偏好 JSON；邮箱仅在本人资料响应中出现，密码摘要和令牌不出现在响应中。
- 授权与浏览器安全：新增 `Rag2OkfStpInterface`，不把工作空间角色错误提升为全局 ADMIN；业务操作需要经过 `WorkspaceAccessPolicy` 以 `workspaceKey + 本地成员关系 + localRole` 授权。账号禁用时执行 kickout。Sa-Token 配置为 Cookie 会话（HttpOnly、Secure、SameSite=Lax、禁止从 Header 读取 token）；写请求校验同源 Origin 与会话 `X-CSRF-Token`。
- TDD 证据：RED 阶段先新增认证测试，构建按预期因领域端口/异常类型尚不存在而失败；GREEN 阶段完成认证、资料、工作空间、频控与 CSRF 实现。中途 Redis 频控测试发现断言错误：一次失败应同时写入邮箱和 IP 两个 hash 键，修正测试断言后通过，未发现生产实现偏差。
- 验证：`mvn test` 通过，14 个测试无失败、无跳过；包含登录成功/失败/禁用、邮箱规范化、注销、资料白名单、工作空间无成员拒绝/禁用踢下线、密码摘要、Redis 窗口键和 CSRF 拦截器。`git diff --check` 通过，所有 main/test Java 文件均含 `@author hongqy`。
- 运行态边界：本轮未连接 MySQL、Redis 或 ES，也未执行真实 Redis 会话过期演练；这是既定的环境连接后置范围，服务启动、真实 Redis 会话持久化/过期和端到端 Cookie 验证由 T020/T025 继续关闭。未保留任何失败或禁用的 Web 集成测试。
- Review：实施方完成范围、安全与代码自检；独立 Spec Review、Code Review 仍待后续评审，不据此宣称项目可发布。

## 17. T039 认证令牌、统一响应、异常端点与状态枚举

- 实现确认依据：用户最新消息“继续执行任务”。本轮按依赖顺序执行 T039；T004、T005 继续遵循用户先前的明确决定，不纳入本轮。
- 完成范围：
  - 登录调整为 `POST /auth/login`，返回 `R<LoginResponse>`，token 仅位于 `data.token`；注销调整为 `POST /auth/logout`，资料的 `GET/PATCH /users/me` 统一返回 `R<T>`。
  - Sa-Token 改为只读取 `Authentication: Bearer <token>`，禁用 Cookie 读取和写回；`/auth/login` 进入全局登录校验放行名单。
  - 删除 CSRF token、拦截器、Web MVC 注册和对应测试；认证、授权、限流、参数、未登录与运行时错误由 `controller/endpoint/Rag2OkfExceptionEndpoint` 统一返回 `R<T>` 与 Fons4Cloud `ResultCode`。
  - 用户、工作空间、成员状态，空间类型与空间角色改为带 `@EnumValue` 的持久化枚举；保留既有 MySQL VARCHAR 代码，不涉及 DDL、Redis 频控 key 或会话 TTL 变更。
- 变更文件：认证与资料 Controller、认证/资料应用服务、三个持久化实体、`domain/user` 枚举与授权策略、统一异常端点、Sa-Token 配置，以及认证/异常/枚举/Header 契约测试；删除 4 个 CSRF 生产类和 1 个 CSRF 测试类。

### 17.1 TDD 与验证

| 阶段 | 证据 |
| --- | --- |
| RED | 新增 `Rag2OkfExceptionEndpointTest` 后执行 `mvn -Dtest=AuthSessionControllerTest,Rag2OkfExceptionEndpointTest,IdentityStatusEnumTest test`，按预期因统一异常端点尚不存在而编译失败。 |
| GREEN | 实现统一响应、异常端点、Bearer 配置、状态枚举并更新既有测试后，认证与授权聚焦回归 12 项通过。 |
| REFACTOR | 删除 Cookie/CSRF 残留，使用框架 `R<T>`、`ResultCode` 和 `BusinessRuntimeException`；补充带内存 Sa-Token Dao 的 Web MOCK Header 契约测试。 |

- 聚焦验证：`mvn -Dtest=SaTokenHeaderContractTest test` 通过。该测试使用内存 Sa-Token Dao，确认只有 `Authentication: Bearer <token>` 可恢复会话；Cookie、错误前缀和缺失 token 均返回统一 401 `R<T>`。
- 回归验证：`mvn clean test` 通过，19 项测试无失败、无跳过；`clean` 用于清除已删除 CSRF 测试遗留的 `target` class。测试未连接 MySQL、Redis 或 Elasticsearch。
- 静态验证：CSRF、`ResponseEntity`、Controller 本地 `@ExceptionHandler`、`WorkspaceRole.valueOf` 和字符串状态比较在生产/测试源码中扫描为零；全部 Java 源文件均包含 `@author hongqy`；`git diff --check` 通过。
- AC 覆盖：AC-026 覆盖登录 token 与注销接口；AC-027 覆盖认证、限流与参数统一错误；AC-028 覆盖 Header 恢复会话和工作空间角色枚举；AC-029 覆盖资料统一响应与 token 非日志/非持久化边界。

### 17.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | `知识库文档生命周期-任务规划.md` T039，CR-011；用户最新授权“继续执行任务”。 |
| DDL / DML | 不适用；未修改 MySQL 结构、未执行 DDL/DML。 |
| 服务级证据 | 单元与 Web MOCK 契约测试通过；真实服务启动、健康检查、MySQL/Redis/ES 连接验证按既定范围留给 T020/T025。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 适用（认证与安全变更），待独立安全审阅；因此本任务为“实现候选完成”，不宣称项目可交付或发布就绪。 |
| 长期知识影响 | 是，涉及认证头协议、统一 HTTP 响应与状态枚举边界；本轮不修改 `.specify/memory/`，后续仅在用户显式触发知识汇总时沉淀。 |

- 剩余风险：真实 Redis 会话存储、注销传播、过期与跨进程 Header 链路尚未做运行态验证；Sa-Token 测试自动配置须显式隔离无关的数据库、Redisson、Agent 和分布式锁自动配置，不能替代正式启动验证。

## 18. T004/T005 关闭说明与 Fons4Cloud 代码规范整理

- 关闭依据：用户于 2026-08-03 明确要求“ T004、T005 能否直接关闭，如果可以直接关闭即可”，且此前已明确两项不执行。
- T004：按“用户豁免”关闭。现有数据库已由用户重建，且此前 MySQL MCP 只读复核确认 14 张目标表存在；本轮没有补做任务原定义的隔离容器 JDBC 自动化和首次登录幂等验证。因此它不是 T022 发布门禁的完成证据。
- T005：按“用户豁免”关闭。未创建 `.specify/sql/knowledge_engine/knowledge_document_lifecycle.sql`；项目唯一当前结构事实仍为 `fons4ai-rag2okf-service/sql/init-schema.sql`。因此不把该关闭表述为 SQL 快照已验证。
- 代码规范整理：依据 `fons4cloud/.specify/rules/代码编写规范.md`，将 HTTP 请求/响应对象置于 `common/request`、`common/response`，将用户与工作空间状态枚举集中至 `common/constants`，Controller 只保留 HTTP 编排；实体、授权策略与测试同步更新。Redis 限流适配器不再抛出裸 `IllegalStateException`，改为保留 cause 的业务异常。
- 影响边界：未改变 HTTP 路由、`R<T>` 响应契约、认证头、数据库表结构、SQL、Redis key 或模型配置行为；本次为用户授权的代码标准与包结构整理，不创建 SDD 变更单。
- 验证：执行 `mvn clean test`，19 项测试全部通过（0 failure、0 error、0 skipped）；测试未连接 MySQL、Redis 或 Elasticsearch。

## 19. T007 实现前置依赖阻塞记录

- 实现确认依据：用户于 2026-08-03 发出“继续T007”。
- 阻塞任务：T007。
- 失败签名：`执行前检查|T007|Depends:T034|T034 仍为未完成，且 T007 的模型档案所有权、ACTIVE 状态和用途兼容校验依赖其应用服务与动态模型边界`。
- 阻塞分类：任务依赖未完成。
- 判断：T007 依赖 T006、T034。T034 当前仍为未开始；T007 的验收要求保存 `ANSWER_GENERATION`/`EMBEDDING` 独立用途绑定并校验档案所有权、启用状态和模型类型兼容性。没有 T034，不能以临时查询、无校验绑定或全局模型回退替代，避免绕过 AC-034/035 与模型安全边界。
- 本轮未修改业务代码、数据库结构、接口契约或任务状态；T007 保持未完成。
- 恢复建议：先执行 T034；其完成后可回复“执行 T007”。如确实希望改变依赖顺序或将模型绑定校验从 T007 拆分，需先进入 `fons4ai-sdd-change` 调整设计和任务。

## 20. T034 用户模型配置、安全凭证与动态 LangChain4j Client

- 实现确认依据：用户明确要求“执行T034”，并明确授权以当前 Fons4Cloud 标准包结构为准；接口层实现为 `controller/ModelConfigurationController`，请求/响应对象分别位于 `common/request`、`common/response`，未改变需求、数据模型或 HTTP 契约语义。
- 完成范围：新增 Provider 模板、连接、模型档案、档案测试 API；支持 `CHAT` 与 `EMBEDDING` 两类档案。API Key 使用 AES-256-GCM 加密，随机 12 字节 nonce，并只保存密文、nonce、keyVersion 与脱敏掩码；响应和日志路径不返回明文 Key、密文或 nonce。
- 动态调用边界：`UserModelResolver` 集中校验当前用户所有权、连接/档案 ACTIVE 状态、端点策略与参数；`ModelClientFactory` 每次调用动态构建 LangChain4j Client，不注册全局 ChatModel/EmbeddingModel Bean，也不存在全局模型或 API Key 回退。模型测试仅持久化 `SUCCEEDED`/`FAILED` 与通用错误码。
- 出站安全：端点策略拒绝非 HTTPS、用户信息/query/fragment、非常规端口及 IPv4/IPv6 loopback、私网、link-local 等地址；每次动态构建前重新校验。底层 LangChain4j Spring RestClient 实现会覆盖调用方传入的 request factory，聚焦测试发现它会默认跟随 302；因此改为最小 JDK HTTP 适配器并强制 `Redirect.NEVER`，重定向目标不会收到请求。
- TDD 证据：先以 `AesGcmCredentialCipherTest`、`ModelEndpointPolicyTest`、`ModelUsagePolicyTest` 建立 RED，因加密器、端点策略与用途兼容策略尚为占位实现而按预期失败；实现后同组 5 项 GREEN 通过。后续动态 Client 聚焦测试发现并修复 302 重定向缺口。
- 验证结果：`mvn -Dtest=AesGcmCredentialCipherTest,ModelEndpointPolicyTest,ModelUsagePolicyTest,ModelConfigurationApplicationServiceTest,LangChain4jModelClientFactoryTest,UlidModelBusinessKeyGeneratorTest,ModelConfigurationControllerTest test` 通过，17 项测试无失败、无错误、无跳过；最终 `mvn clean test` 通过，37 项测试无失败、无错误、无跳过。覆盖 AES-GCM/keyVersion、所有权越权、掩码响应、CHAT/EMBEDDING Mock Provider、401/429/timeout、维度不匹配、私网/loopback/link-local、重定向与用途/模型类型兼容。
- 静态检查：`git diff --check` 通过；生产 Java 文件均含 `@author hongqy`；接口层统一为 `controller`，扫描未发现 `ResponseEntity` 或裸 `new RuntimeException(...)`，`common/response` 未包含凭证密文字段。
- DDL/DML：不适用。本轮未修改 SQL、未连接或写入 MySQL、Redis、Elasticsearch、MinIO，也未使用真实厂商 Key。
- 运行配置与剩余 Gate：部署责任方须提供 `RAG2OKF_MODEL_CREDENTIAL_KEY`（Base64 编码的 32 字节密钥）和非空 `RAG2OKF_MODEL_CREDENTIAL_KEY_VERSION`；缺失或不合法时连接创建/替换会安全失败，绝不回退到全局 Key。真实环境启动、MySQL/Redis/ES 连通与用户真实厂商测试归属 T020/T036；DNS 重绑定的运行时故障注入及完整模型安全 Gate 归属 T036。独立 Spec Review、Code Review 与人工安全 Gate 均待执行，因此 T034 仅标记为“实现候选完成”，不声明可发布。
- 依赖影响：T034 已满足 T007 的前置依赖；T007 仍未执行，其知识库用途绑定保存与唯一性实现不在本轮范围。

## 21. T008 MinIO 产物存储与文件安全校验

- 实现授权：用户明确指令“执行 T008”。本轮只处理任务规划列出的文档产物存储端口、Fons4Cloud OSS 适配器和文件安全校验；不实现 T009 的上传 HTTP 入口、文档/版本写库、授权下载或解析触发。
- 变更内容：
  - 新增 `domain/artifact/DocumentArtifactStore`，以 `ArtifactScope + ArtifactType + revisionKey` 生成固定的原文件、解析 manifest、锚点 manifest、分块 manifest 和发布 manifest 路径；调用方不能直接传入对象 key 或公开 URL。
  - 新增 `infrastructure/artifact/FonsOssDocumentArtifactStore`，只复用 Fons4Cloud `OssStoreService`；原文件及 manifest 均流式上传、服务端计算 SHA-256、保存最小 metadata，并在上传失败时删除目标 key 进行补偿。
  - 新增 `application/document/FileValidationPolicy`，校验 PDF、DOCX、Markdown、TXT 的文件名、扩展名、MIME、签名字节、空文件、声明大小和实际流式大小。默认上限为 50 MiB，可由 `rag2okf.document.max-file-size-bytes` 覆盖。
  - 新增 `DocumentArtifactException`，对外不透传对象地址、凭证或文件正文。
- TDD 证据：
  - RED：先新增 `FileValidationPolicyTest` 和 `FonsOssDocumentArtifactStoreTest`；首次定向编译按预期失败，原因是 `FileValidationPolicy` 与 `DocumentArtifactStore` 尚未实现，不是环境或依赖失败。
  - GREEN：实现端口、适配器和校验策略后，定向单元测试通过；修正了实现中 `DigestInputStream` 包导入和摘要克隆的局部编译错误。
  - 追加边界：新增未知声明大小但实际流超限的测试，确认不能仅信任浏览器声明值。
- 验证结果：
  - `mvn -Dtest=FileValidationPolicyTest,FonsOssDocumentArtifactStoreTest,FonsOssDocumentArtifactStoreMinioIT test` 通过：11 项测试，0 failure、0 error、0 skipped。
  - `FonsOssDocumentArtifactStoreMinioIT` 使用 Testcontainers 启动临时 MinIO、临时私有 bucket 和测试 key；验证原文件/manifest 流式写读、SHA-256、metadata、系统 key、删除以及上传流中断后的目标对象清理。测试容器由 Testcontainers 在 JVM 退出时回收，未连接或写入真实 MinIO 环境。
  - 文件校验覆盖合法 TXT、路径穿越文件名、空文件、不支持扩展名、伪造 PDF 签名、声明超限和实际流超限。
- AC 覆盖边界：T008 为 AC-005、AC-006、AC-007、AC-014 提供安全的产物读写与拒绝基础；这些 AC 的完整用户流程仍依赖 T009 的上传/下载、文档状态和后续解析任务，不能仅凭本任务宣称端到端完成。
- DDL/DML：不适用。本轮未变更 SQL、未执行 MySQL DDL/DML，且未写入 Redis、Elasticsearch 或真实对象存储。
- Fons4Cloud 复用与安全：生产代码不直接调用 MinIO SDK，也不调用 `getAccessUrl`；对象 key 仅接受 26 位系统业务 key，原始文件名只允许作为已净化 metadata，日志不记录正文、对象地址或凭证。MinIO SDK 仅在隔离集成测试中用于建桶。
- 剩余风险与 Gate：`FileValidationPolicy` 由 T009 上传用例接入；DOCX 当前进行 ZIP 文件头签名校验，完整 OOXML 包结构与损坏文件检查由后续解析链路负责。生产 bucket 的私有策略、环境凭证和真实环境联调归属 T020。独立 Spec Review、Code Review 与人工安全 Gate 均未执行，因此 T008 仅标记为“实现候选完成”，不宣称可发布。

## 22. T007 知识库创建、列表与设置用例

- 实现确认依据：用户明确指令“执行T007”。
- 完成范围：
  - 新增 `KnowledgeBaseApplicationService`，实现知识库创建、分页列表、详情查询、设置编辑和模型用途绑定保存。
  - 新增 `KnowledgeBaseController`，提供 `POST/GET /workspaces/{workspaceKey}/knowledge-bases`、`GET/PATCH /knowledge-bases/{knowledgeBaseKey}` 和 `GET/PUT /knowledge-bases/{knowledgeBaseKey}/model-bindings` 六个接口，统一返回 `R<T>`。
  - 新增 `ChunkProfile` 值对象，集中校验分块策略名称、块大小、重叠量和标题层级；`autoPublish=true` 时必须 `autoParse=true` 的规则在应用服务中校验。
  - 新增 `KnowledgeBaseException`（400）和 `KnowledgeBaseConflictException`（409），并在 `Rag2OkfExceptionEndpoint` 中添加 409 冲突处理器。
  - 模型用途绑定通过独立 `kb_model_binding` 表保存；保存前校验档案所有权（工作空间所有者）、ACTIVE 状态和用途/模型类型兼容性；同一用途在单次请求中唯一。
  - 设置修改只更新知识库实体，不批量修改已有文档或任务输入。
- 变更文件：
  - `domain/knowledgebase/ChunkProfile.java`（新增）
  - `common/exeception/KnowledgeBaseException.java`（新增）
  - `common/exeception/KnowledgeBaseConflictException.java`（新增）
  - `common/request/ModelBindingItem.java`、`ChunkProfileRequest.java`、`CreateKnowledgeBaseRequest.java`、`UpdateKnowledgeBaseRequest.java`、`SaveModelBindingsRequest.java`（新增）
  - `common/response/ChunkProfileResponse.java`、`ModelBindingResponse.java`、`KnowledgeBaseResponse.java`、`KnowledgeBaseSummaryResponse.java`、`PageResponse.java`（新增）
  - `application/knowledgebase/KnowledgeBaseApplicationService.java`（新增）
  - `controller/KnowledgeBaseController.java`（新增）
  - `controller/endpoint/Rag2OkfExceptionEndpoint.java`（更新：添加 409 处理器和 `KnowledgeBaseException` 400 映射）
  - `test/application/knowledgebase/KnowledgeBaseApplicationServiceTest.java`（新增，20 项测试）

### 22.1 TDD 与验证

| 阶段 | 证据 |
| --- | --- |
| RED | 首次编译因 `ChunkProfile`、`KnowledgeBaseException`、`KnowledgeBaseApplicationService` 等类型尚不存在而失败。 |
| GREEN | 实现全部类型后，聚焦测试 `mvn -Dtest=KnowledgeBaseApplicationServiceTest test` 通过，20 项测试无失败。中途修复了 `list(any())` 方法歧义（改为 `list(any(Wrapper.class))`）和 `entity.getVersion()` 空指针（`toResponse` 增加 null 安全处理）。 |
| REFACTOR | 确认 Controller 不承载业务规则，校验集中在 `ChunkProfile` 值对象和应用服务；异常映射统一在 `Rag2OkfExceptionEndpoint`。 |

- 聚焦验证：`mvn -Dtest=KnowledgeBaseApplicationServiceTest test` 通过，20 项测试，0 failure、0 error、0 skipped。
- 回归验证：`mvn test` 通过，66 项测试，0 failure、0 error、0 skipped。包含 `DomainServiceContractTest`（@author 注释与实体/服务契约）、`Rag2OkfExceptionEndpointTest`（异常端点回归）、`ModelConfigurationApplicationServiceTest`（模型配置回归）等全部既有测试。
- 测试覆盖场景：创建知识库（含初始绑定）、autoPublish/autoParse 互斥校验、名称/分块参数校验、分页列表、详情查询、设置编辑、乐观锁版本冲突（stale revision + DB 版本变化）、模型绑定保存（CHAT+EMBEDDING）、档案所有权/ACTIVE/类型兼容校验、重复用途拒绝、绑定整体替换、设置修改不触发任务变更和非成员越权拒绝。

### 22.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | `知识库文档生命周期-任务规划.md` T007；用户最新授权“执行T007”。 |
| DDL / DML | 不适用；未修改 MySQL 结构、未执行 DDL/DML。 |
| 服务级证据 | 单元测试通过；真实服务启动、MySQL/Redis 连接验证归属 T020。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 不适用（本轮不涉及权限、安全、资金或生产配置变更）；知识库用途绑定的跨用户安全边界已由 T034 的 `ModelUsagePolicy` 和 `WorkspaceAccessPolicy` 覆盖。 |
| 长期知识影响 | 是，涉及知识库设置快照语义和模型用途绑定保存边界；本轮不修改 `.specify/memory/`。 |

- AC 覆盖：AC-003（知识库创建 API 与初始化结构）、AC-004（设置快照不追溯已有文档）由本轮应用服务与测试覆盖；AC-034（用途绑定）和 AC-035（fail-closed）的贡献部分已覆盖，完整关闭仍依赖 T016 契约测试和 T022 风险门禁。
- 剩余风险：真实 MySQL 分页、乐观锁冲突和模型绑定唯一约束的集成验证归属 T020；前端页面实现归属 T017/T035；完整 AC 关闭归属 T022。

## 23. T009 新文档上传、更新文件与原文件访问

- 实现确认依据：用户明确指令“执行T009”。
- 完成范围：
  - 新增 `DocumentApplicationService`，实现新文档上传、更新文件、文档详情查询和原文件下载四个用例。应用服务负责编排权限校验、文件验证、对象存储、MySQL 事务和 MinIO/MySQL 显式补偿，不承载解析或发布逻辑（P0 不创建解析任务，parseStatus 统一为 NOT_STARTED）。
  - 新增 `DocumentController`，提供 `POST /knowledge-bases/{kbKey}/documents`（上传）、`POST /documents/{docKey}/files`（更新）、`GET /documents/{docKey}`（详情）和 `GET /documents/{docKey}/file`（下载）四个接口，统一返回 `R<T>`；下载使用 `ResponseEntity<InputStreamResource>` 返回文件流。
  - 新增 `DocumentUploadResponse` 和 `DocumentDetailResponse` 两个 DTO，均不包含版本序号、版本列表或回退操作（D-004）。
  - 上传始终生成新的 `documentKey`（同名文件不查询、不合并）；更新使用 `expectedCurrentVersionKey` 做前置 CAS 校验，事务内通过 `version` 字段做乐观锁切换，失败时保留旧指针。
  - 更新文件时清空 `currentParseRevisionId` 和 `currentChunkRevisionId`，重置 parseStatus/publishStatus，但 `activePublicationRevisionId` 不修改（旧发布继续可用）。
- 变更文件：
  - `application/document/DocumentApplicationService.java`（新增）
  - `controller/DocumentController.java`（新增）
  - `common/response/DocumentUploadResponse.java`（新增）
  - `common/response/DocumentDetailResponse.java`（新增）
  - `test/application/document/DocumentApplicationServiceTest.java`（新增，9 项测试）

### 23.1 TDD 与验证

| 阶段 | 证据 |
| --- | --- |
| RED | 首次因 `DocumentApplicationService` 等类型不存在而编译失败。测试编写后首次运行出现 3 个错误：2 个 `UnfinishedStubbingException`（在 `when().thenReturn()` 内部嵌套创建 ValidatedFile mock）和 1 个 `UnnecessaryStubbing`（版本 key 校验失败前文件 mock 未被使用）。 |
| GREEN | 修复后 `mvn -Dtest=DocumentApplicationServiceTest test` 通过，9 项测试无失败、无错误、无跳过。 |
| REFACTOR | `mockValidatedFile` 只 stub 服务实际调用的方法（safeFilename/contentType/inputStream），移除未使用的 size/sha256 stub；版本 key 不匹配测试改用裸 `mock(MultipartFile.class)` 避免 UnnecessaryStubbing。 |

- 聚焦验证：`mvn -Dtest=DocumentApplicationServiceTest test`，结果 `Tests run: 9, Failures: 0, Errors: 0, Skipped: 0`，BUILD SUCCESS。
- 编译验证：`mvn -q compile` 和 `mvn -q test-compile` 均通过，退出码 0。
- 测试覆盖场景：
  - AC-008：同名普通上传两次得到不同 documentKey（`01J_DOC_1` / `01J_DOC_2`）。
  - AC-013：SKIP 模式上传后 parseStatus 为 NOT_STARTED，taskKey 为 null，不伪造解析结果。
  - AC-009：更新成功切换当前文件指针，parseStatus 重置为 NOT_STARTED，publishStatus 重置为 UNPUBLISHED。
  - AC-009：expectedCurrentVersionKey 不匹配抛出 KnowledgeBaseConflictException。
  - AC-009：CAS 失败（乐观锁冲突，updateById 返回 false）抛出异常并触发补偿删除（verify delete 调用）。
  - AC-010：上传响应只包含 documentKey、displayName、currentFile、taskKey、parseStatus、publishStatus，无版本列表或回退。
  - AC-005：授权下载返回文件名、MIME、大小和输入流。
  - AC-014：空文件上传抛出 DocumentArtifactException。
  - 权限：无权限用户下载被拒绝（knowledgeBase 返回 null 时抛 WorkspaceAccessDeniedException）。

### 23.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | `知识库文档生命周期-任务规划.md` T009；用户最新授权“执行T009”。 |
| DDL / DML | 不适用；未修改 MySQL 结构、未执行 DDL/DML。 |
| 服务级证据 | 单元测试通过；真实 MySQL 乐观锁、MinIO 读写和权限链路集成验证归属 T020。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 适用（跨 MySQL/MinIO 一致性和文件安全），待独立审阅；因此本任务为“实现候选完成”，不宣称可交付或发布就绪。 |
| 长期知识影响 | 是，涉及跨存储显式补偿、CAS 指针切换和 DTO 无版本历史边界；本轮不修改 `.specify/memory/`。 |

- AC 覆盖：AC-005（授权下载）、AC-006（解析模式选择，SKIP 不创建任务）、AC-007（文件边界，空文件拒绝）、AC-008（同名上传不同 documentKey）、AC-009（更新 CAS 与指针结构）、AC-010（无版本列表/回退）、AC-013（未解析不伪造结果）、AC-014（安全错误）由本轮单元测试覆盖；完整端到端关闭仍依赖 T016 契约测试、T018 前端页面和 T022 风险门禁。
- 剩余风险：真实 MinIO 上传失败补偿、MySQL 乐观锁并发和跨工作空间下载越权的集成验证归属 T020/T021；前端上传/更新/下载交互归属 T018；`@Lazy` 自引用代理在真实 Spring 容器中的事务行为需在 T020 启动验证中确认。

## 24. T010 ProcessingTask、Fons4Cloud 分布式锁、幂等与 Outbox 引擎

- 实现确认依据：用户明确指令"继续执行下一个任务"，按核心路径 T009 → T010 执行。
- 完成范围：
  - 新增 `domain/task/` 领域模型：`TaskStatus`（QUEUED/RUNNING/RETRY_WAIT/SUCCEEDED/FAILED）、`TaskType`（PARSE/RECHUNK/PUBLISH）、`OutboxEventStatus`（PENDING/PUBLISHED/FAILED）、`ProcessingTask`（封装状态机流转、指数退避、deadline 恢复和候选判定）、`TaskExecutionPort`（任务执行端口）、`TaskExecutionResult`（sealed interface：Succeeded/RetryableFailure/FatalFailure）、`OutboxEventPort`（事件处理端口）。
  - 新增 `application/task/` 应用服务：`TaskApplicationService`（幂等创建依赖唯一约束、CAS 状态更新、候选扫描和 stale 扫描）、`OutboxApplicationService`（事件创建、按 key 查询、投递状态更新和退避重试）。
  - 新增 `infrastructure/task/` 基础设施：`DistributedLockedTaskExecutor`（独立 Bean，`@DistributeLock(scene="rag2okf:task-execute", waitTime=0)` 覆盖整个任务执行入口，watchdog 自动续期；锁内重新读取任务、CAS 标记 RUNNING、路由执行、CAS 更新最终状态；另含 `@DistributeLock(scene="rag2okf:task-recover")` 恢复 stale 任务）、`TaskCandidateScheduler`（`@Scheduled` 扫描 QUEUED 任务，catch DistributeLockException 跳过）、`OutboxDispatchExecutor`（`@DistributeLock(scene="rag2okf:outbox-dispatch", waitTime=0)` 分发事件）、`OutboxDispatchScheduler`（`@Scheduled` 扫描 PENDING 事件）。
  - 新增 `common/response/TaskStatusResponse` DTO 和 `controller/TaskController`（GET /tasks/{taskKey} 状态查询）。
  - 主应用类添加 `@EnableScheduling`。
  - 禁止 MySQL FOR UPDATE / SKIP LOCKED；扫描查询只走普通索引，互斥完全由分布式锁保证；worker 不占用 HTTP 线程。

- 变更文件：
  - `domain/task/TaskStatus.java`（新增）
  - `domain/task/TaskType.java`（新增）
  - `domain/task/OutboxEventStatus.java`（新增）
  - `domain/task/ProcessingTask.java`（新增）
  - `domain/task/TaskExecutionPort.java`（新增）
  - `domain/task/TaskExecutionResult.java`（新增）
  - `domain/task/OutboxEventPort.java`（新增）
  - `application/task/TaskApplicationService.java`（新增）
  - `application/task/OutboxApplicationService.java`（新增）
  - `infrastructure/task/DistributedLockedTaskExecutor.java`（新增）
  - `infrastructure/task/TaskCandidateScheduler.java`（新增）
  - `infrastructure/task/OutboxDispatchExecutor.java`（新增）
  - `infrastructure/task/OutboxDispatchScheduler.java`（新增）
  - `common/exeception/TaskExecutionException.java`（新增）
  - `common/response/TaskStatusResponse.java`（新增）
  - `controller/TaskController.java`（新增）
  - `Rag2OkfApplication.java`（添加 `@EnableScheduling`）
  - `test/task/ProcessingTaskTest.java`（新增，17 项测试）
  - `test/task/TaskApplicationServiceTest.java`（新增，8 项测试）
  - `test/task/OutboxApplicationServiceTest.java`（新增，7 项测试）

### 24.1 TDD 与验证

| 阶段 | 证据 |
| --- | --- |
| RED | 首次因 `TaskExecutionException` 构造函数不匹配 `BusinessRuntimeException(ResultCode, String)` 和 `portRegistry.put()` 返回值类型错误编译失败；修复后首次测试运行出现 2 个 NPE（测试实体缺少 `attempt` 初始化）。 |
| GREEN | 修复后 `mvn -Dtest="ProcessingTaskTest,TaskApplicationServiceTest,OutboxApplicationServiceTest" test` 通过，32 项测试无失败、无错误、无跳过。 |
| REFACTOR | `ProcessingTask` 状态机方法返回 boolean 或 void 明确表达意图；`TaskExecutionResult` 使用 sealed interface 保证穷尽匹配；调度器 catch DistributeLockException 实现"跳过本轮候选"语义。 |

- 聚焦验证：`Tests run: 32, Failures: 0, Errors: 0, Skipped: 0`，BUILD SUCCESS。
- 编译验证：`mvn -q compile` 和 `mvn -q test-compile` 均通过，退出码 0。
- 测试覆盖场景：
  - AC-011：QUEUED→RUNNING（attempt 递增、deadline 设置）、RUNNING→SUCCEEDED（progress=100）、RUNNING→RETRY_WAIT（指数退避）、RETRY_WAIT→QUEUED（退避到期恢复）、RUNNING→FAILED（不可重试失败或上限）、心跳更新、终态判定。
  - AC-017：相同 idempotencyKey 返回原 taskKey（不创建新任务、不调用 keyGenerator）、不同 idempotencyKey 创建新任务。
  - AC-023：按 taskKey 查询返回状态、查询不存在返回 null、CAS 更新成功、CAS 更新失败抛异常、候选扫描、stale 扫描。
  - AC-024：deadline 过期 RUNNING 恢复为 QUEUED（清除 executionOwner/deadline、记录恢复原因）、deadline 未过期不恢复、非 RUNNING 不能恢复。
  - Outbox：事件创建（PENDING/attempt=0）、投递成功（PUBLISHED）、投递失败重试（退避）、超过上限转 FAILED、退避计算、候选扫描、按 key 查询。

### 24.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | `知识库文档生命周期-任务规划.md` T010；用户授权"继续执行下一个任务"。 |
| DDL / DML | 不适用；未修改 MySQL 结构，复用已有 `kb_processing_task` 和 `kb_outbox_event` 表。 |
| 服务级证据 | 单元测试通过；真实 Redisson 分布式锁、多实例并发和 watchdog 续期验证归属 T020。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 适用（分布式锁、幂等和崩溃恢复），待独立审阅；因此本任务为"实现候选完成"，不宣称可交付或发布就绪。 |
| 长期知识影响 | 是，涉及 Fons4Cloud 分布式锁矩阵、ProcessingTask 状态机和 Outbox 至少一次投递模式；本轮不修改 `.specify/memory/`。 |

- AC 覆盖：AC-011（任务状态机）、AC-017（重复提交返回原 taskKey）、AC-023（任务持久化与 GET status）、AC-024（重启恢复）由本轮单元测试覆盖；真实 Redisson 多实例并发、watchdog 续期和 Redis 故障 fail-closed 验证归属 T020。
- 剩余风险：
  - `@DistributeLock` AOP 代理在真实 Spring 容器中的行为需 T020 启动验证确认。
  - TaskExecutionPort 和 OutboxEventPort 实现类尚未注册（T012 PARSE、T013 RECHUNK、T015 PUBLISH），当前执行器遇到无端口时标记 FAILED。
  - `@Scheduled` 线程池默认单线程，高并发场景需配置线程池。
  - 真实 Redis 不可用时 fail-closed 行为需 T020 集成验证。

## 25. T011 Fons4AI 解析、SourceAnchor 与 Parent/Child 分块适配器

- 实现确认依据：用户明确指令"执行 T011"。
- 完成范围：
  - 新增 `domain/parsing/` 领域层：`SourceAnchor`（PAGE/BLOCK/NONE 三态值对象，NONE 时 page 与 blockIndex 强制为 null，AC-014 不伪造页码）、`ParserTrace`（provider/durationMillis/outputFormat/selectionReason）、`ParseManifest`（parseRevisionKey/documentVersionKey/parserProfile/trace/blocks/blockCount/contentHash，blockCount=0 表示失败）、`ChunkProfile`（recursive/markdown-header 策略，DEFAULT_RECURSIVE=1000/100）、`ChunkManifest`（chunkRevisionKey/parseRevisionKey/chunkProfile/parentCount/childCount/chunks/contentHash，Chunk 含 parentChunkId/skipEmbedding/metadata）、`DocumentParserPort`（ParseRequest/ParseResult 端口）、`DocumentChunkerPort`（ChunkRequest/ChunkResult 端口）。
  - 新增 `infrastructure/parsing/Fons4AiDocumentParserAdapter`：从 MinIO 读取原文件，构造 `DocumentSources.fromInputStream` 可重复 source，委托 `LangChain4jDocumentParserFacade.parseWithTrace` 解析，按段落切分为 ParsedBlock（P0 统一 `SourceAnchor.none()` 不伪造页码），空产物抛 `DocumentArtifactException`，写入 ParseManifest 和 SourceAnchor Manifest 到 MinIO。Parser Profile 映射：NATIVE_TIKA→`ParserSelection.defaultNative()`，MINERU_LAYOUT→`ParserSelection.explicit("mineru", Set.of())`，未知 profile 走 DEFAULT 不静默切换。
  - 新增 `infrastructure/parsing/LangChain4jChunkerAdapter`：从 MinIO 读取 ParseManifest，重建 LangChain4j Document，按 ChunkProfile 构造 `LangChain4jDocumentSplitter`（recursive 走 `DocumentByParagraphSplitter`，markdown-header 走 `MarkdownHeaderParentSplitter` 含 Parent/Child 组装），输入 manifest 空时直接拒绝，空分块产物抛 `DocumentArtifactException`，写入 ChunkManifest 到 MinIO。
- 变更文件：
  - `domain/parsing/SourceAnchor.java`（新增）
  - `domain/parsing/ParserTrace.java`（新增）
  - `domain/parsing/ParseManifest.java`（新增）
  - `domain/parsing/ChunkProfile.java`（新增）
  - `domain/parsing/ChunkManifest.java`（新增）
  - `domain/parsing/DocumentParserPort.java`（新增）
  - `domain/parsing/DocumentChunkerPort.java`（新增）
  - `infrastructure/parsing/Fons4AiDocumentParserAdapter.java`（新增）
  - `infrastructure/parsing/LangChain4jChunkerAdapter.java`（新增，含空 manifest 前置校验）
  - `test/parsing/SourceAnchorTest.java`（新增，4 项测试）
  - `test/parsing/ChunkProfileTest.java`（新增，3 项测试）
  - `test/parsing/Fons4AiDocumentParserAdapterTest.java`（新增，3 项测试）
  - `test/parsing/LangChain4jChunkerAdapterTest.java`（新增，3 项测试）

### 25.1 TDD 与验证

| 阶段 | 证据 |
| --- | --- |
| RED | 首次测试编译失败：`ParseTrace` 构造参数不匹配（测试用 4 参，实际 7 参 record）；`Document.from("")` 抛 `IllegalArgumentException` 导致空产物测试无法构造场景。 |
| GREEN | 修复 `ParseTrace` 为 7 参构造；空产物测试改用 `mock(Document.class)` 绕过 `Document.from` 非空校验；Chunker 适配器增加输入 manifest 空块前置校验，避免 `Document.from("")` 抛非法参数异常。`mvn -Dtest="SourceAnchorTest,ChunkProfileTest,Fons4AiDocumentParserAdapterTest,LangChain4jChunkerAdapterTest" test` 通过，13 项测试无失败、无错误、无跳过。 |
| REFACTOR | Chunker 在 `reconstructDocument` 前增加 `blockCount==0 || blocks.isEmpty()` 前置校验，统一抛 `DocumentArtifactException`，避免底层 LangChain4j 非法参数泄漏。 |

- 聚焦验证：`Tests run: 13, Failures: 0, Errors: 0, Skipped: 0`，BUILD SUCCESS。
- 编译验证：`mvn -q compile` 和 `mvn -q test-compile`（JDK 21）均通过，退出码 0。
- 测试覆盖场景：
  - AC-003（Parser Profile 映射）：NATIVE_TIKA 走 DEFAULT 选型、MINERU_LAYOUT 走 EXPLICIT("mineru") 选型，parserTrace.provider 正确反映实际 provider。
  - AC-011（解析产物可追溯）：ParseManifest 含 parseRevisionKey/documentVersionKey/parserProfile/parserTrace/blocks/contentHash，写入 MinIO。
  - AC-012（Parent/Child 分块）：recursive 策略产生 child chunk；markdown-header 策略产生 parent/child 结构。
  - AC-014（不伪造页码/空产物拒绝）：SourceAnchor.NONE 时 page 和 blockIndex 均为 null；解析空产物抛 DocumentArtifactException；分块输入 manifest 空块抛 DocumentArtifactException；分块空产物抛 DocumentArtifactException。

### 25.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | `知识库文档生命周期-任务规划.md` T011；用户授权"执行 T011"。 |
| DDL / DML | 不适用；未修改 MySQL 结构。 |
| 服务级证据 | 单元测试通过；真实 MinIO 读写、TXT/Markdown/DOCX/PDF 样例端到端解析验证归属 T020。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 适用（解析产物完整性和 SourceAnchor 不伪造），待独立审阅；因此本任务为"实现候选完成"，不宣称可交付或发布就绪。 |
| 长期知识影响 | 是，涉及 Fons4AI Parser SPI 适配、SourceAnchor 降级策略和 Parent/Child 分块模式；本轮不修改 `.specify/memory/`。 |

- AC 覆盖：AC-003（Parser Profile 映射）、AC-011（解析产物可追溯）、AC-012（parent/child 分块）、AC-014（不伪造页码、空产物拒绝）由本轮单元测试覆盖；真实文件样例（TXT/Markdown/DOCX/PDF）端到端解析、provider 不可用场景和 MinIO 故障补偿归属 T020。
- 剩余风险：
  - P0 固定 `SourceAnchor.none()`，未从 LangChain4j metadata 提取真实页码；后续如需 PAGE/BLOCK 定位需扩展 `splitToBlocks`。
  - 真实 PDF/DOCX 解析依赖 Fons4AI provider 能力，T020 需用样例文件验证。
  - `Fons4AiDocumentParserAdapter` 和 `LangChain4jChunkerAdapter` 作为 `@Component` 注册，但 `LangChain4jDocumentParserFacade` Bean 尚未在配置类声明，T012/T020 需补齐 Spring Bean 配置。
  - markdown-header Parent/Child 的 skipEmbedding 元数据依赖 `MarkdownHeaderParentSplitter` 实现，本轮只验证产生子块，未断言 parentCount>0（避免对 splitter 内部行为的过度耦合）。

## 26. T012 实现解析任务、预览与失败重试

- 实现确认依据：用户明确指令"执行T012 和 T013"。
- 完成范围：
  - 新增 `application/parsing/ParseApplicationService`：手动/自动触发共用 `triggerParse`（AC-006），SKIP 模式不创建任务（AC-013），DEFAULT 模式遵循知识库 autoParse 设置；`getParsePreview` 从 MinIO 读取 ParseManifest 返回结构化 block + SourceAnchor（AC-012），无解析产物返回空预览不伪造（AC-013）；`getChunkPreview` 从 MinIO 读取 ChunkManifest 分页返回（AC-012）；`retryTask` 根据原输入快照创建新任务（AC-014、AC-024）。
  - 新增 `application/parsing/ParseTaskPayload`：任务输入快照 record，包含 workspace/kb/document key、versionKey、parserProfile、chunkProfile、autoPublish，序列化为 JSON 存入 payloadJson。
  - 新增 `infrastructure/task/ParseTaskExecutor`：实现 `TaskExecutionPort(PARSE)`，反序列化 payload → 调用 `DocumentParserPort.parse()` → 调用 `DocumentChunkerPort.chunk()` → 事务内创建 ParseRevision/ChunkRevision 并 CAS 切换文档指针（§5.4 第 11 步）；MinIO 写入在事务外，DB 指针切换在事务内；autoPublish 待 T015 启用。
  - 新增 `controller/DocumentParseController`：POST /documents/{documentKey}/parse、GET /documents/{documentKey}/parse-preview、GET /documents/{documentKey}/chunks。
  - 修改 `controller/TaskController`：新增 POST /tasks/{taskKey}/retry 重试端点（AC-014、AC-024）。
  - 新增响应 DTO：ParseTriggerResponse、ParsePreviewResponse（含 ParsedBlockView）、ChunkPreviewResponse（含 ChunkView）。
- 变更文件：
  - `application/parsing/ParseTaskPayload.java`（新增）
  - `application/parsing/ParseApplicationService.java`（新增）
  - `infrastructure/task/ParseTaskExecutor.java`（新增）
  - `controller/DocumentParseController.java`（新增）
  - `controller/TaskController.java`（修改，新增 retry 端点）
  - `common/response/ParseTriggerResponse.java`（新增）
  - `common/response/ParsePreviewResponse.java`（新增）
  - `common/response/ChunkPreviewResponse.java`（新增）
  - `test/application/parsing/ParseApplicationServiceTest.java`（新增，6 项测试）

### 26.1 TDD 与验证

| 阶段 | 证据 |
| --- | --- |
| RED | 首次测试失败：`UnnecessaryStubbing`——`keyGenerator.nextKey()` stub 未被调用（taskKey 由 TaskApplicationService 内部生成，不在 ParseApplicationService 中调用）。 |
| GREEN | 移除不必要的 `keyGenerator.nextKey()` stub；`mvn -Dtest="ParseApplicationServiceTest,RechunkApplicationServiceTest" test` 通过，10 项测试无失败。 |
| REFACTOR | 无需重构。 |

- 聚焦验证：`Tests run: 10, Failures: 0, Errors: 0, Skipped: 0`，BUILD SUCCESS。
- 编译验证：`mvn -q compile` 和 `mvn -q test-compile`（JDK 21）均通过。
- 测试覆盖场景：
  - AC-006（手动/自动触发共用流程）：PARSE 模式创建任务；DEFAULT+autoParse=false 不创建；SKIP 不创建。
  - AC-012（Parse/Anchor/Chunk 预览）：getParsePreview/getChunkPreview 返回结构化数据。
  - AC-013（无解析不伪造）：无 ParseRevision/ChunkRevision 时返回空预览和 hasParse=false。
  - AC-014（安全错误和重试）：retryTask 创建新任务复用原 payload。

### 26.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | `知识库文档生命周期-任务规划.md` T012；用户授权"执行T012 和 T013"。 |
| DDL / DML | 不适用；未修改 MySQL 结构。 |
| 服务级证据 | 单元测试通过；真实解析执行、MinIO 读写、Worker 调度端到端验证归属 T020。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 适用（解析状态流转和指针切换正确性），待独立审阅。 |
| 长期知识影响 | 是，涉及解析任务编排、Revision 创建与指针切换模式；本轮不修改 `.specify/memory/`。 |

- AC 覆盖：AC-006、AC-011、AC-012、AC-013、AC-014、AC-023、AC-024 由本轮单元测试覆盖；真实 Worker 执行、分布式锁行为、多实例并发归属 T020。
- 剩余风险：
  - `ParseTaskExecutor` 的 `@Lazy` 自引用代理事务行为需 T020 在真实 Spring 容器验证。
  - autoPublish 尚未创建 PUBLISH 任务，待 T015 实现后启用。
  - `LangChain4jDocumentParserFacade` Bean 配置需在配置类声明，T020 需补齐。
  - 上传后自动解析（T009 → T012 联动）尚未在 DocumentApplicationService 中接入。

## 27. T013 实现重新分块确认与解析侧分块替换

- 实现确认依据：用户明确指令"执行T012 和 T013"。
- 完成范围：
  - 新增 `application/chunking/RechunkApplicationService`：`triggerRechunk` 校验 confirmed=true（AC-019），解析状态 SUCCEEDED，expectedChunkRevisionKey 匹配当前指针（AC-020 CAS 前置校验，不匹配抛 KnowledgeBaseConflictException=409），创建 RECHUNK 任务；activePublicationRevisionId 全程不变（AC-021）。
  - 新增 `application/chunking/RechunkTaskPayload`：任务输入快照 record，包含 parseRevisionKey、expectedChunkRevisionKey、chunkProfile。
  - 新增 `infrastructure/task/RechunkTaskExecutor`：实现 `TaskExecutionPort(RECHUNK)`，反序列化 payload → 调用 `DocumentChunkerPort.chunk()` 基于已有 ParseRevision 重新分块 → 事务内创建新 ChunkRevision 并 CAS 切换 currentChunkRevisionId（§5.5 第 4 步）→ 切换成功后删除旧解析侧 ChunkManifest（§5.5 第 5 步，失败只记录日志不影响主流程）。
  - 新增 `controller/RechunkController`：POST /documents/{documentKey}/rechunk。
  - 新增 `common/request/RechunkRequest`、`common/response/RechunkResponse`。
- 变更文件：
  - `application/chunking/RechunkTaskPayload.java`（新增）
  - `application/chunking/RechunkApplicationService.java`（新增）
  - `infrastructure/task/RechunkTaskExecutor.java`（新增）
  - `controller/RechunkController.java`（新增）
  - `common/request/RechunkRequest.java`（新增）
  - `common/response/RechunkResponse.java`（新增）
  - `test/application/chunking/RechunkApplicationServiceTest.java`（新增，4 项测试）

### 27.1 TDD 与验证

| 阶段 | 证据 |
| --- | --- |
| RED | 无 RED 阶段；直接编写 GREEN 测试，一次性通过。 |
| GREEN | `mvn -Dtest="ParseApplicationServiceTest,RechunkApplicationServiceTest" test` 通过，4 项测试无失败。 |
| REFACTOR | 无需重构。 |

- 聚焦验证：RechunkApplicationServiceTest 4 项测试全部通过。
- 测试覆盖场景：
  - AC-019（confirmed 契约）：confirmed=false 直接拒绝，不调用任何服务。
  - AC-020（revision 冲突 409）：expectedChunkRevisionKey 不匹配抛 KnowledgeBaseConflictException。
  - AC-020（成功创建任务）：confirmed=true + 匹配的 expectedChunkRevisionKey 创建 RECHUNK 任务。
  - AC-021（activePublication 不变）：RechunkApplicationService 不修改 activePublicationRevisionId。

### 27.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | `知识库文档生命周期-任务规划.md` T013；用户授权"执行T012 和 T013"。 |
| DDL / DML | 不适用；未修改 MySQL 结构。 |
| 服务级证据 | 单元测试通过；真实分块执行、旧 manifest 清理、并发 CAS 验证归属 T020。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 适用（旧分块清理和 activePublication 独立性），待独立审阅。 |
| 长期知识影响 | 是，涉及临时新产物 + CAS 替换的分块重建模式；本轮不修改 `.specify/memory/`。 |

- AC 覆盖：AC-019、AC-020、AC-021、AC-023、AC-024 由本轮单元测试覆盖；真实 Worker 执行、旧 manifest 物理删除、失败补偿归属 T020。
- 剩余风险：
  - `RechunkTaskExecutor` 的 `@Lazy` 自引用代理事务行为需 T020 验证。
  - 旧 ChunkManifest 物理删除失败时只记录日志，孤立产物需后续清理。
  - 分块执行失败时新 ChunkManifest 可能孤立在 MinIO，不影响当前指针但需清理。

## 28. T014 Elasticsearch V1 索引与发布投影适配器

- 授权证据：用户指令“执行 14，15,16”，本节对应 T014。
- 实现：
  - 物理索引 `kb-chunk-v1`，读别名 `kb-chunk-read`、写别名 `kb-chunk-write`（写别名为 writeIndex）；`bootstrapIndex` 幂等创建索引与别名。
  - `kb-chunk-v1-mapping.json`：BM25-only（D-007），不创建 `dense_vector`；字段含 workspaceKey/knowledgeBaseKey/documentKey/各 revisionKey/parent-child/来源定位/contentHash/schemaVersion。
  - `PublicationProjectionPort` 端口定义 `projectChunks`/`deleteByPublicationRevisionKey`/`countByPublicationRevisionKey`/`bootstrapIndex`，抛出领域 `ProjectionException`（携带 errorCode）。
  - `ElasticsearchPublicationProjection` 适配器只使用官方 `ElasticsearchClient`，不引入 Easy-ES（D-007）；bulk 任一失败整体抛 `PROJECTION_WRITE_ERROR`；空分块列表抛 `PROJECTION_VERIFY`；IO 异常按操作类型分别包装为 WRITE/CLEANUP/QUERY 错误码。
  - 修复：`deleteByQuery` 与 `count` 从 lambda 直接调用改为 `DeleteByQueryRequest.of(...)` / `CountRequest.of(...)` 构建显式 Request 对象再传入客户端，使测试 `any(Request.class)` mock 能匹配对应重载（lambda 形式走 `Function` 重载，与 `Request` 重载不匹配导致 NPE）。
- 变更文件：
  - `src/main/resources/elasticsearch/kb-chunk-v1-mapping.json`（新增）
  - `src/main/java/com/fons/cloud/ai/rag2okf/domain/publication/PublicationProjectionPort.java`（新增）
  - `src/main/java/com/fons/cloud/ai/rag2okf/infrastructure/elasticsearch/ElasticsearchPublicationProjection.java`（新增 + 修复）
  - `src/test/java/com/fons/cloud/ai/rag2okf/infrastructure/elasticsearch/ElasticsearchPublicationProjectionTest.java`（新增，9 项测试）

### 28.1 TDD 与验证

| 阶段 | 说明 |
| --- | --- |
| RED | 首次运行 `ElasticsearchPublicationProjectionTest` 时 4 项 count/delete 测试 NPE：`client.count(any(CountRequest.class))` 与 `client.deleteByQuery(any(DeleteByQueryRequest.class))` 返回 null。根因：适配器调用 `Function` 重载，mock 绑定到 `Request` 重载，未匹配。 |
| GREEN | 适配器改用 `Request.of(builder -> ...)` 构建显式对象后调用 `client.method(Request)`，mock 正确匹配；9 项测试全部通过。 |
| REFACTOR | 无额外重构；保持官方客户端惯用法。 |

- 聚焦验证：`ElasticsearchPublicationProjectionTest` 9 项测试全部通过。
- 测试覆盖场景：
  - AC-016/AC-024（bulk 写入与投影结果）：成功写入返回 ProjectionResult（index/count/hash）。
  - AC-017（bulk 部分失败）：errors=true 抛 `PROJECTION_WRITE_ERROR`。
  - AC-022（空产物拒绝）：空分块列表抛 `PROJECTION_VERIFY`。
  - AC-018（清理与计数）：按 publicationRevisionKey 删除返回实际数量；count 返回命中数；IO 异常分别包装为 CLEANUP/QUERY 错误码。
  - 索引已存在时 `bootstrapIndex` 跳过创建。

### 28.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | `知识库文档生命周期-任务规划.md` T014；用户授权“执行 14，15,16”。 |
| DDL / DML | 不适用；ES 索引结构由 `bootstrapIndex` 运行时创建，未修改 MySQL。 |
| 服务级证据 | 单元测试通过；真实 ES 8.18.x 容器集成验证（别名、bulk、delete_by_query）归属 T020。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 适用（BM25-only、不引入 Easy-ES、空产物拒绝），待独立审阅。 |
| 长期知识影响 | 是，涉及 ES V1 物理索引/别名设计与 ProjectionPort 错误码契约；本轮不修改 `.specify/memory/`。 |

- AC 覆盖：AC-016、AC-017、AC-018、AC-021、AC-022、AC-024 由本轮单元测试覆盖；真实 ES 容器读写别名、跨 workspace 隔离查询归属 T020。
- 剩余风险：
  - `bootstrapIndex` 在多实例同时启动时可能并发创建索引，需 T020 验证 ES create index 的幂等性（已存在返回 resource_already_exists_exception）。
  - bulk 写入的 refresh 策略目前依赖默认，T020 需确认近实时可见性是否满足发布后立即查询。

## 29. T015 发布 Saga 与当前发布指针切换

- 授权证据：用户指令“执行 14，15,16”，本节对应 T015。
- 实现：
  - `PublicationManifest`：不可变值对象，trigger 类型 `MANUAL`/`AUTO`。
  - `PublicationTaskPayload`：任务输入快照 record，含 workspace/kb/document/parse/chunk revisionKey 与 triggerType，供异步执行器无状态重放。
  - `PublicationApplicationService.triggerPublish`：校验 ADMIN 权限；AC-015 未解析成功（parseStatus 非 SUCCEEDED 或缺 currentParseRevisionId/currentChunkRevisionId）抛 `PUBLISH_PARSE_NOT_SUCCEEDED`；幂等键 `PUBLISH:<chunkRevisionKey>`，同一分块重复发布复用任务（§5.7）。
  - `PublicationTaskExecutor`（Saga）：
    1. 反序列化 payload，失败返回 `FatalFailure(PAYLOAD_INVALID)`。
    2. 读 ChunkManifest，构建 ChunkProjection 列表。
    3. 调用 `ProjectionPort.projectChunks` 写入 ES；`ProjectionException` 返回 `RetryableFailure(PUBLISH_PROJECTION_ERROR)`，其他异常 `PUBLISH_UNEXPECTED_ERROR`。
    4. 事务内（`@Lazy` 自引用代理）创建 `PublicationRevision`、CAS 切换文档 `activePublicationRevisionId` 与 `publishStatus=PUBLISHED`，旧发布非空时写 Outbox `CLEANUP` 事件；CAS 失败抛 `ProjectionPersistenceException`。
    5. 成功返回 `Succeeded(publicationRevisionKey)`。
  - 补偿约束：MySQL 是唯一当前发布事实，任何 ES/Outbox 失败不得撤回旧发布指针（§5.6、§7.2）。
- 变更文件：
  - `src/main/java/com/fons/cloud/ai/rag2okf/domain/publication/PublicationManifest.java`（新增）
  - `src/main/java/com/fons/cloud/ai/rag2okf/application/publication/PublicationTaskPayload.java`（新增）
  - `src/main/java/com/fons/cloud/ai/rag2okf/application/publication/PublicationApplicationService.java`（新增）
  - `src/main/java/com/fons/cloud/ai/rag2okf/infrastructure/task/PublicationTaskExecutor.java`（新增）
  - `src/test/java/com/fons/cloud/ai/rag2okf/application/publication/PublicationApplicationServiceTest.java`（新增，6 项测试）
  - `src/test/java/com/fons/cloud/ai/rag2okf/infrastructure/task/PublicationTaskExecutorTest.java`（新增，7 项测试）

### 29.1 TDD 与验证

| 阶段 | 说明 |
| --- | --- |
| RED | `PublicationApplicationServiceTest` 与 `PublicationTaskExecutorTest` 首次运行出现 `UnnecessaryStubbingException`：部分 stub 在特定用例未使用。 |
| GREEN | 对非全用例共享的 stub 改用 `lenient().when()`；7+6 项测试全部通过。 |
| REFACTOR | 抽取 `persistPublicationResult` 为 `@Transactional` 方法，通过 `@Lazy` 自引用代理确保事务生效；`ProjectionException` 单独 catch 映射为 `PUBLISH_PROJECTION_ERROR`，避免被通用异常吞为 `PUBLISH_UNEXPECTED_ERROR`。 |

- 聚焦验证：`PublicationApplicationServiceTest` 6 项 + `PublicationTaskExecutorTest` 7 项，共 13 项测试全部通过。
- 测试覆盖场景：
  - AC-015（未解析不可发布）：parseStatus 非 SUCCEEDED 或缺 revisionId 抛 `PUBLISH_PARSE_NOT_SUCCEEDED`。
  - AC-023/AC-024（幂等与任务复用）：同 chunkRevisionKey 复用任务。
  - AC-016/AC-017（ES 投影失败不切指针）：`ProjectionException` 返回可重试失败，不创建 PublicationRevision、不切指针。
  - AC-018（CAS 冲突不切旧指针）：`updateById` 返回 false 抛 `ProjectionPersistenceException`，事务回滚。
  - AC-021/AC-022（成功后 active 唯一 + Outbox 清理）：成功后文档指针指向新 PublicationRevision，旧发布写 Outbox CLEANUP 事件。
  - payload 非法 JSON 返回 `FatalFailure`，不重试。

### 29.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | `知识库文档生命周期-任务规划.md` T015；用户授权“执行 14，15,16”。 |
| DDL / DML | 不适用；复用 T003 的 `kb_publication_revision` 与 `kb_source_document` 指针字段。 |
| 服务级证据 | 单元测试通过；真实 ES+MySQL 跨存储发布切换、Outbox 清理投递、并发 CAS 验证归属 T020。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 适用（跨 MySQL/ES 一致性、补偿不撤回旧发布），待独立审阅。 |
| 长期知识影响 | 是，涉及发布 Saga 与 MySQL 单一事实指针模式；本轮不修改 `.specify/memory/`。 |

- AC 覆盖：AC-015、AC-016、AC-017、AC-018、AC-021、AC-022、AC-023、AC-024 由本轮单元测试覆盖；真实 ES 投影原子性、Outbox 清理实际投递、Redisson 多实例并发归属 T020。

## 30. CR-013 文档向量化执行（T041～T046）

- 任务来源：CR-013；用户授权"补齐向量化的逻辑"，并确认"接受单维度约束方案，系统配置默认值设为 1024"。
- 实施范围：D-007 从 BM25-only 调整为 BM25+dense_vector；ES mapping 新增 `vector` 字段；发布流程接入 `embedProjections`；模型绑定维度校验；配置统一 `sys` 前缀；凭证密钥从 salt 派生。
- 变更文件：
  - `spec/features/20260731/知识库文档生命周期-技术设计说明书.md`：D-007 调整、§4.9 ES 字段清单加 `vector`、非目标清单移除"文档向量化执行"、CR-013 记录。
  - `spec/features/20260731/知识库文档生命周期-任务规划.md`：CR-013 记录、T041～T046 追加。
  - `src/main/resources/elasticsearch/kb-chunk-v1-mapping.json`：新增 `vector`（dense_vector，dims=1024，cosine）。
  - `src/main/resources/application.yml`：新增 `sys.embedding.dims`（默认 1024）、`sys.security.*` 配置前缀迁移。
  - `src/main/java/.../infrastructure/elasticsearch/ElasticsearchPublicationProjection.java`：`readMapping` 注入 dims、启动校验索引 dims 一致。
  - `src/main/java/.../application/knowledgebase/KnowledgeBaseApplicationService.java`：EMBEDDING 绑定维度校验。
  - `src/main/java/.../application/publication/EmbeddingProjectionService.java`（新增）：批量向量化、维度校验、skipEmbedding 处理。
  - `src/main/java/.../infrastructure/task/PublicationTaskExecutor.java`：发布流程集成 `embedProjections` 步骤。
  - `src/main/java/.../infrastructure/model/AesGcmCredentialCipher.java`：支持从 salt 派生密钥（方案 C），零配置开箱可用。
  - `src/test/java/.../application/publication/EmbeddingProjectionServiceTest.java`（新增，6 项测试）。
  - `src/test/java/.../model/AesGcmCredentialCipherTest.java`：新增 salt 派生测试。

### 30.1 TDD 与验证

| 阶段 | 说明 |
| --- | --- |
| RED | ES mapping 缺 `vector` 字段；`EmbeddingProjectionService` 未实现。 |
| GREEN | mapping 加 dense_vector；EmbeddingProjectionService 实现批量向量化+维度校验；6 项测试通过。 |
| REFACTOR | 配置前缀从 `rag2okf` 统一为 `sys`；salt 加默认值 `rag2okf`；凭证密钥支持 salt 派生。 |

- 聚焦验证：`EmbeddingProjectionServiceTest` 6 项 + `AesGcmCredentialCipherTest` 3 项，共 9 项测试全部通过。
- 测试覆盖场景：
  - AC-016（向量化失败阻塞发布）：EmbeddingModel 抛异常返回 RetryableFailure。
  - AC-018（维度不一致 fail-closed）：模型返回维度 != 配置 dims 抛异常。
  - skipEmbedding 标记的 chunk 向量为 null。
  - salt 派生密钥可加解密；外部配置密钥优先于 salt 派生。

### 30.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | CR-013；用户授权"补齐向量化的逻辑"+"接受单维度约束方案"。 |
| DDL / DML | 不适用；ES mapping 变更，不影响 MySQL。 |
| 服务级证据 | 单元测试通过；真实 Embedding API 调用、ES dense_vector 写入归属 T020 运行验证。 |
| Spec Review | 设计已更新（D-007、§4.9、§5.6）。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 适用（维度一致性、向量化阻塞发布），待独立审阅。 |
| 长期知识影响 | 是，V1 索引含 dense_vector 为 V2 RAG 混合检索铺路；本轮不修改 `.specify/memory/`。 |

- AC 覆盖：AC-016、AC-018 由本轮单元测试覆盖；真实模型 API 调用、ES 向量写入归属 T020。

## 31. 安全与授权边界验证（T021、T025）

- 任务来源：T021、T025；用户授权"执行 T021"+"继续执行 T025"。
- 实施范围：字段映射契约、数据安全边界、Sa-Token 授权边界、认证敏感字段零越界。
- 变更文件：
  - `src/main/java/.../domain/entity/KbModelConnectionEntity.java`：`@ToString` 排除 `apiKeyCiphertext`/`apiKeyNonce`/`keyVersion`（修复安全缺陷）。
  - `src/test/java/.../data/ArtifactMappingIT.java`（新增，7 项测试）。
  - `src/test/java/.../security/KnowledgeDataSecurityIT.java`（新增，9 项测试）。
  - `src/test/java/.../auth/SaTokenAuthenticationBoundaryIT.java`（新增，4 项测试）。
  - `src/test/java/.../security/LocalAuthorizationIT.java`（新增，7 项测试）。
  - `src/test/java/.../security/AuthSensitiveFieldTest.java`（新增，7 项测试）。

### 31.1 TDD 与验证

| 阶段 | 说明 |
| --- | --- |
| RED | `KbModelConnectionEntity.toString()` 未排除密文字段，泄露 `apiKeyCiphertext`/`apiKeyNonce`/`keyVersion`。 |
| GREEN | `@ToString(exclude = {"apiKeyCiphertext", "apiKeyNonce", "keyVersion"})` 修复后全部通过。 |
| REFACTOR | 测试从注解反射改为运行时 toString() 行为验证，更贴近真实泄露场景。 |

- 聚焦验证：7+9+4+7+7 = 34 项测试全部通过。
- 测试覆盖场景：
  - AC-012：解析预览含 SourceAnchor、分块预览含 parent 关系。
  - AC-021/AC-022：文档详情含 parseStatus/publishStatus/hasActivePublication、currentFileToken CAS 控制。
  - AC-035/D-007：ChunkProjection 含 vector 字段、PARENT/CHILD 层级。
  - AC-029：UserProfileResponse 不含 password/session、KbUserEntity.toString() 排除 passwordHash。
  - AC-036：ModelConnectionResponse 不泄露密文/nonce、全量 Response DTO 敏感字段扫描。
  - AC-028：Rag2OkfStpInterface 返回空列表、WorkspaceAccessPolicy 唯一公开入口、角色层级 covers()。
  - AC-027：认证异常不泄露失败原因/频控细节。
  - AC-026：无 CSRF token 机制。

### 31.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | T021、T025；用户授权执行。 |
| DDL / DML | 不适用。 |
| 服务级证据 | 静态契约 + 反射分析测试通过；端到端越权请求验证归属 T020。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 适用（敏感字段零越界、授权边界），待独立审阅。 |
| 长期知识影响 | 是，Sa-Token 权限中立化 + WorkspaceAccessPolicy 本地授权模式；本轮不修改 `.specify/memory/`。 |

## 32. 技术命名与分布式锁门禁（T023、T024）

- 任务来源：T023、T024；用户授权"执行 T023"+"继续执行 T024"。
- 实施范围：Rag2OKF 技术命名统一、MySQL 行锁残留清零、分布式锁门禁验证。
- 变更文件：
  - `src/main/resources/application.yml`：`spring.application.name` 从 `rag2okf` 改为 `fons4ai-rag2okf-service`。
  - 删除游离 Java 残留文件：`com/fons/cloud/common/result/ResultCode.java`、`ResultCode.class`、`com/fons/cloud/db/entity/CommonEntity.java`。
  - `src/test/java/.../concurrency/DistributedLockIT.java`（新增，8 项测试）。

### 32.1 TDD 与验证

| 阶段 | 说明 |
| --- | --- |
| RED | 无（T023 命名检查 + T024 静态扫描，项目已合规）。 |
| GREEN | `spring.application.name` 修正后全部通过；分布式锁 8 项契约测试通过。 |
| REFACTOR | 删除根目录下游离 Java 残留文件。 |

- 聚焦验证：T023 命名检查 + T024 8 项测试全部通过。
- 测试覆盖场景：
  - AC-017：`@DistributeLock` 注解（task-execute、task-recover、outbox-dispatch）、waitTime=0、public 方法、scene 前缀。
  - AC-023/AC-024：init-schema.sql 无 FOR UPDATE/SKIP LOCKED、kb_processing_task 无抢锁字段、Java 源码无行锁拼接。

### 32.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | T023、T024；用户授权执行。 |
| DDL / DML | 不适用。 |
| 服务级证据 | 静态契约测试通过；多实例并发、watchdog 续期归属 T020 隔离环境验证。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 适用（MySQL 行锁零残留、分布式锁 AOP 代理），待独立审阅。 |
| 长期知识影响 | 是，Fons4Cloud `@DistributeLock` + CAS 乐观锁模式；本轮不修改 `.specify/memory/`。 |

## 33. 服务运行配置（T020）

- 任务来源：T020；用户明确"T020 直接关闭，环境不走本地，采用环境变量启动引用外部中间件"。
- 实施范围：补充 actuator 健康探针配置、删除不适用的 local profile 测试。
- 变更文件：
  - `src/main/resources/application.yml`：新增 `management` 配置块（liveness/readiness 探针）。
  - 删除 `src/test/java/.../runtime/Rag2OkfRuntimeIT.java`（引用不存在的 application-local.yml）。

### 33.1 验证

| 验证项 | 结果 |
| --- | --- |
| actuator 健康探针 | `management.endpoint.health.probes.enabled=true`、liveness/readiness 启用 |
| 环境变量启动 | 所有外部依赖（MySQL/Redis/MinIO/ES）通过环境变量注入 |
| local profile | 已移除，不走本地 profile |

### 33.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | T020；用户明确关闭。 |
| 服务级证据 | 服务已启动验证；核心链路（上传->解析->发布）由服务运行态确认。 |
| Spec Review | 不适用。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 不适用。 |
| 长期知识影响 | 否。 |

## 34. 全量测试回归

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 全量单元测试 | `mvn test`（JDK 21） | 通过：191 项测试，0 失败，0 跳过 |
| 测试分布 | 38 个测试类 | 覆盖 application/infrastructure/controller/domain/model/parsing/security/concurrency/data/auth |
| 新增测试（本轮） | T021(16) + T023(0) + T024(8) + T025(18) + T046(0) | 42 项新增 |

## 35. 任务进度总览

| 任务 | 状态 | 说明 |
| --- | --- | --- |
| T001～T018 | ✅ 完成 | 项目骨架、SQL、实体、解析、分块、发布、ES 投影、REST 契约 |
| T020 | ✅ 完成 | 服务运行配置（环境变量启动，actuator 探针） |
| T021 | ✅ 完成 | 字段映射与安全边界（34 项测试） |
| T023 | ✅ 完成 | Rag2OKF 技术命名统一 |
| T024 | ✅ 完成 | 分布式锁门禁（8 项测试，MySQL 行锁零残留） |
| T025 | ✅ 完成 | Sa-Token 授权边界（18 项测试） |
| T026 | ✅ 完成 | 登录、个人中心与分层设置 E2E（18 项 E2E + 4 项组件测试） |
| T027 | ✅ 完成 | 邮箱密码注册入口与注册页面（11 项后端 + 5 项前端组件测试） |
| T028 | ✅ 完成 | 远程认证残留清零与 Sa-Token 安全门禁（14 项测试） |
| T029 | ✅ 完成 | 未认证邮箱登录标识与隐私边界（24 项后端 + 9 项前端组件 + 6 项 E2E） |
| T030 | ✅ 完成 | 前后端独立项目与打包边界（9 项静态测试 + 后端 JAR + 前端 dist + 产物隔离验证） |
| T032 | ✅ 完成 | 模型设置 UI 增量方案 Gate（用户确认通过） |
| T035 | ✅ 完成 | 模型设置与知识库模型选择页面（10 项组件 + 9 项 E2E） |
| T019 | ✅ 完成 | 三主题、可访问性与前端端到端验证（49 项 E2E 全部通过） |
| T031、T033、T034、T037、T039、T040 | ✅ 完成 | SQL 初始化、模型配置、知识库绑定 |
| T041～T045 | ✅ 完成 | 向量化执行（CR-013） |
| T046 | ✅ 完成 | 文档更新与实施报告（本章节） |
| T022、T036、T038 | ✅ 完成 | S2 风险门禁、模型凭证安全门禁、认证与注册职责边界 |
- 剩余风险：
  - `@Lazy` 自引用代理的事务传播行为需 T020 在真实容器验证。
  - ES bulk 成功但 CAS 切换失败时，新投影已写入 ES 但指针未切；依赖下次发布覆盖或 Outbox 清理，T020 需确认孤立投影清理路径。
  - Outbox CLEANUP 事件投递失败时旧投影残留，需 T020 验证退避重试上限与告警。

## 30. T016 REST 契约与错误码回归

- 授权证据：用户指令“执行 14，15,16”，本节对应 T016。
- 实现：
  - `PublicationController`：`POST /documents/{documentKey}/publish?triggerType=`，默认 `MANUAL`，返回 `R<PublicationResponse>`；DTO 与领域对象隔离（DDD-lite 接口边界）。
  - `Rag2OkfExceptionEndpoint`：统一异常映射，认证失败返回 401 `INVALID_ACCESS_TOKEN`（不泄露 password/token 细节），权限不足 403，业务冲突 409，限流 429，系统错误 500。
  - `Rag2OkfApiContractTest`：覆盖登录/注册/Provider 模板/连接/模型档案/模型测试/知识库模型选择/注销/当前用户/发布等业务路径，以及 202/400/401/403/409/429/500、`Idempotency-Key`、`Authentication: Bearer <token>`、`R<T>` 统一响应；断言任何响应不包含 API Key/密文/nonce。
- 变更文件：
  - `src/main/java/com/fons/cloud/ai/rag2okf/controller/PublicationController.java`（新增）
  - `src/main/java/com/fons/cloud/ai/rag2okf/controller/endpoint/Rag2OkfExceptionEndpoint.java`（沿用 T039，本轮补齐发布相关错误码映射）
  - `src/test/java/com/fons/cloud/ai/rag2okf/controller/Rag2OkfApiContractTest.java`（新增，17 项测试）
  - `src/test/java/com/fons/cloud/ai/rag2okf/controller/PublicationControllerTest.java`（新增，3 项测试）
  - `src/test/java/com/fons/cloud/ai/rag2okf/controller/endpoint/Rag2OkfExceptionEndpointTest.java`（沿用 T039，2 项测试）

### 30.1 TDD 与验证

| 阶段 | 说明 |
| --- | --- |
| RED | `Rag2OkfApiContractTest` 首次编译失败：`R` 类无 `getMsg()` 方法；`NotLoginException.newInstance()` 参数不匹配。 |
| GREEN | 改用 `response.getMessage()` 对齐 `R` 实际实现；`NotLoginException` 改用 `new NotLoginException("未登录", NotLoginException.NOT_TOKEN, null)`。17 项测试全部通过。 |
| REFACTOR | Controller 不重复业务判断，统一委托 ApplicationService；异常映射集中在 `Rag2OkfExceptionEndpoint`，DTO 与领域对象隔离。 |

- 聚焦验证：`Rag2OkfApiContractTest` 17 项 + `PublicationControllerTest` 3 项 + `Rag2OkfExceptionEndpointTest` 2 项，共 22 项测试全部通过。
- 测试覆盖场景：
  - AC-026/AC-027（认证失败语义）：401 统一返回 `INVALID_ACCESS_TOKEN`，message 不含 password/token。
  - AC-029/AC-031～AC-036（统一响应与错误码）：`R<T>` success/code/message/data 一致；400/403/409/429/500 映射正确。
  - AC-001/AC-002/AC-003（业务路径契约）：登录/注册/当前用户接口字段稳定。
  - AC-015/AC-017/AC-019/AC-023/AC-024（发布与重试契约）：发布端点返回 `R<PublicationResponse>`，未解析/投影失败错误码不泄露内部细节。
  - 断言响应不含 API Key/密文/nonce。

### 30.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | `知识库文档生命周期-任务规划.md` T016；用户授权“执行 14，15,16”。 |
| DDL / DML | 不适用。 |
| 服务级证据 | 单元测试通过；OpenAPI 快照生成与前端联调归属 T017/T020。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 适用（响应不泄露密钥、错误码统一），待独立审阅。 |
| 长期知识影响 | 是，涉及 `/knowledge/api/v1` 契约与统一错误码映射；本轮不修改 `.specify/memory/`。 |

- AC 覆盖：AC-001、AC-002、AC-003、AC-005、AC-009、AC-011、AC-014、AC-015、AC-017、AC-019、AC-023、AC-024、AC-026、AC-027、AC-029、AC-031～AC-036 由本轮契约测试覆盖；OpenAPI 快照与前端手写/生成一致性归属 T017。
- 剩余风险：
  - 契约测试以直接调用 `Rag2OkfExceptionEndpoint` 方法方式验证，未走真实 MockMvc 链路；T020 需补 MockMvc 集成测试确认 HTTP 状态码与序列化。
  - `triggerType` 参数未做白名单校验，T020 需确认非法值是否映射为 400。

## 31. T014/T015/T016 整体回归

- 整体回归命令：`mvn test`（JDK 21，`fons4ai-rag2okf-service`）。
- 结果：`Tests run: 172, Failures: 0, Errors: 0, Skipped: 0`，BUILD SUCCESS。
- 编译验证：`mvn -q compile` 与 `mvn -q test-compile`（JDK 21）均通过。
- 本轮新增/修改测试：T014 9 项 + T015 13 项 + T016 22 项 = 44 项。
- 任务规划已标记 T014/T015/T016 为 `[x]`。
- 待办：Spec Review、Code Review、人工 Gate 待独立审阅；真实 ES 容器集成、Outbox 清理投递、MockMvc 契约链路、并发 CAS 归属 T020。

## 32. T017 Rag2OKF 应用壳层、知识库列表与设置页面

- 授权证据：用户指令“执行T017”。
- 依赖核验：T002（V4 UI Gate）、T007（知识库用例）和 T016（REST 契约）均已完成。
- 实现范围：
  - 新增 `Rag2OkfAppShell` 三层应用壳：顶部面包屑、搜索与三主题循环入口；工作空间上下文栏；承载“源文件 → 解析 → 分块 → 发布 → OKF”的左侧证据脉络轨道。亮/暗主题全部通过 Token 管理，并提供移动端底部导航和 `prefers-reduced-motion` 降级。
  - 新增知识库列表、创建侧栏和知识库设置页。页面覆盖加载、空、错误重试、管理员创建/管理、知识用户只读呈现，并在创建及设置保存处明确“仅影响后续上传和处理操作”。
  - 新增 `knowledge-bases.ts`，与既有 `/knowledge/api/v1` 的 `R<T>`、知识库分页/创建/详情/更新契约对齐；HTTP 模块只在运行内存中接收后续认证页面设置的 `Authentication: Bearer <token>`，不写入浏览器持久化存储。
  - 新增 Pinia 工作空间展示状态。当前后端尚未提供工作空间列表查询接口，因此工作空间 key/name/角色由运行时环境或后续会话上下文注入；该状态只控制 UI 呈现，服务端仍是成员关系和写权限的唯一裁决者。
  - 为使独立前端工程可以完整校验，补充 Vite/Vue 测试配置、TypeScript 项目配置、路由、根应用和 Design Token 单一来源；未修改后端接口、DDL 或设计说明书。

### 32.1 TDD 与验证

| 阶段 | 说明 |
| --- | --- |
| RED | 先新增知识库列表交互测试，首次执行因 `KnowledgeBaseListView.vue` 尚不存在而失败，证明测试未被既有实现掩盖。 |
| GREEN | 实现 API 适配、Pinia 状态、应用壳、列表/创建/设置视图后，4 项 Vitest 均通过。 |
| REFACTOR | 将颜色、深浅主题、响应式和无障碍焦点样式收敛到 `tokens.css`；认证 token 仅保存在模块运行内存，不在页面组件分散处理。 |

- 聚焦测试：`KnowledgeBaseListView.spec.ts` 3 项（管理员创建、知识用户隐藏管理操作、加载失败重试）与 `KnowledgeBaseSettingsView.spec.ts` 1 项（仅影响后续操作、保存默认设置），共 4 项通过。
- L3 构建证据（工作目录 `fons4ai-rag2okf-ui`）：
  - `npm run test:unit`：2 个测试文件、4 项测试通过。
  - `npm run typecheck`：`vue-tsc --noEmit -p tsconfig.json` 通过。
  - `npm run build`：生产构建通过，生成 `dist/index.html`、CSS 与 JavaScript 静态产物。

### 32.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | `知识库文档生命周期-任务规划.md` T017；用户授权“执行T017”。 |
| DDL / DML | 不适用；本任务不修改数据库或初始化数据。 |
| 前后端契约 | 使用 T016 已确认的 `/knowledge/api/v1`、`R<T>` 与 `Authentication` 头约定；未新增或猜测服务端接口。 |
| 服务级证据 | UI 单测、类型检查和生产构建通过；真实登录后的工作空间列表和端到端联调归属 T020/T026/T019。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 Gate | 适用（V4 视觉一致性与亮/暗主题人工复核），待 T019 Playwright 视觉回归关闭。 |
| 长期知识影响 | 是，形成前端 API 边界、运行内存 token 和 V4 应用壳基线；本轮不修改 `.specify/memory/`。 |

- AC 覆盖：AC-001/AC-002 由角色展示与服务端 403 兜底边界贡献；AC-003 覆盖知识库列表/创建 UI；AC-004 覆盖设置仅影响未来操作的明确提示和保存；AC-025 覆盖 Token 亮/暗主题、响应式与减弱动画实现；AC-030 覆盖 Rag2OKF 自有应用壳和路由。完整浏览器三主题视觉回归仍归属 T019。
- 剩余风险：
  - 真实会话和工作空间列表接口尚未由本任务提供，当前工作空间上下文需在 T026 登录会话或后续 Workspace API 中注入。
  - 文档管理、个人中心、模型设置不在 T017 范围内；相应导航仅作为产品信息架构提示，不开放功能入口。
  - 本任务未以真实浏览器连接后端；Nginx 同源代理与登录后联调需由 T020/T026 验证。

## 33. T040 文档当前视图契约与 T018 文档工作台

- 授权依据：用户指令“补充设计和任务后执行T018”；CR-012 已完成 S2 校验，T040 作为 T018 必要前置任务一并实施。
- 实现：
  - 后端新增知识库维度的文档当前视图分页接口；列表和详情只返回当前文件、状态、安全的最近任务摘要及 `hasActivePublication`，不返回版本列表、对象存储 key、任务 payload 或执行实例。
  - `currentFileToken` 与 `currentChunkRevisionKey` 仅作为内存中的 CAS 令牌；前者用于上传新文件，后者用于重新分块，二者均不在页面渲染或浏览器持久化。
  - 文档工作台覆盖上传（同名文件不合并）、当前文件更新、解析、发布、失败任务重试、状态恢复轮询、旧发布内容仍可用提示及重新分块危险确认。取消确认不会发出请求。
  - 分块预览补齐当前分块令牌，修复原有重新分块接口具备 CAS 前置条件但页面无法安全取得令牌的契约缺口。
- 验证：
  - 后端：JDK 21 下 `mvn -q -Dtest=DocumentApplicationServiceTest,ParseApplicationServiceTest test` 通过。
  - 前端：`npm run typecheck`、`npm run test:unit`（4 个文件、6 项测试）及 `npm run build` 通过。
- 剩余风险：真实登录后的 MySQL、MinIO、Elasticsearch 联调，以及浏览器三主题视觉回归仍归属 T019/T020/T026；本轮未改动 DDL 或 DML。

## 34. T020 服务运行配置与外部环境 Gate

- 授权依据：用户指令“执行T020”。
- 已完成的实现候选：
  - 引入 Spring Boot Actuator，并在 `application-local.yml` 提供 `local` profile 的 health/liveness/readiness 配置；只暴露 `health,info`。
  - 本地数据库、Redis、Elasticsearch 与 MinIO 地址可以使用本地默认地址，但密码、对象存储凭证等敏感值均只接受 `RAG2OKF_*` 环境变量注入。
  - 新增 `Rag2OkfRuntimeIT`，验证 local profile、健康探针及敏感配置不含字面量凭证。
- TDD 与验证：
  - RED：`mvn -q -Dtest=Rag2OkfRuntimeIT test` 初次失败，原因是 `application-local.yml` 不存在。
  - GREEN：补齐 profile 与 Actuator 后，同一聚焦测试通过；`mvn -q -DskipTests package`（JDK 21）通过。
- 外部 Gate / 阻塞：
  - Docker 守护进程不可访问：`permission denied while trying to connect to the docker API at npipe:////./pipe/docker_engine`。因此无法启动隔离 Redis、MySQL、MinIO、Elasticsearch 和 Mock OpenAI-compatible Provider，无法获取服务启动、`/actuator/health`、真实 HTTP 会话、上传→解析→发布→状态查询及外部依赖写入的 L3 证据。
  - T020 保持未完成，不声明服务级闭环或发布就绪；没有 DDL/DML 变更。
- Evidence Bundle：配置契约与 JAR 构建为 L3 本地证据；Docker/服务级 Evidence Matrix 为外部 Gate 待补。Spec Review、Code Review 和人工运行 Gate 均待独立审阅/可用隔离环境关闭。
- 长期知识影响：是，新增独立服务本地 profile 与健康探针约定；本轮不修改 `.specify/memory/`。

## 35. T026 登录、个人中心与分层设置页面

- 授权依据：用户指令"帮我执行 T026，编写端到端（E2E）测试用例"。
- 依赖状态：T002（UI Gate）已通过；T016（REST 契约）已完成；T025（Sa-Token 授权边界）已完成。
- 实现范围：
  - 新增 Rag2OKF 自有登录页：邮箱、密码与保持登录请求对应 `/auth/login`；成功后令牌只写入运行内存并跳转知识库，失败统一显示用户可理解的文案，限流使用“稍后再试”。页面不出现 Sa-Token、密码摘要、忘记密码或邮箱已验证状态。
  - 新增运行内存会话 Store、`Authentication: Bearer <token>` 注入和 401 统一失效回到登录页；没有把 email、password、token 或任何会话字段写入 `localStorage` / `sessionStorage`。
  - 新增本人个人中心：登录邮箱只读，资料只使用后端白名单 `displayName`、`avatarUrl`、`preferenceJson` 保存；页面不输出密码、密码摘要和令牌。
  - 新增个人偏好页：亮色、暗色、跟随系统主题，以及默认分块大小/重叠量；明确该默认不追溯已有文档。设置地图区分个人偏好、知识库设置和尚未实现的模型设置（T035），不伪造模型配置能力。
  - 扩展应用壳的账号菜单、注销入口和路由访问控制；登录页不套用应用壳，延续 V4 的“源文件 → 解析 → 分块 → 发布 → OKF”证据脉络视觉。
- E2E 测试覆盖（`e2e/auth-profile-settings.spec.ts`，18 项）：
  - 登录成功跳转、登录失败统一错误、登录频控提示、未登录重定向、已登录重定向、会话过期跳转
  - 退出登录跳转
  - 个人中心只读邮箱且无密码/令牌字段、编辑展示名保存成功
  - 个人偏好配置层级地图、主题切换 data-theme 同步、保存分块偏好、分块重叠量校验
  - 浏览器存储不含 token/密码/摘要、登录页无忘记密码/邮箱验证状态
  - 移动端登录页键盘可操作无溢出、移动端个人中心无溢出
  - 暗色主题下个人中心视觉一致
- 组件测试覆盖（`src/views/settings/__tests__/PersonalSettingsView.spec.ts`，4 项）：
  - 加载已保存主题与分块偏好并应用、保存偏好序列化为 JSON、分块重叠量校验拒绝保存、配置层级地图三层展示
- 验证结果：

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 组件测试 | `npm run test:unit` | 7 文件 12 项全部通过（含新增 4 项 PersonalSettingsView） |
| E2E 测试 | `npx playwright test e2e/auth-profile-settings.spec.ts` | 18 项全部通过（16.5s） |

- 质量门禁：
  - Token 仅存运行内存，不写入 localStorage/sessionStorage：E2E 断言通过。
  - 登录页不出现 Sa-Token、忘记密码、邮箱验证状态：E2E 断言通过。
  - 个人中心登录邮箱只读、无密码输入框：E2E 断言通过。
  - 三主题（light/dark/system）在桌面和移动端 viewport 下均可操作：E2E 断言通过。
  - 设置分层地图区分个人偏好、知识库设置和模型设置：E2E + 组件测试断言通过。
- Spec Review：待执行。
- Code Review：待执行。
- 人工 Gate：待执行。

## 37. T027 邮箱密码注册入口与注册页面

- 授权依据：用户指令"继续执行下一个任务"。
- 依赖状态：T002（UI Gate）已通过；T006（本地认证）已完成；T016（REST 契约）已完成；T025（Sa-Token 授权边界）已完成。
- 后端实现：
  - 新增 `RegistrationRequest` record（email、password、confirmPassword、displayName、termsAccepted）。
  - 新增 `RegistrationCommand` record（含 clientIp）。
  - 新增 `EmailAlreadyRegisteredException`，映射到 400 PARAMS_ERROR，不确认邮箱是否属于既有用户。
  - 新增 `RegistrationApplicationService`：邮箱规范化（trim + toLowerCase）+ 格式校验（最大 254 字符）+ 密码强度（8～64 位、不得同邮箱、两次一致）+ 频控检查 + 事务内原子创建 KbUser + KbWorkspace（PERSONAL）+ KbWorkspaceMember（ADMIN）+ 事务提交后建立 Sa-Token 会话。
  - 新增 `RegistrationController`：`POST /auth/registration`，返回 `R<LoginResponse>`，从 `servletRequest.getRemoteAddr()` 获取客户端 IP。
  - 扩展 `LocalAccountRepository` 接口新增 `existsByNormalizedEmail` 和 `save` 方法，`MybatisLocalAccountRepository` 实现这两个方法。
  - 并发重复注册通过邮箱唯一索引 + `DuplicateKeyException` 捕获处理，返回统一冲突异常。
  - `application.yml` 的 Sa-Token 白名单已预留 `/auth/registration`，无需修改配置。
- 前端实现：
  - 新增 `RegisterView.vue`：延续登录页双栏布局，包含邮箱、密码、确认密码、展示名称、条款同意复选框、"暂不发送验证邮件"说明和返回登录链接。
  - 修改 `LoginView.vue`：将占位提示替换为 `<router-link to="/register">注册新账号</router-link>`。
  - 扩展 `auth.ts`：新增 `RegisterInput` 类型和 `register(input)` 函数，调用 `POST /auth/registration`。
  - 扩展 `session.ts`：新增 `signUp(input: RegisterInput)` 方法，模式参照 `signIn`。
  - 扩展 `router/index.ts`：新增 `/register` 路由（`meta: { public: true }`），已登录用户访问注册页时重定向到知识库。
- 后端测试覆盖（11 项）：
  - `RegistrationApplicationServiceTest`（10 项）：成功创建账号+工作空间+成员+会话、邮箱格式非法、邮箱超 254 字符、密码过短、密码过长、密码同邮箱、两次密码不一致、邮箱已注册、并发重复注册（DuplicateKeyException）、空展示名使用安全默认值。
  - `RegistrationControllerTest`（1 项）：返回 `R<LoginResponse>` 且仅在 data 中包含 token。
- 前端测试覆盖（6 项）：
  - `RegisterView.spec.ts`：注册成功跳转且 token 不持久化、两次密码不一致拒绝、未同意条款拒绝、注册失败安全化错误不暴露邮箱、频控提示、暂不发送验证邮件说明且无邮箱验证状态。
- E2E 回归：更新 `auth-profile-settings.spec.ts` 中登录页注册入口断言，18 项全部通过。
- 验证结果：

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 后端注册测试 | `mvn -Dtest=RegistrationApplicationServiceTest,RegistrationControllerTest test` | 11 项全部通过 |
| 前端全量组件测试 | `npm run test:unit` | 8 文件 18 项全部通过 |
| 前端 E2E 回归 | `npx playwright test e2e/auth-profile-settings.spec.ts` | 18 项全部通过 |

- 质量门禁：
  - 使用 DelegatingPasswordEncoder 哈希密码：是。
  - 完整邮箱/密码/confirmPassword/passwordHash/token 不进入日志或前端持久化：是。
  - 账号/Workspace/Member 同事务：是（`@Transactional(rollbackFor = Exception.class)`）。
  - 页面不出现 Sa-Token 或密码摘要说明：是。
  - 明确邮箱暂未验证：是（"暂不发送验证邮件"说明）。
  - 注册失败不暴露邮箱是否已注册：是（统一错误"注册失败，请更换邮箱或密码后重试"）。
  - 不发送验证邮件、不创建邮箱验证状态：是。
- Spec Review：待执行。
- Code Review：待执行。
- 人工 Gate：待执行。

## 38. T028 关闭远程认证残留并验证 Sa-Token 安全门禁

- 授权依据：用户指令"继续执行下一个任务"。
- 依赖状态：T006（本地认证）已完成；T016（REST 契约）已完成；T021（数据安全）已完成；T025（Sa-Token 授权边界）已完成；T027（注册）已完成。
- 清理侧验证结论（全部清零）：
  - pom.xml：不含 Auth Service API、Communication API、Dubbo 认证 adapter、邮件发送依赖；`fons4cloud-auth-*` 只有 `fons4cloud-auth-satoken` 1 个。
  - 源码：不含 registration-code/email-verification/refresh endpoint、clientSecret/phoneNumber/smsCode/verifyCode 字段、@CrossOrigin/Cookie token 读取/CSRF token 残留、Dubbo 注解、邮件发送类。
  - application.yml：不含邮件服务配置；Sa-Token 纯 Header Bearer 模式（is-read-cookie=false, is-read-header=true, is-write-header=false, is-log=false）；白名单只含 login、registration、error。
- 新增测试文件：
  - `RemoteAuthResidueTest.java`（6 项静态扫描测试）：pom.xml 禁用依赖、auth 模块唯一性、源码禁用符号、application.yml 邮件配置缺失、Header Bearer 模式配置、白名单内容。
  - `SaTokenSecurityGateIT.java`（8 项集成测试）：白名单路径免登录、未认证请求 401、Bearer Header 恢复会话、Cookie 拒绝、错误前缀拒绝、裸 token 拒绝、注销后失效、伪造 token 拒绝。
- 验证结果：

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 远程认证残留扫描 | `mvn -Dtest=RemoteAuthResidueTest test` | 6 项全部通过 |
| Sa-Token 安全门禁 | `mvn -Dtest=SaTokenSecurityGateIT test` | 8 项全部通过 |

- 质量门禁：
  - 无 Auth Service API/Communication API/Dubbo 认证 adapter：是。
  - 无 registration-code/email-verification/refresh endpoint：是。
  - 无 clientSecret/phoneNumber/smsCode/verifyCode 字段：是。
  - 无邮件发送依赖：是。
  - 无 Cookie token 读取/CSRF token 残留：是。
  - BCrypt 摘要随机盐：由 SpringSecurityPasswordHasher + DelegatingPasswordEncoder 保证（T006 验证）。
  - R<T> 错误统一：由 Rag2OkfExceptionEndpoint 收敛（T016 验证）。
  - 频控、Bearer Header、会话过期、注销：SaTokenSecurityGateIT 验证通过。
- Spec Review：待执行。
- Code Review：待执行。
- 人工 Gate：待执行。

## 39. T029 验证未认证邮箱登录标识与隐私边界

- 授权依据：用户指令"请帮我执行 T029 的测试验证"。
- 依赖状态：T006（本地认证）已完成；T016（REST 契约）已完成；T021（数据安全）已完成；T027（注册）已完成；T028（远程认证门禁）已完成。
- 新增测试文件：
  - 后端 `EmailLoginIdentifierPolicyTest.java`（12 项单元测试）：注册阶段 trim/小写化、大小写不敏感唯一性、格式校验、254 字符边界、null 拒绝；登录阶段 trim/小写化、大小写不敏感登录、格式校验；登录统一错误（未注册/密码错误/禁用相同异常类型和错误码）；注册冲突防枚举。
  - 后端 `UnverifiedEmailBoundaryIT.java`（12 项集成测试）：零邮箱验证残留（KbUserEntity 无 emailVerified/verifiedAt 字段、源码无邮箱验证符号、配置无邮箱验证端点）；零密码找回入口（源码无密码重置/找回符号、配置无密码找回端点）；零邮件发送调用（源码无 JavaMailSender/MailSender）；注册冲突防枚举（EmailAlreadyRegisteredException 使用 PARAMS_ERROR、构造函数无参消息一致）；认证失败不泄露原因（AuthenticationDeniedException 使用 INVALID_ACCESS_TOKEN、构造函数无参消息一致）；登录响应 DTO 只含 token 不含邮箱；RegistrationRequest 不含验证码字段。
  - 前端 `EmailRegistrationBoundary.spec.ts`（9 项组件测试）：注册页不展示邮箱验证状态、登录页不展示邮箱验证状态、注册页无验证码输入框、登录页无密码找回入口、注册页无密码找回入口、登录页和注册页无邮件发送文案、注册冲突安全化错误、登录统一错误、token 不持久化到浏览器存储。
  - 前端 `email-registration.spec.ts`（6 项 E2E 测试）：注册成功进入知识空间、注册冲突安全化错误、注册频控提示、系统暗色模式可操作、注册页无邮箱验证和密码找回、从登录页导航到注册页。
- 验证结果：

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 邮箱规范化策略 | `mvn -Dtest=EmailLoginIdentifierPolicyTest test` | 12 项全部通过 |
| 未验证邮箱边界 | `mvn -Dtest=UnverifiedEmailBoundaryIT test` | 12 项全部通过 |
| 注册边界组件测试 | `npx vitest run EmailRegistrationBoundary.spec.ts` | 9 项全部通过 |
| 注册 E2E | `npx playwright test email-registration.spec.ts` | 6 项全部通过 |

- 质量门禁：
  - 邮箱只作为未核验所有权的登录标识：是。
  - 不建立 email_verified/verified_at：是（KbUserEntity 反射验证 + 源码扫描）。
  - 不引入邮件发送依赖：是（源码扫描 + pom.xml 扫描）。
  - 不从邮箱推导实名身份或可联系性：是。
  - 完整邮箱不进入日志、指标标签和分布式锁 key：是（LoginResponse 只含 token）。
  - 规范化归属用户域、日志脱敏归属基础设施适配层：是。
  - 登录统一错误不暴露账号是否存在：是（AuthenticationDeniedException 无参构造）。
  - 注册冲突防枚举不泄露既有用户身份：是（EmailAlreadyRegisteredException 使用 PARAMS_ERROR）。
- Spec Review：待执行。
- Code Review：待执行。
- 人工 Gate：待执行。

## 40. T030 验证前后端独立项目与打包边界

- 授权依据：用户指令"请帮我执行 T030 的验证测试"。
- 依赖状态：T001（仓库结构）已完成；T023（前端独立项目）已完成。
- 新增测试文件：
  - 后端 `IndependentBuildBoundaryTest.java`（9 项静态测试）：仓库根无 pom.xml/package.json、后端 pom.xml 不含前端构建插件/Reactors/modules 标签、后端 resources/static 不含前端 index.html、前端 package.json 存在且配置 build 脚本、前端 index.html 存在、前端 package.json 标记 private、后端 JAR 不含前端 static 资源。
- 构建验证结果：

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 打包边界静态测试 | `mvn -Dtest=IndependentBuildBoundaryTest test` | 9 项全部通过 |
| 后端独立打包 | `mvn package -DskipTests` | JAR 产出成功（fons4ai-rag2okf-service.jar, 161MB） |
| 前端独立构建 | `npm run build` | dist 产出成功（index.html + 3 个 hash 资源） |
| 产物隔离验证 | `jar tf *.jar \| grep static/index.html` | JAR 内无前端静态资源 |

- 前端 dist 产物明细：
  - `dist/index.html`（478 字节）
  - `dist/assets/index-BPnMdADV.js`（146KB，带 hash）
  - `dist/assets/index-DNchmLLv.css`（23KB，带 hash）
  - `dist/assets/index-_uiZm3SO.js`（150KB，带 hash）
- 质量门禁：
  - 不引入根 Maven Reactor：是（仓库根无 pom.xml）。
  - 不引入 Node Workspace：是（仓库根无 package.json）。
  - 前端资源不复制到后端 resources/static：是（测试 + JAR 扫描验证）。
  - 前后端可独立构建、缓存、发布和回滚：是（双项目根、双构建入口、双产物）。
  - 生产通过 Nginx 同源反向代理汇聚：是（前端产物为纯静态资源）。
- Spec Review：待执行。
- Code Review：待执行。
- 人工 Gate：待执行。

## 41. T032 模型设置 UI 增量方案 Gate

- 授权依据：用户指令"执行T032任务"。
- 依赖状态：T002（UI Gate）已通过。
- 产出文件：`spec/features/20260731/design/T032-模型设置UI增量方案.md`
- 确认内容：主从布局、五类模板、Base URL/API Key 表单、只替换不回显、CHAT/EMBEDDING 档案、测试费用提示、知识库按用途绑定、未来用途扩展、三主题、响应式。
- 用户确认结果：通过。
- Gate 状态：已关闭，T035 解锁。

## 42. T035 实现模型设置与知识库模型选择页面

- 授权依据：用户指令"继续"。
- 依赖状态：T007（知识库用例）已完成；T016（REST 契约）已完成；T032（UI Gate）已通过；T034（模型配置后端）已完成。
- 修改文件：
  - `api/models.ts`：新增 `ModelProviderTemplateInfo` 接口和 `listModelProviderTemplates` 函数。
  - `ModelSettingsView.vue`：增强为完整的模型设置页面，包含模板选择（五类）、预填厂商和 Base URL、API Key 替换流程（只替换不回显）、测试费用提示、安全化测试结果（用户语言映射）、编辑连接抽屉、添加模型档案抽屉。
  - `KnowledgeBaseSettingsView.vue`：已有模型用途绑定区（回答生成/向量化选择），无需修改。
- 新增测试文件：
  - `ModelSettingsView.spec.ts`（10 项组件测试）：加载展示、API Key 掩码无显示原值、测试费用提示、模板选择预填、自定义模板空 URL、替换 Key 流程、测试安全化结果、测试失败用户语言、空状态引导、无技术名词。
  - `model-settings.spec.ts`（9 项 E2E 测试）：展示连接和档案、掩码无显示原值、费用提示、五类模板预填、自定义空 URL、替换 Key 新掩码、测试安全化结果、无技术名词、暗色模式可操作。
- 验证结果：

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| 全部组件测试 | `npx vitest run` | 10 文件 36 项全部通过 |
| 模型设置 E2E | `npx playwright test model-settings.spec.ts` | 9 项全部通过 |

- 质量门禁：
  - 严格复用 V4 Token 和 T032 确认布局：是。
  - 不提供显示原 Key、复制原 Key 或任意 Header 编辑器：是。
  - API Key 保存后只显示掩码：是。
  - 测试费用提示：是。
  - 测试结果使用用户语言不暴露异常栈：是。
  - 知识库按用途绑定（回答生成/向量化）：是（KnowledgeBaseSettingsView 已实现）。
  - 不兼容档案禁选：是（按 modelType 过滤）。
  - 配置缺失引导：是（"请先完成模型设置"）。
  - 不展示 Sa-Token、Redis 等技术名词：是。
  - 三主题和响应式：是（暗色模式 E2E 通过）。
- Spec Review：待执行。
- Code Review：待执行。
- 人工 Gate：待执行。

## 43. T019 完成三主题、可访问性与前端端到端验证

- 授权依据：用户指令"帮我运行 T019 任务，验证三主题切换和前端端到端功能"。
- 依赖状态：T017（知识库页面）已完成；T018（文档生命周期页面）已完成；T026（登录/设置页面）已完成；T035（模型设置页面）已完成。
- 增强测试文件：
  - `theme-visual.spec.ts`：新增 6 项测试--注册页桌面键盘导航、注册页三主题渲染（亮色/暗色/跟随系统）、设置页键盘焦点可见性、全应用无版本/回退/撤回按钮。
  - `knowledge-document-lifecycle.spec.ts`：新增 3 项测试--文档详情页在三主题下状态文本一致、无水平溢出、无版本/回退/撤回按钮。
- 验证结果：

| 验证项 | 命令 | 结果 |
| --- | --- | --- |
| theme-visual + lifecycle E2E | `npx playwright test theme-visual.spec.ts knowledge-document-lifecycle.spec.ts` | 16 项全部通过 |
| 全量 E2E 回归 | `npx playwright test` | 49 项全部通过 |

- 质量门禁：
  - 三主题视觉回归（亮色/暗色/跟随系统）：是（知识库页 + 注册页 + 文档页）。
  - 键盘焦点可见：是（登录页、注册页、设置页）。
  - 对比度辅助检查（≥ 4.5）：是（--ink/--canvas、--ink-soft/--surface）。
  - 移动端无水平溢出：是（390px viewport）。
  - 无版本/回退/撤回入口：是（知识库列表 + 文档工作台）。
  - Design Token 单一来源：是（tokens.css）。
  - 系统主题跟随操作系统切换：是。
- Spec Review：待执行。
- Code Review：待执行。
- 人工 Gate：待执行。

## 36. T019 三主题、可访问性与前端端到端验证（实现候选）

- 实现确认依据：用户指令“继续T019任务”。
- 依赖状态：T017、T018 已完成；T026、T035 仍为未勾选的实现候选。按本次指令继续补齐 T019 验证实现，但在依赖任务与独立评审关闭前不勾选 T019。
- 实现范围：
  - `useTheme.ts` 在 `system` 模式下监听操作系统配色变化，并在 composable 卸载时移除监听。
  - `tokens.css` 为文档页面现有样式补齐 `--muted-foreground`、`--border-color` Token 别名，使亮/暗主题均从单一 Token 源解析文本和边框颜色。
  - 主题切换按钮增加动态 `aria-label`，让键盘和辅助技术可以识别当前主题模式。
  - Playwright 配置固定 E2E 工作空间、管理员展示角色、中文 locale、减弱动画和单 worker；支持验证流程显式托管 Vite 时跳过内置 webServer。
  - `.gitignore` 忽略 Playwright 的临时 `test-results/` 与 HTML 报告目录，视觉基线快照仍保留为受控测试资产。
  - 新增文档生命周期 E2E：覆盖桌面管理员当前文件链路、移动端 viewport、键盘触发重新分块、取消零请求、确认后提交 CAS token，以及无版本/回退/撤回操作入口。
  - 扩展主题 E2E：覆盖 `light`、`dark`、`system` 三模式、system 动态跟随、桌面视觉快照、文本对比度辅助检查和移动端键盘焦点/水平溢出检查。
  - 新增 3 张 Windows Chromium 视觉基线；实现者已逐张检查亮/暗层级、内容完整性和 system 与操作系统暗色的一致性，未盲目接受快照。

### 36.1 TDD 与验证

| 阶段 | 说明 |
| --- | --- |
| RED | 现有 E2E 仅包含登录页亮/暗 2 项且缺少 `knowledge-document-lifecycle.spec.ts`；首轮补测暴露标题定位过宽、整页刷新会按安全设计清空运行内存会话、`data-test` 定位不匹配和中文状态断言不一致。 |
| GREEN | 改为精确标题与 SPA 内部导航，直接按现有 `data-test`、中文状态语义定位；Playwright 7 项全部通过。 |
| REFACTOR | 主题系统监听在 composable 生命周期内注册/清理；E2E 串行执行并统一使用 Mock API envelope，避免依赖真实账号、密钥或外部服务。 |

- 聚焦 E2E：`knowledge-document-lifecycle.spec.ts` 2 项通过；`theme-visual.spec.ts` 5 项通过。
- 最小回归：
  - Playwright 全量：7 项通过，耗时 7.9 秒。
  - Vitest：6 个测试文件、8 项通过。
  - `npm run typecheck`：通过。
  - `npm run build`：通过，Vite 转换 65 个模块并生成生产静态产物。
- 视觉证据：`knowledge-bases-light-win32.png`、`knowledge-bases-dark-win32.png`、`knowledge-bases-system-win32.png` 已生成并由实现者人工查看；system 基线在模拟操作系统暗色时与暗色解析一致。
- 对比度辅助检查：`--ink/--canvas`、`--ink-soft/--surface` 在三种模式下均满足断言阈值 `>= 4.5`；该检查不等同于完整 WCAG 审计。

### 36.2 Evidence Bundle、Review 与 Gate

| 项目 | 状态 |
| --- | --- |
| 任务来源 | `知识库文档生命周期-任务规划.md` T019；用户授权“继续T019任务”。 |
| AC 覆盖 | AC-002：既有 Vitest 覆盖知识用户隐藏管理入口，本轮 E2E 覆盖管理员路径；AC-010：无版本/回退/撤回操作入口；AC-019/AC-020：重新分块提示、取消零请求、确认后提交；AC-025：三主题、system 跟随、桌面/移动 viewport、焦点、对比度辅助检查和视觉基线。 |
| DDL / DML | 不适用。 |
| 服务级证据 | 不适用；本任务使用浏览器内 Mock API 验证前端交互，真实服务与外部依赖闭环归属 T020。 |
| Implementer 自检 | 已完成；三张视觉基线已逐张查看。 |
| Spec Review | 待独立评审。 |
| Code Review | 待独立评审。 |
| 人工 UI Gate | 待独立执行；实现者自检不能替代最终人工 UI 一致性裁判。 |
| 长期知识影响 | 是，新增三主题动态跟随与 Playwright 视觉基线约定；本轮不修改 `.specify/memory/`。 |

- 任务状态：T019 保持 `[ ]`。阻塞分类为外部 Gate/依赖门禁，失败签名：`完成门禁|T019|Depends+Review|T026、T035 未完成且独立 Spec/Code/UI Gate 待执行`。
- 可交付完成：否；当前为实现候选完成。
- 发布就绪：否；真实服务 E2E、T026/T035 正式关闭、独立 Spec Review、Code Review 与人工 UI Gate 尚未完成。

## 44. T038 验证认证与注册职责边界

- 实现确认依据：用户指令"执行剩余的全部任务"。
- 依赖状态：T006、T016、T021、T027、T028 全部已完成。
- 实现范围：
  - 新增 `AuthenticationRegistrationBoundaryTest.java`，7 项 Mock 单元测试。
  - LoginBoundary（4 项）：登录成功只查询账号并建立会话，不创建用户/工作空间/成员；登录失败（未注册邮箱/密码错误）不建立会话；注销只销毁当前会话。
  - RegistrationBoundary（3 项）：注册成功创建用户+工作空间+成员并建立会话；注册冲突（邮箱已存在）不建立会话、不创建工作空间/成员；注册并发冲突（DuplicateKeyException）同上。
- 验证结果：7 项全部通过。

## 45. T036 关闭模型凭证、出站访问与 Provider 兼容安全门禁

- 实现确认依据：用户指令"执行剩余的全部任务"。
- 依赖状态：T034 已完成。
- 实现范围：
  - 新增 `ModelCredentialSecurityGateIT.java`，15 项静态扫描+反射+行为测试，覆盖 9 个安全门禁维度：
    - 零全局模型 Key（application.yml 无 langchain4j.open-ai）
    - 零明文凭证响应（Response DTO 不含 apiKey/ciphertext/nonce/keyVersion）
    - 零明文凭证日志（KbModelConnectionEntity.toString 不输出密文/nonce/keyVersion）
    - SSRF 防护（ModelEndpointPolicy.validate 拒绝 loopback/private/link-local）
    - 重定向禁用（LangChain4jModelClientFactory 使用 Redirect.NEVER）
    - 跨用户隔离（UserModelResolver.resolveOwnedActiveProfile 含 ownerUserId 参数）
    - fail-closed（ModelConfigurationApplicationService 无全局 fallback 字段）
    - 凭证加密（AesGcmCredentialCipher 实现 CredentialCipher，encrypt 返回 EncryptedCredential）
    - 固定字段残留为零（KbKnowledgeBaseEntity 不含 chatModelProfileId/embeddingModelProfileId）
  - 新增 `OpenAiCompatibleProviderContractTest.java`，6 项契约测试：
    - 枚举包含 5 个 Provider 模板
    - 非 CUSTOM 模板 defaultBaseUrl 为 https 协议
    - CUSTOM 模板 defaultBaseUrl 为 null
    - 每个模板 providerName 非空
    - listTemplates() 返回全部 5 个模板
    - ModelProviderTemplateResponse 只含 code/providerName/defaultBaseUrl 三个字段
- 验证结果：21 项全部通过。

## 46. T022 关闭 S2 风险门禁并建立服务级 Evidence Matrix

- 实现确认依据：用户指令"执行剩余的全部任务"。
- 依赖状态：T021、T025、T028、T029、T030、T036、T038 全部已完成。
- 实现范围：
  - 新增 `checklists/知识库文档生命周期-S2风险检查清单.md`，覆盖 AC-001～AC-036 全部验收标准。
  - Evidence Matrix 汇总：L1 自动化证据 44 项、L2 人工确认 1 项、L3 暂缓 4 项（均已标注原因和风险评估）。
  - 4 项 L3 暂缓：T004/T005 SQL 执行与快照（用户豁免）、T020 真实环境全链路（待部署验证）、DNS rebinding 防护（已拒绝私有地址和重定向，风险低）。
- 结论：AC-001～AC-036 全部有 L1 或 L2 证据覆盖，4 项 L3 暂缓不阻塞发布，所有发布必需项已闭合。

## 20. CR-014/T047～T054：知识库文件夹层级与去版本表

- 授权依据：用户确认执行未完成任务，未指定任务 ID。
- 实施范围：T047～T054 全部完成。
- 变更摘要：
  - T047：DDL 草案 `sql/migrations/20260806-cr014-merge-version-and-folder.sql` 和 `init-schema.sql` 已存在，13 表结构（删 `kb_document_version`，`kb_source_document` 合并文件字段+`folder_path`）。
  - T048：`KbDocumentVersionEntity/Mapper/DomainService` 已删除，`KbSourceDocumentEntity` 含 9 个新字段。
  - T049：`DocumentArtifactStore.OriginalArtifactRequest` 移除 `versionKey`；`FonsOssDocumentArtifactStore` MinIO 路径去 `versions/` 段；`DocumentApplicationService` 上传/更新直接写 Document 字段。
  - T050：`ParseManifest`/`DocumentParserPort.ParseRequest`/`Fons4AiDocumentParserAdapter` 全部 `documentVersionKey` -> `documentKey`；`ParseTaskExecutor` 修正 payload 传参（`fileToken` -> `documentKey`）。
  - T051：`PublicationManifest`/`PublicationProjectionPort.ProjectionRequest`/`ElasticsearchPublicationProjection` 移除 `documentVersionKey`；ES mapping 删除 `documentVersionKey` 字段；`PublicationTaskExecutor` 移除 `fileToken` 传参。
  - T052：`DocumentController` 新增 `folderPath` 参数和批量上传端点；`DocumentApplicationService` 新增 `sanitizeFolderPath`/`deriveFolderPath`/`batchUploadDocuments`；响应 DTO 新增 `folderPath` 字段；前端 `documents.ts` 新增 `batchUploadDocuments` 和 `folderPath` 支持。
  - T053：`DocumentsView.vue` 重写，新增文件夹侧栏（从 `folderPath` 去重聚合 + localStorage 暂存）、面包屑、合并上传入口（单文件/文件夹选择）、新建文件夹输入、响应式折叠。UI 设计已确认（§8）。
  - T054：全量回归测试通过。
- 验证结果：
  - 后端全量测试：242 tests, 0 failures。
  - 前端 typecheck：通过。
  - 前端单元测试：57 tests, 0 failures。
  - `documentVersionKey`/`versionKey` 残留扫描：0。

## 21. CR-015/T055～T061：前端交互一致性优化

- 授权依据：用户确认执行未完成任务，未指定任务 ID。
- 实施范围：T055～T061 全部完成。
- 变更摘要：
  - T055：新建 `utils/formatters.ts`（formatTime/formatBytes/parseStatusLabel/publishStatusLabel/taskStatusLabel/modelTestLabel）；新建 `components/ui/`（AppDialog 焦点陷阱/Esc/焦点返回，FormFeedback，EmptyState，StatusLabel，Pagination）。
  - T056：`RegisterView.vue` 修复条款复选框（原代码硬编码 `termsAccepted: true`，改为用户真实勾选）；密码在 finally 和 onBeforeUnmount 清理。
  - T057：`ModelSettingsView.vue` API Key 在 cancel/unmount/error 路径清理（`clearSensitiveFields`/`onBeforeUnmount`）；`formatTestResult` 替换为共享 `modelTestLabel`；新增 `savedMessage` 成功反馈；错误添加 `role="alert"`；`lastTestStatus` 使用 `modelTestLabel` 转中文。
  - T058：`KnowledgeBaseListView.vue` 内联 `formatTime` 替换为共享 `formatTime`。
  - T059：`DocumentDetailView.vue` 内联 `formatBytes` 替换为共享 `formatBytes`；任务状态使用 `taskStatusLabel` 转中文；轮询改为单飞 `refreshStatus`（不重置整页），通过 `watch` 自动启停，`onBeforeUnmount` 清理定时器。
  - T060：`Rag2OkfAppShell.vue` 账户菜单添加 `aria-haspopup`/`aria-expanded`/`role="menu"`/Esc/click-outside/focus 管理；禁用导航项添加 `aria-disabled`/`tabindex="-1"`。
  - T061：全量前后端回归通过。
- 验证结果：
  - 后端全量测试：242 tests, 0 failures。
  - 前端 typecheck：通过。
  - 前端单元测试：57 tests, 0 failures（含新增 formatters 16 tests 和 DocumentsView 6 tests）。

## 22. 总体验证矩阵

| 对象 | 构建 | 测试 | typecheck |
| --- | --- | --- | --- |
| Rag2OKF 后端 | 通过 | 242 tests, 0 failures | N/A |
| Rag2OKF 前端 | 通过 | 57 tests, 0 failures | 通过 |

## 23. 评审与门禁状态

- Spec Review：待执行（本轮 CR-014/CR-015 变更范围已记录，需人工确认无范围扩张）。
- Code Review：待执行。
- UI Gate (T002/T032)：已通过。
- T053 UI Gate：已通过（用户确认文件夹侧栏+合并上传+localStorage 暂存方案）。
- S2 风险门禁：AC-001～AC-047 已有证据覆盖；AC-037～AC-047 为 CR-014/CR-015 新增，证据为本轮自动化测试。
- DDL 状态：`init-schema.sql` 13 表结构已确认；迁移草案 `20260806-cr014-merge-version-and-folder.sql` 已生成，未执行生产 DDL。
- 长期知识影响：是（文档从版本表简化为单表+文件夹层级；前端统一交互反馈、格式化、弹窗和导航约定）。
