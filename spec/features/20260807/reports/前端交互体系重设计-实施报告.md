# 前端交互体系重设计 实施报告

> 功能目录：`spec/features/20260807/`
> SDD 等级：S2
> 报告日期：2026-08-08
> 实现确认依据：用户消息"执行T001"、"执行 T002"、"继续下一个可执行任务"、"继续下一个可执行任务"（T006）、"继续下一个可执行任务"（T007）、"执行前端交互体系重设计-任务规划.md 下一个任务"（T022）、"执行 T008 知识库列表管理"（T008）

## 1. 任务范围

| 任务 ID | 任务名称 | 状态 | 说明 |
| --- | --- | --- | --- |
| T001 | 确认前端交互体系 UI 设计方案 | 已完成 | 用户逐项确认 8 个维度均可行 |
| T002 | 实现演示数据切换机制与 mock 数据 | 已完成 | useDataSource composable + 3 个 mock 数据文件，11 个单元测试通过 |
| T003 | 升级 AppDialog 并新增 ConfirmDialog 组件 | 已完成 | AppDialog 新增 size/persistent props + 中央弹窗样式；ConfirmDialog 基于 AppDialog 默认 danger+persistent；15 个测试通过 |
| T004 | 实现 useTaskPolling composable | 已完成 | 指数退避轮询（2s/×1.5/15s 上限）+ 终态停止 + 单飞 + 卸载清理；12 个测试通过 |
| T005 | 实现 ContextMenu 与 TreeMenu 组件 | 已完成 | ContextMenu 鼠标定位+点击外部关闭；TreeMenu 递归渲染+展开/收起+层级缩进；19 个测试通过 |
| T006 | 实现前端 API 层合并契约 | 已完成 | models.ts 重写为合并 ModelConfig 契约（连接+档案+高级参数）；knowledge-bases.ts 新增 deleteKnowledgeBase 与 ownerUserKey/canDelete/modelConfigKey；mock 数据对齐字段名；相关视图适配；30 个 API 层测试 + 14 个视图测试通过 |
| T007 | 重塑 Rag2OkfAppShell 为顶部导航并实现动态面包屑 | 已完成 | 移除 evidence-rail 和 contextbar；topbar 改造为顶部导航（logo 首页、知识库、搜索 disabled、聊天 disabled）；移除 OKF/问答/评测占位和个人偏好入口；activeNav 选中态计算；面包屑 sectionLabel 驱动；11 个测试通过 |
| T022 | 移除个人偏好页面并改造 useTheme 为 localStorage 持久化 | 已完成 | useTheme 改为 localStorage 持久化（键 `rag2okf.theme`），模块级 loadTheme 初始化；移除 PersonalSettingsView.vue 及 `/settings/personal` 路由；App.vue 移除 preferenceJson 主题应用逻辑；ProfileView 移除"打开个人偏好"入口与 useRouter；11 个 useTheme 测试通过 |
| T008 | 重塑 KnowledgeBaseListView 支持重命名与删除 | 已完成 | 卡片右上角新增三点操作菜单（重命名+删除）；重命名用 AppDialog size sm 仅提交 name；删除用 AppDialog danger+persistent 需输入名称匹配确认；canDelete=false 隐藏删除入口；点击外部关闭菜单；11 个测试通过（含 8 个新增） |

## 2. 变更文件

| 文件 | 变更类型 | 说明 |
| --- | --- | --- |
| `spec/features/20260807/前端交互体系重设计-技术设计说明书.md` | 修改 | 版本记录新增 V1.3.1，记录 UI 设计确认完成 |
| `spec/features/20260807/前端交互体系重设计-任务规划.md` | 修改 | T001-T005 标记为 `[x]` 已完成 |
| `spec/features/20260807/reports/前端交互体系重设计-实施报告.md` | 新增/更新 | 本实施报告，追加 T003-T005 记录 |
| `fons4ai-rag2okf-ui/src/composables/useDataSource.ts` | 新增 | 演示数据切换 composable（envMode + runtimeOverride + isDemoMode） |
| `fons4ai-rag2okf-ui/src/composables/__tests__/useDataSource.spec.ts` | 新增 | useDataSource 单元测试（11 个用例） |
| `fons4ai-rag2okf-ui/src/api/mock/knowledge-bases.ts` | 新增 | 知识库 mock 数据（3 条，含 ownerUserKey/canDelete） |
| `fons4ai-rag2okf-ui/src/api/mock/documents.ts` | 新增 | 文档 mock 数据（4 条，含 SUCCEEDED/RUNNING/FAILED/PENDING 四种状态） |
| `fons4ai-rag2okf-ui/src/api/mock/models.ts` | 新增 | 模型配置 mock 数据（3 条合并后 ModelConfig 结构，含 contextWindow） |
| `fons4ai-rag2okf-ui/src/components/ui/AppDialog.vue` | 修改 | 新增 size（sm/md/lg）和 persistent props；watch 改为 immediate 并提前绑定键盘监听；新增 scoped style 中央弹窗样式（居中+尺寸+danger 边框） |
| `fons4ai-rag2okf-ui/src/components/ui/ConfirmDialog.vue` | 新增 | 确认弹窗组件，基于 AppDialog，默认 danger+persistent，支持 confirmText/cancelText 自定义，确认/取消事件 |
| `fons4ai-rag2okf-ui/src/components/ui/ContextMenu.vue` | 新增 | 右键上下文菜单，鼠标坐标定位，backdrop 点击外部关闭，支持 disabled/danger 菜单项样式 |
| `fons4ai-rag2okf-ui/src/components/ui/TreeMenu.vue` | 新增 | 递归树状菜单，defineOptions name 实现自引用，展开/收起（collapsed Set），data-depth 层级缩进，selectedPath 选中高亮，documentCount 展示 |
| `fons4ai-rag2okf-ui/src/components/ui/index.ts` | 修改 | 导出 ConfirmDialog、ContextMenu、TreeMenu |
| `fons4ai-rag2okf-ui/src/components/ui/__tests__/AppDialog.spec.ts` | 新增 | AppDialog 测试（8 个用例：size、persistent、Esc、danger） |
| `fons4ai-rag2okf-ui/src/components/ui/__tests__/ConfirmDialog.spec.ts` | 新增 | ConfirmDialog 测试（7 个用例：danger/persistent 默认、确认/取消、文案自定义） |
| `fons4ai-rag2okf-ui/src/composables/useTaskPolling.ts` | 新增 | 异步任务轮询 composable（指数退避+终态停止+单飞+卸载清理），intervalMs/maxIntervalMs/backoffFactor 可配置 |
| `fons4ai-rag2okf-ui/src/composables/__tests__/useTaskPolling.spec.ts` | 新增 | useTaskPolling 测试（12 个用例：首次 2s、×1.5 退避、15s 上限、终态停止、单飞、stop、卸载清理、自定义参数） |
| `fons4ai-rag2okf-ui/src/components/ui/__tests__/ContextMenu.spec.ts` | 新增 | ContextMenu 测试（8 个用例：显示/隐藏、定位、菜单项、select、disabled、danger、点击外部关闭） |
| `fons4ai-rag2okf-ui/src/components/ui/__tests__/TreeMenu.spec.ts` | 新增 | TreeMenu 测试（11 个用例：递归渲染、select、展开/收起、active、documentCount、depth、空状态） |
| `fons4ai-rag2okf-ui/src/api/models.ts` | 重写 | 移除旧两步式（createModelConnection/createModelProfile）API；导出合并后 ModelConfig 类型（连接+档案+高级参数：providerCode/baseUrl/apiKeyMask/apiKeyConfigured/modelType/modelName/dimensions/contextWindowLength/timeoutSeconds/temperature/status）和单步 CRUD（listModelConfigs/createModelConfig/updateModelConfig/replaceModelApiKey/testModelConfig/deleteModelConfig）+ listModelProviderTemplates；demo 模式走 mock，real 模式走 http.request |
| `fons4ai-rag2okf-ui/src/api/knowledge-bases.ts` | 修改 | KnowledgeBaseSummary 新增 ownerUserKey/canDelete 字段；ModelBinding.modelProfileKey 改为 modelConfigKey；新增 deleteKnowledgeBase(key) 函数（demo 走 mock 校验 canDelete，real 走 DELETE /knowledge-bases/{key}）；KnowledgeBase 类型同步扩展 ownerUserKey/canDelete |
| `fons4ai-rag2okf-ui/src/api/mock/models.ts` | 修改 | 对齐合并后 ModelConfig 契约：configKey→modelConfigKey、contextWindow→contextWindowLength、status: INACTIVE→DISABLED、新增 apiKeyConfigured；mock 数据 3 条（CHAT/EMBEDDING/禁用） |
| `fons4ai-rag2okf-ui/src/api/mock/knowledge-bases.ts` | 修改 | 移除扩展类型 MockKnowledgeBase，将 ownerUserKey/canDelete 加入正式 KnowledgeBase 契约；modelBindings.modelProfileKey→modelConfigKey；3 条 mock 数据标注 canDelete |
| `fons4ai-rag2okf-ui/src/api/__tests__/models.spec.ts` | 新增 | models API 测试（18 个用例：导出类型与函数、demo 模式返回 mock、real 模式调用 http.request、API Key 只在创建/替换时提交、updateModelConfig 不提交 apiKey、合并契约字段齐全、apiKeyConfigured 语义） |
| `fons4ai-rag2okf-ui/src/api/__tests__/knowledge-bases.spec.ts` | 新增 | knowledge-bases API 测试（12 个用例：导出 deleteKnowledgeBase、KnowledgeBaseSummary 含 ownerUserKey/canDelete、deleteKnowledgeBase demo 走 mock、不能删除 canDelete=false、real 模式调用 DELETE、modelConfigKey 字段） |
| `fons4ai-rag2okf-ui/src/views/settings/ModelSettingsView.vue` | 修改 | 适配合并后 API：import 从 createModelConnection/createModelProfile 改为 createModelConfig/updateModelConfig/listModelConfigs/testModelConfig/replaceModelApiKey；表单字段合并（基础信息+高级参数）；选中态展示 modelName |
| `fons4ai-rag2okf-ui/src/views/knowledge-bases/KnowledgeBaseSettingsView.vue` | 修改 | 适配 modelConfigKey：import listModelConfigs/ModelConfig；modelBindings.modelProfileKey→modelConfigKey；添加 knowledgeBase undefined 安全检查；modelConfigs 过滤 ACTIVE |
| `fons4ai-rag2okf-ui/src/views/settings/__tests__/ModelSettingsView.spec.ts` | 修改 | 适配合并契约：mock 数据改为 ModelConfig 结构（modelConfigKey/contextWindowLength/apiKeyConfigured/DISABLED）；新增"切换配置展示 modelName""测试失败展示用户语言"用例 |
| `fons4ai-rag2okf-ui/src/views/knowledge-bases/__tests__/KnowledgeBaseListView.spec.ts` | 修改 | mock 数据新增 ownerUserKey/canDelete 字段 |
| `fons4ai-rag2okf-ui/src/views/knowledge-bases/__tests__/KnowledgeBaseSettingsView.spec.ts` | 修改 | mock 数据新增 ownerUserKey/canDelete；listModelConfigs mock |
| `fons4ai-rag2okf-ui/src/layouts/Rag2OkfAppShell.vue` | 重写 | 移除 aside.evidence-rail 整块（rail-nav/evidence-steps）和 div.contextbar 整块（workspace-select/context-nav）；topbar 改造为顶部导航：左侧 brand-mark（logo 首页 button）+ topbar-nav（知识库 RouterLink、搜索 disabled button、聊天 disabled button），右侧 breadcrumb + theme-toggle + account-menu；新增 activeNav computed（knowledge-base*/document* 路由高亮知识库）；移除 useWorkspaceStore import（contextbar 不再需要）；账号菜单移除"个人偏好"入口 |
| `fons4ai-rag2okf-ui/src/layouts/__tests__/Rag2OkfAppShell.spec.ts` | 新增 | 外壳测试（11 个用例：导航项渲染、evidence-rail 不渲染、contextbar 不渲染、无 OKF/问答/评测占位、知识库路由高亮、非知识库路由不高亮、logo 首页跳转、主题切换按钮、账号菜单无个人偏好、面包屑 sectionLabel、插槽内容） |
| `fons4ai-rag2okf-ui/src/styles/tokens.css` | 修改 | .app-shell 改为单列（移除 78px evidence-rail 占位）；移除 .evidence-rail/.rail-nav/.rail-action/.evidence-steps/.evidence-step/.contextbar/.workspace-select/.context-nav 样式；.brand-mark 从 42px 调整为 38px 适配 topbar；新增 .topbar-left/.topbar-nav/.nav-item/.nav-item.active/.nav-item.disabled 样式；.workspace-main 高度从 calc(100vh-111px) 改为 calc(100vh-64px)；移动端响应式移除 evidence-rail/contextbar 规则，新增 topbar-nav 窄屏降级 |
| `fons4ai-rag2okf-ui/src/composables/useTheme.ts` | 修改 | 新增 STORAGE_KEY/loadTheme，模块级 mode 从 localStorage 读取并立即 applyTheme；setTheme 同步写入 localStorage；typeof window/document 保护适配非浏览器环境 |
| `fons4ai-rag2okf-ui/src/composables/__tests__/useTheme.spec.ts` | 新增 | useTheme 单元测试（11 个用例：loadTheme 无缓存/缓存/非法值、setTheme 写入 localStorage/mode/dataset.theme、system 跟随系统、循环切换、系统主题变化） |
| `fons4ai-rag2okf-ui/src/App.vue` | 修改 | 移除 useTheme import 与调用；移除 applyThemeFromProfile 函数和 watch profile；onMounted 只保留 loadProfile 恢复会话（主题由 useTheme 模块级自动应用） |
| `fons4ai-rag2okf-ui/src/router/index.ts` | 修改 | 移除 PersonalSettingsView import 和 `/settings/personal` 路由 |
| `fons4ai-rag2okf-ui/src/views/profile/ProfileView.vue` | 修改 | 移除"打开个人偏好"按钮（指向已删除路由的死链）；移除未使用的 useRouter；副标题"展示资料与个人偏好"改为"展示资料" |
| `fons4ai-rag2okf-ui/src/views/settings/PersonalSettingsView.vue` | 删除 | 移除个人偏好页面（主题与默认分块配置） |
| `fons4ai-rag2okf-ui/src/views/settings/__tests__/PersonalSettingsView.spec.ts` | 删除 | 随被测文件移除 |
| `spec/features/20260807/前端交互体系重设计-任务规划.md` | 修改 | T022、T008 标记为 `[x]` 已完成 |
| `fons4ai-rag2okf-ui/src/views/knowledge-bases/KnowledgeBaseListView.vue` | 修改 | 卡片右上角新增三点操作菜单（重命名+删除）；新增重命名 AppDialog（size sm，仅提交 name）；新增删除确认 AppDialog（danger+persistent，输入名称匹配确认）；openMenuKey 控制菜单展开；handleDocumentClick 点击外部关闭；onBeforeUnmount 清理监听 |
| `fons4ai-rag2okf-ui/src/views/knowledge-bases/__tests__/KnowledgeBaseListView.spec.ts` | 修改 | 新增 8 个测试用例：操作菜单可见性（canDelete true/false）、重命名提交流程、重命名空值校验、删除确认流程、删除名称不匹配校验、删除取消、删除失败反馈；保留原 3 个回归测试 |

## 3. TDD 记录

### T001
不适用。T001 是 UI 设计确认任务，不涉及业务代码编写，无 RED -> GREEN -> REFACTOR 流程。

### T002
- **RED**：编写 `useDataSource.spec.ts`（11 个测试用例），覆盖环境变量 demo/real 切换、非法值默认 real、运行时 setMode/resetMode、不写入 localStorage、isDemoMode 同步检查。运行测试确认失败（模块不存在：`Failed to resolve import "../useDataSource"`）。
- **GREEN**：实现 `useDataSource.ts`（envMode 模块加载时读取、runtimeOverride 内存 ref、getDataSourceMode/isDemoMode/useDataSource 导出）。运行测试确认 11 个全部通过。
- **REFACTOR**：自检可读性、DDD-lite（切换策略封装在 composable，视图只消费 isDemo/mode）、命名表达力、工具复用（Vue computed/ref）、mock 数据结构忠实模拟（KnowledgeBaseSummary 含 ownerUserKey/canDelete、ModelConfig 合并后含 contextWindow、DocumentSummary 含四种状态）。无需额外修改。

### T003
- **RED**：编写 `AppDialog.spec.ts`（8 个用例）和 `ConfirmDialog.spec.ts`（7 个用例），覆盖 size 控制、persistent 遮罩/Esc 行为、danger 样式、确认/取消流程、文案自定义。运行测试确认失败（组件不存在/props 缺失）。
- **GREEN**：
  - AppDialog：新增 `size`（sm/md/lg，默认 md）和 `persistent`（默认 false）props，替换原 `dismissible`（语义反转）；`panelClass` 新增 `size-${size}` 类；`canDismiss()` 替代直接检查 `dismissible`；watch 添加 `{ immediate: true }` 并将 `addEventListener` 移到 `nextTick` 之前解决初始 modelValue 时 Esc 监听未绑定问题；新增 scoped style（`.drawer-backdrop` 居中覆盖全局 flex-end、`.modal-panel` 中央弹窗样式、`.size-sm/md/lg` 宽度、`.danger-modal` 红色强调边）。
  - ConfirmDialog：基于 AppDialog 组合，默认 `danger: true` + `persistent: true`；`visible` computed 双向绑定；`handleConfirm`/`handleCancel` 触发事件并关闭；支持 `confirmText`/`cancelText`/`description` props；scoped style 定义确认按钮 danger 红色背景。
  - index.ts 导出 ConfirmDialog。
- **REFACTOR**：自检 DDD-lite（组件纯展示层，状态由调用方管理）、焦点陷阱延续、ConfirmDialog 基于 AppDialog 组合复用、三主题一致（使用 CSS 变量）、键盘可访问。无需额外修改。

### T004
- **RED**：编写 `useTaskPolling.spec.ts`（12 个用例），覆盖首次轮询 2s、×1.5 退避、15s 上限、SUCCEEDED/FAILED 终态停止、onUpdate 回调、undefined 不回调、单飞、stop()、多次 start() 防重复、卸载清理、自定义参数。运行测试确认失败（模块不存在）。
- **GREEN**：实现 `useTaskPolling.ts`：
  - `scheduleNext()`：检查 running && isActive，安排 `setTimeout(poll, currentInterval)`。
  - `poll()`：检查 inFlight（单飞跳过）→ 检查 isActive → 执行 fetcher → onUpdate → 检查 isActive → 递增 `currentInterval = min(current * backoffFactor, maxIntervalMs)` → scheduleNext。
  - `start()`：防重复（`if (running) return`），重置 currentInterval。
  - `stop()`：running = false，clearTimer。
  - `onBeforeUnmount`：仅在 `getCurrentInstance()` 时注册，避免非组件环境警告。
- **REFACTOR**：自检 DDD-lite（轮询策略封装在 composable，视图只消费回调）、无内存泄漏（定时器+监听器清理）、退避系数/上限可配置、单飞防止重复请求。无需额外修改。

### T005
- **RED**：编写 `ContextMenu.spec.ts`（8 个用例）和 `TreeMenu.spec.ts`（11 个用例），覆盖显示/隐藏、鼠标定位、菜单项渲染、select 事件、disabled/danger 样式、点击外部关闭、递归渲染、展开/收起、选中高亮、documentCount、depth 属性、空状态。运行测试确认失败（组件不存在）。
- **GREEN**：
  - ContextMenu：`visible`/`x`/`y`/`items` props；backdrop 固定定位全屏覆盖，点击关闭；menu panel absolute 定位 `left/top` 跟随鼠标；`handleSelect` 检查 disabled 后触发 select + 关闭；scoped style 定义菜单项样式（hover/disabled/danger）。
  - TreeMenu：`<script lang="ts">` 导出 `TreeNode` 接口；`defineOptions({ name: 'TreeMenu' })` 实现递归自引用；`nodes`/`selectedPath`/`depth` props；`collapsed` ref Set 管理收起状态（默认展开）；`toggle` 切换 Set 成员并触发响应式；模板递归渲染 `<TreeMenu>` 子组件并转发 `@select`；`data-depth` 属性标记层级；内联 `paddingLeft` 按深度缩进；scoped style 定义节点/图标/计数样式。
  - index.ts 导出 ContextMenu 和 TreeMenu。
- **REFACTOR**：自检 DDD-lite（纯展示层无业务规则）、递归组件正确性、移动端兼容（ContextMenu 可由按钮触发）、三主题一致（CSS 变量）。无需额外修改。

### T006
- **RED**：编写 `models.spec.ts`（18 个用例）和 `knowledge-bases.spec.ts`（12 个用例），覆盖合并后 ModelConfig 契约导出、demo 模式返回 mock、real 模式调用 http.request、API Key 只在创建/替换时提交（updateModelConfig 不提交 apiKey）、字段齐全（modelConfigKey/contextWindowLength/apiKeyConfigured/DISABLED）、deleteKnowledgeBase 导出与权限校验（canDelete=false 拒绝）、KnowledgeBaseSummary 含 ownerUserKey/canDelete、modelConfigKey 字段。运行测试确认失败（旧两步式 API 导出缺失、字段名不匹配）。
- **GREEN**：
  - `models.ts` 重写：移除 `createModelConnection`/`createModelProfile`/`listModelProfiles` 等旧两步式 API；导出合并后 `ModelConfig` 接口（连接字段 providerCode/providerName/baseUrl/apiKeyMask/apiKeyConfigured + 档案字段 modelType/modelName/dimensions + 高级参数 contextWindowLength/timeoutSeconds/temperature + 状态 status/lastTestStatus/lastTestAt）；导出 `ModelProviderTemplateInfo` 和 `listModelProviderTemplates`；单步 CRUD 函数 `listModelConfigs`/`createModelConfig`/`updateModelConfig`/`replaceModelApiKey`/`testModelConfig`/`deleteModelConfig`；每个函数检测 `isDemoMode()`，demo 走 mock，real 走 `http.request`。
  - `knowledge-bases.ts` 扩展：`KnowledgeBaseSummary` 新增 `ownerUserKey: string` 和 `canDelete: boolean`；`KnowledgeBase` 同步扩展；`ModelBinding.modelProfileKey` 改为 `modelConfigKey`；新增 `deleteKnowledgeBase(key)` 函数（demo 模式调用 mock 校验 canDelete，real 模式 `DELETE /knowledge-bases/{key}`）。
  - `mock/models.ts` 对齐：`configKey`→`modelConfigKey`、`contextWindow`→`contextWindowLength`、`status: INACTIVE`→`DISABLED`、新增 `apiKeyConfigured`。
  - `mock/knowledge-bases.ts` 对齐：移除 `MockKnowledgeBase` 扩展类型，`ownerUserKey`/`canDelete` 加入正式 `KnowledgeBase` 接口；`modelBindings.modelProfileKey`→`modelConfigKey`；3 条 mock 数据标注 canDelete。
  - `ModelSettingsView.vue` 适配：import 从旧两步式改为合并后单步式；表单字段合并；选中态展示 modelName。
  - `KnowledgeBaseSettingsView.vue` 适配：import `listModelConfigs`/`ModelConfig`；`modelBindings.modelProfileKey`→`modelConfigKey`；添加 `knowledgeBase` undefined 安全检查；`modelConfigs` 过滤 `status === 'ACTIVE'`。
  - 相关视图测试 mock 数据对齐新字段（`ownerUserKey`/`canDelete`/`modelConfigKey`/`contextWindowLength`/`apiKeyConfigured`/`DISABLED`）。
- **REFACTOR**：自检 DDD-lite（API 层为基础设施适配，业务规则 canDelete 校验在 mock 层模拟后端语义，real 模式由后端校验）、API Key 安全（只在创建/替换时提交，更新其他字段不提交，只返回掩码不回显原值）、demo/real 切换无数据污染、字段命名与后端契约一致（modelConfigKey/contextWindowLength）。无需额外修改。

### T007
- **RED**：编写 `Rag2OkfAppShell.spec.ts`（11 个用例），覆盖顶部导航项渲染（logo 首页/知识库/搜索 disabled/聊天 disabled）、evidence-rail 不渲染、contextbar 不渲染、无 OKF/问答/评测占位、知识库相关路由（knowledge-bases/documents/document-detail/knowledge-base-settings）高亮"知识库"导航项、非知识库路由不高亮、logo 首页点击跳转、主题切换按钮存在、账号菜单无个人偏好入口、面包屑 sectionLabel 动态、插槽内容渲染。运行测试确认失败（旧实现仍有 evidence-rail/contextbar，无 data-test 属性，有 OKF 占位）。
- **GREEN**：
  - `Rag2OkfAppShell.vue` 重写：移除 `<aside class="evidence-rail">` 整块（rail-nav 5 个占位 + evidence-steps 5 个阶段点）；移除 `<div class="contextbar">` 整块（workspace-select + context-nav 5 个占位）；改造 `<header class="topbar">` 为顶部导航：左侧 `<div class="topbar-left">` 含 brand-mark（logo 首页 button，@click goHome）+ `<nav class="topbar-nav">` 含知识库 RouterLink、搜索 disabled button、聊天 disabled button；右侧 topbar-actions 含 breadcrumb（sectionLabel 驱动）+ theme-toggle + account-menu（移除"个人偏好"入口，保留个人中心/模型设置/退出登录）；新增 `activeNav` computed（route.name 以 knowledge-base 或 document 开头时返回 'knowledge-bases'）；移除未使用的 `useWorkspaceStore` import。
  - `tokens.css` 更新：`.app-shell` 从 `grid-template-columns: 78px minmax(0,1fr)` 改为 `minmax(0,1fr)` 单列；移除 `.evidence-rail`/`.rail-nav`/`.rail-action`/`.evidence-steps`/`.evidence-step`/`.contextbar`/`.workspace-select`/`.workspace-dot`/`.context-nav` 样式；`.brand-mark` 从 42px 调整为 38px 适配 topbar；新增 `.topbar-left`/`.topbar-nav`/`.nav-item`/`.nav-item.active`/`.nav-item.disabled` 样式（使用 CSS 变量三主题一致）；`.workspace-main` 高度从 `calc(100vh - 111px)` 改为 `calc(100vh - 64px)`；移动端 `@media (max-width: 760px)` 移除 evidence-rail/contextbar 规则，新增 topbar-nav 窄屏降级（gap/padding/font-size 缩小）。
  - 测试 mock：`vue-router` mock useRoute 返回 reactive mockRoute（支持响应式切换）、useRouter 返回 mockPush；`RouterLinkStub` 渲染为 `<a>` 透传 attrs；mock useTheme/useSessionStore；mountShell 添加 `global.stubs: { RouterLink: RouterLinkStub }`。
- **REFACTOR**：自检 DDD-lite（外壳为纯展示层，activeNav 路由匹配为视图关注点无业务规则，导航项真实/disabled 状态按设计硬编码）、可读性（activeNav/sectionLabel/goHome/chooseTheme 命名表达意图）、复用（保留账号菜单/Esc/点击外部关闭逻辑）、依赖管理（移除未使用 useWorkspaceStore）、三主题一致（nav-item 使用 var(--violet)/var(--violet-soft)）、键盘可访问（nav-item 为 button/a 支持 Tab，账号菜单 Esc 关闭）、移动端响应式（窄屏导航降级无底部栏）、无内存泄漏（onBeforeUnmount 清理监听）。无需额外修改。

### T022
- **RED**：编写 `useTheme.spec.ts`（11 个用例），覆盖 loadTheme 无缓存默认 system、缓存 dark/light 读取、非法值默认 system；setTheme 写入 localStorage 键 `rag2okf.theme`、更新 mode、应用 dataset.theme、system 跟随系统明暗；light→dark→system→light 循环切换并每次写入 localStorage；system 模式下系统主题变化时更新 dataset.theme。运行测试确认 4 个失败（loadTheme 不从 localStorage 读、setTheme 不写 localStorage），7 个通过（mode 切换/applyTheme/system 跟随为旧实现已有行为）。
- **GREEN**：
  - `useTheme.ts` 改造：新增 `STORAGE_KEY = 'rag2okf.theme'` 和 `VALID_MODES`；新增 `loadTheme()` 从 localStorage 读取，无缓存/非法值默认 'system'，`typeof localStorage` 保护；模块级 `mode = ref(loadTheme())`；模块加载时立即 `refreshSystemTheme()` + `applyTheme()`（使主题在所有页面含 public 页面即时生效，不依赖组件挂载），`typeof window/document` 保护；`setTheme` 新增 `localStorage.setItem(STORAGE_KEY, nextMode)`；保留 onMounted matchMedia 监听注册。
  - `App.vue` 改造：移除 `useTheme` import 与 `setTheme` 调用；移除 `applyThemeFromProfile` 函数（从 preferenceJson 解析主题）和 `watch profile`；onMounted 只保留 `loadProfile` 恢复会话；移除未使用的 `watch` import。
  - `router/index.ts` 改造：移除 `PersonalSettingsView` import 和 `/settings/personal` 路由。
  - `ProfileView.vue` 改造：移除"打开个人偏好 →"按钮（指向已删除路由的死链）；移除未使用的 `useRouter` import 与 `router` 变量；副标题"维护只属于你的展示资料与个人偏好。"改为"维护只属于你的展示资料。"。
  - 删除 `PersonalSettingsView.vue` 和 `PersonalSettingsView.spec.ts`。
- **REFACTOR**：自检 DDD-lite（主题持久化 localStorage 读写为基础设施关注点封装在 useTheme composable 内，视图只调用 setTheme 不直接操作 localStorage；App.vue 不再包含主题应用逻辑）、容错（loadTheme 无缓存/非法值默认 system）、可读性（STORAGE_KEY/VALID_MODES/loadTheme 命名表达意图）、复用（保留 resolveTheme/refreshSystemTheme/applyTheme/followSystemTheme 既有结构）、依赖管理（App.vue/ProfileView 移除未使用 import）、三主题一致（dataset.theme 驱动 tokens.css，light/dark/system 均覆盖）、无内存泄漏（onBeforeUnmount 清理 matchMedia 监听）。无需额外修改。

### T008
- **RED**：重写 `KnowledgeBaseListView.spec.ts`（11 个用例），保留原 3 个回归测试（加载/创建、权限隐藏、错误恢复），新增 8 个：canDelete=true 卡片操作菜单含重命名和删除、canDelete=false 无删除入口、重命名弹窗预填名称并提交 updateKnowledgeBase 后列表即时更新、重命名空值拒绝提交、删除弹窗输入名称匹配后调用 deleteKnowledgeBase 并移除、删除名称不匹配拒绝提交并提示错误、删除取消不执行、删除失败显示错误且列表保留。运行测试确认 8 个失败（找不到 `card-menu-trigger` 等 data-test 选择器），3 个通过。
- **GREEN**：
  - `KnowledgeBaseListView.vue` 改造：script 新增 `openMenuKey`（控制卡片菜单展开）、`renameTarget/renameName/renameError/renaming`（重命名状态）、`deleteTarget/deleteConfirmName/deleteError/deleting`（删除状态）；新增 `toggleMenu`（切换菜单+stopPropagation）、`openRename`（预填当前名称）、`submitRename`（仅提交 name，revision 传 0 表示不校验版本，成功后更新列表）、`openDelete`、`submitDelete`（名称匹配校验后调用 deleteKnowledgeBase，成功后从列表过滤移除）、`handleDocumentClick`（点击外部关闭菜单）；onMounted 注册 document click 监听，onBeforeUnmount 清理。
  - template 改造：card-top 内新增 `card-menu-wrap`（三点按钮 + 条件渲染下拉菜单，重命名始终可见，删除仅 canDelete=true 可见）；新增重命名 AppDialog（size sm，name 输入框，回车提交）；新增删除确认 AppDialog（size sm，danger + persistent，输入名称匹配提示，确认按钮 danger 样式）；import AppDialog。
  - style 改造：新增 `.card-menu-wrap/.card-menu-trigger/.card-menu/.danger-item`（卡片操作菜单样式）、`.rename-field/.dialog-description/.danger-text/.dialog-actions/.danger-btn`（弹窗内表单和操作区样式），全部使用 CSS 变量保证三主题一致。
  - 设计决策：删除确认需"输入名称匹配"（任务 Verification 要求），但 ConfirmDialog 只有确认/取消按钮不支持输入，用 AppDialog 等价实现 danger+persistent+输入确认，语义一致，不扩大 T003 的 ConfirmDialog 范围；删除确认按钮未 disabled（名称不匹配时仍可点击），改为点击校验并提示错误，UX 上明确告知用户"名称不匹配"比 disabled 无反馈更好。
- **REFACTOR**：自检 DDD-lite（重命名/删除的业务语义在后端，视图只编排交互和反馈；操作状态归属到对应弹窗 renameTarget/deleteTarget，不使用全局锁）、可读性（openMenuKey/renameTarget/deleteTarget 命名表达意图）、复用（AppDialog 已实现焦点陷阱/Esc/persistent，直接复用）、权限分层（前端 canDelete 控制可见性，服务端二次校验创建者）、三主题一致（全部使用 CSS 变量）、无内存泄漏（onBeforeUnmount 清理 document click 监听）。无需额外修改。

## 4. Evidence Bundle

### 4.1 验证结果

| 验证项 | 验证方式 | 结果 |
| --- | --- | --- |
| 顶部导航布局（A） | 用户确认 | 确认可行 |
| 中央弹窗模式（B） | 用户确认 | 确认可行 |
| 知识库列表（C） | 用户确认 | 确认可行 |
| 文档工作台（D） | 用户确认 | 确认可行 |
| 文档详情（E） | 用户确认 | 确认可行 |
| 模型设置（F） | 用户确认 | 确认可行 |
| 知识库设置（G） | 用户确认 | 确认可行 |
| 信息密度规则（H） | 用户确认 | 确认可行（用户询问信息密度规则含义后确认） |
| useDataSource 环境变量 demo 切换 | `npx vitest run src/composables/__tests__/useDataSource.spec.ts` | 11/11 通过 |
| AppDialog size/persistent/Esc/danger | `npx vitest run src/components/ui/__tests__/AppDialog.spec.ts` | 8/8 通过 |
| ConfirmDialog 确认/取消/文案/danger | `npx vitest run src/components/ui/__tests__/ConfirmDialog.spec.ts` | 7/7 通过 |
| useTaskPolling 退避/终态/单飞/卸载 | `npx vitest run src/composables/__tests__/useTaskPolling.spec.ts` | 12/12 通过 |
| ContextMenu 显示/定位/select/外部关闭 | `npx vitest run src/components/ui/__tests__/ContextMenu.spec.ts` | 8/8 通过 |
| TreeMenu 递归/展开收起/active/depth | `npx vitest run src/components/ui/__tests__/TreeMenu.spec.ts` | 11/11 通过 |
| models API 合并契约/demo/real/API Key 安全 | `npx vitest run src/api/__tests__/models.spec.ts` | 18/18 通过 |
| knowledge-bases API 扩展契约/deleteKnowledgeBase 权限 | `npx vitest run src/api/__tests__/knowledge-bases.spec.ts` | 12/12 通过 |
| ModelSettingsView 适配合并契约 | `npx vitest run src/views/settings/__tests__/ModelSettingsView.spec.ts` | 10/10 通过 |
| KnowledgeBaseListView/SettingsView 适配新字段 | `npx vitest run src/views/knowledge-bases/__tests__/*.spec.ts` | 4/4 通过 |
| Rag2OkfAppShell 顶部导航/选中态/无占位 | `npx vitest run src/layouts/__tests__/Rag2OkfAppShell.spec.ts` | 11/11 通过 |
| useTheme localStorage 持久化/循环切换/系统跟随 | `npx vitest run src/composables/__tests__/useTheme.spec.ts` | 11/11 通过 |
| ProfileView 适配（移除个人偏好入口） | `npx vitest run src/views/profile/__tests__/ProfileView.spec.ts` | 1/1 通过 |
| KnowledgeBaseListView 重命名/删除/权限/反馈 | `npx vitest run src/views/knowledge-bases/__tests__/KnowledgeBaseListView.spec.ts` | 11/11 通过 |
| TypeScript 类型检查 | `npx vue-tsc --noEmit` | exit code 0，无错误 |
| 全量回归测试 | `npx vitest run` | 20 文件 170 测试全部通过 |

### 4.2 AC 覆盖

| AC | 覆盖状态 | 说明 |
| --- | --- | --- |
| AC-001 | 设计确认覆盖 | 顶部导航布局已确认 |
| AC-006 | T003 实现覆盖 | AppDialog 支持 size 和 persistent，中央弹窗模式可用 |
| AC-007 | T003 实现覆盖 | ConfirmDialog 默认 danger+persistent，破坏性操作二次确认 |
| AC-010 | T005 实现覆盖 | ContextMenu 右键菜单可用，支持"新建目录"等选项 |
| AC-011 | T005 实现覆盖 | TreeMenu 递归渲染目录层级，可展开收起 |
| AC-013 | T004 实现覆盖 | useTaskPolling 指数退避+终态停止+单飞，局部刷新不遮蔽整页 |
| AC-014 | 设计确认覆盖 | 文档工作台管理操作已确认 |
| AC-015 | T002 实现覆盖 | useDataSource demo/real 切换机制可用，mock 数据忠实模拟真实接口响应 |
| AC-003 | T006 数据层覆盖 | knowledge-bases.ts 支持 deleteKnowledgeBase（重命名复用 updateKnowledgeBase），视图层交互在 T008 |
| AC-004 | T006 数据层覆盖 | deleteKnowledgeBase API 可用，二次确认交互在 T008 视图层（ConfirmDialog） |
| AC-005 | T006 数据层覆盖 | KnowledgeBaseSummary 含 canDelete 字段，前端可据此控制删除入口可见性，视图层在 T008 |
| AC-008 | T006 数据层覆盖 | models.ts 导出合并后 ModelConfig 单步 CRUD，视图层合并表单在 T011 |
| AC-001 | T007 实现覆盖 | 顶部导航项为 logo 首页、知识库、搜索 disabled、聊天 disabled，无 OKF/问答/评测占位；evidence-rail 和 contextbar 已移除 |
| AC-002 | T007 实现覆盖 | activeNav 计算正确：知识库相关路由高亮"知识库"，非知识库路由不高亮，logo 首页不参与选中态 |
| AC-019 | T022 实现覆盖 | useTheme 改为 localStorage 持久化（键 `rag2okf.theme`），setTheme 同步写入，刷新沿用缓存；PersonalSettingsView.vue 与 `/settings/personal` 路由已移除；默认分块参数随页面移除；topbar 主题按钮直接调用 setTheme 循环切换 |
| AC-003 | T008 实现覆盖 | 知识库卡片操作菜单含重命名，AppDialog size sm 单 name 字段，提交 PATCH 仅 name，列表即时更新 |
| AC-004 | T008 实现覆盖 | 删除确认弹窗 danger+persistent，需输入知识库名称匹配确认，防止误删 |
| AC-005 | T008 实现覆盖 | 前端根据 canDelete 控制删除入口可见性，canDelete=false 隐藏删除，服务端二次校验创建者 |
| AC-014 | T008 实现覆盖 | 知识库列表原地管理（重命名/删除），无需跳转设置页 |

### 4.3 未验证项

- T003 的 AppDialog 已接入 KnowledgeBaseListView（T008 重命名/删除确认），ConfirmDialog 尚未接入具体视图（ModelSettingsView 合并表单在 T011，KnowledgeBaseSettingsView 弹窗化在 T012，文档删除在 T024）。
- T004 的 useTaskPolling 尚未接入 DocumentDetailView（在 T010 实现）。
- T005 的 ContextMenu/TreeMenu 尚未接入 DocumentsView（在 T009 实现）。
- T006 的 API 层已就绪，KnowledgeBaseListView 重命名/删除已接入（T008）；documents.ts 的 demo 拦截和删除 API 待 T023/T024 扩展；ModelSettingsView 合并表单在 T011，KnowledgeBaseSettingsView 弹窗化在 T012。
- T022 已完成 useTheme localStorage 持久化改造与个人偏好页面移除；public 页面（login/register）未挂载 AppShell，matchMedia 监听仅在非 public 页面注册，public 页面主题由模块级 applyTheme 初始化（系统主题实时变化不响应，属可接受的短暂页面降级）。

## 5. DDL / DML / Seed 状态

不适用。T001-T007、T022、T008 均为前端组件、composable、API 层和外壳布局实现，不涉及 DDL、DML 或 Seed 数据。

## 6. 服务级 Evidence Matrix

不适用。T001-T007、T022、T008 不涉及独立可运行服务闭环。

## 7. Review 与人工 Gate

| Review 类型 | 状态 | 说明 |
| --- | --- | --- |
| Spec Review | 通过 | T001 用户逐项确认技术设计方案可行；T003-T007、T022、T008 实现符合技术设计说明书 §5.2/§5.4/§5.5/§3.1/§3.2/§2.2/§5.1 设计（T022 对应 §5.1 useTheme 改造与 §8.5 兼容性，T008 对应 §5.7 知识库列表管理操作） |
| Code Review | 待执行 | T003-T007、T022、T008 代码已完成自检（DDD-lite、可读性、命名、复用、三主题一致、API Key 安全、导航选中态、主题 localStorage 持久化、重命名/删除反馈归属化），但未经独立 Code Review |
| 人工 Gate | 待执行 | T003-T007、T022、T008 为前端组件/composable/API 层/外壳布局实现，不涉及权限/安全/数据迁移（canDelete 为前端可见性控制，服务端校验在 T016），但最终交付需用户确认交互效果 |

## 8. 代码质量复盘

| 检查项 | 结果 | 说明 |
| --- | --- | --- |
| DDD-lite | 通过 | 组件为纯展示层，轮询策略封装在 composable，视图只消费回调；useTheme 将 localStorage 持久化封装在 composable；KnowledgeBaseListView 重命名/删除只编排交互和反馈，业务语义在后端，操作状态归属到对应弹窗 |
| 可读性 | 通过 | 函数命名表达意图（canDismiss、scheduleNext、hasChildren、isExpanded） |
| 复用 | 通过 | ConfirmDialog 基于 AppDialog 组合；TreeMenu 递归自引用复用自身 |
| 依赖管理 | 通过 | 无新增三方依赖，useTaskPolling 使用标准 setTimeout |
| 异常处理 | 通过 | useTaskPolling 的 finally 确保 inFlight 清除；ContextMenu disabled 检查 |
| 三主题一致 | 通过 | 所有组件使用 CSS 变量（var(--surface)/var(--danger)等），自动适配 light/dark；useTheme 三态（light/dark/system）切换并 localStorage 持久化，模块级 applyTheme 保证所有页面生效 |
| 键盘可访问 | 通过 | AppDialog 焦点陷阱+Esc+Tab；ContextMenu 点击外部关闭 |
| 内存泄漏 | 通过 | useTaskPolling onBeforeUnmount 清理定时器；AppDialog onBeforeUnmount 移除监听；useTheme onBeforeUnmount 清理 matchMedia 监听；KnowledgeBaseListView onBeforeUnmount 清理 document click 监听 |

## 9. 数据验证结果

不适用。T001-T007、T022、T008 不涉及数据变更（T006 为前端 API 层契约定义，mock 数据模拟后端语义，真实数据变更在 T014-T016 后端阶段；T022 主题持久化改为 localStorage 属前端本地存储，不涉及后端数据；T008 重命名/删除调用已有 API 层契约，不涉及数据结构变更）。

## 10. 长期知识影响

否。T001-T007、T022、T008 均为前端交互组件、composable、API 层和外壳布局实现，不产生长期知识影响。

## 11. 风险与后续建议

- T001-T007、T022、T008 已完成。后续页面实现任务（T009-T024，T022/T008 除外）可以技术设计方案、已就绪组件、API 层契约和顶部导航外壳为基线开始实现。
- T002 的 mock 数据层已通过 T006 接入 API 层（models.ts/knowledge-bases.ts 检测 isDemoMode 返回 mock）。
- T006 的 API 层合并契约已就绪：T008 需接入 KnowledgeBaseListView 实现重命名/删除入口；T011 需接入 ModelSettingsView 合并表单；T012 需接入 KnowledgeBaseSettingsView 弹窗化；T023/T024 需扩展 documents.ts 的 demo 拦截和删除 API。
- T007 的顶部导航外壳已就绪，T022 已完成 useTheme localStorage 改造与个人偏好页面移除：T008/T011/T012 的视图重塑可直接在顶部导航外壳内进行，主题切换由 topbar 按钮直调 setTheme 循环切换（light→dark→system）。
- T003 的 AppDialog/ConfirmDialog 已就绪，T008/T011/T012 需接入具体视图替换右侧抽屉。
- T004 的 useTaskPolling 已就绪，T010 需接入 DocumentDetailView 替换固定 setInterval。
- T005 的 ContextMenu/TreeMenu 已就绪，T009 需接入 DocumentsView 实现树状目录和右键创建。
- 后续实现任务按依赖顺序执行：T009-T012（含 T023/T024 并行，T022/T008 已完成）-> T013 -> T014-T016 -> T017-T021。

## 12. 实现确认状态

T001-T007、T022、T008 已完成。后续任务实现确认状态仍为 `pending`，需用户明确回复 `执行`、`开始实现`、`继续执行` 或 `执行 Txxx` 后才进入实现。下一批可执行任务为 T011（模型设置合并表单）、T012（知识库设置弹窗化）、T023（SourceFilePreview 组件）、T024（文档删除与批量删除），均依赖 T003/T006/T007 已满足；T009（文档工作台树状目录）依赖 T005/T007 已满足；T010（文档详情反馈归属化）依赖 T004/T007/T024。

---

## 附录 A：T016 / T026 / T027 并行后端实施记录（2026-08-09）

> 实现确认依据：用户 2026-08-09 消息"开始并行执行 T016、T026、T027 三个后端任务"。
> 三个任务 Files 范围无交集，按 fons4ai-sdd-implement 并行 Agent 约定执行；详细报告片段见同目录下 `前端交互体系重设计-实施报告-T016.md`、`-T026.md`、`-T027.md`。

### A.1 任务完成状态

| 任务 ID | 任务名称 | 状态 | 生产代码改动 | 测试改动 |
| --- | --- | --- | --- | --- |
| T016 | 后端知识库删除 API 与列表扩展 | 已完成 | 零改动（核心实现已满足 AC） | KnowledgeBaseControllerTest 新增 3 个控制器契约测试（权限 403、幂等、列表字段） |
| T026 | 后端 /model-catalog + 7 枚举白名单 + CHAT 别名 | 已完成 | 零改动（核心实现与测试均已到位） | 零改动（ModelCatalogControllerTest + ModelTypeCompatibilityTest 已覆盖） |
| T027 | 后端 PATCH /users/me 局部 JSON 合并加固 | 已完成 | UserProfileApplicationService 新增 @Slf4j + 合并审计日志（仅记变更顶层 key 列表，不记值） | UserProfileServicePatchPreferenceJsonTest 新增 2 个聚焦测试（defaultModels 覆盖槽位不残留旧键 + 三层嵌套合并保留 theme/language） |

### A.2 合并回归验证

编排者在三个并行 Agent 完成后重新运行合并回归（JDK 21）：

```
mvn test "-Dtest=KnowledgeBaseControllerTest,KnowledgeBaseApplicationServiceTest,ModelCatalogControllerTest,ModelTypeCompatibilityTest,ModelConfigurationControllerTest,ModelUsagePolicyTest,UserProfileServicePatchPreferenceJsonTest,UserProfileControllerTest"
```

- 退出码：0（BUILD SUCCESS）
- 合计 28 个测试用例全部通过，三个任务的改动无冲突
- 运行期日志验证 T027 审计日志生效：`preferenceJson 局部合并，变更顶层 key: [defaultModels]` / `[theme]`（只记 key，不记值）

### A.3 AC 覆盖证据

| AC | 覆盖任务 | 证据 |
| --- | --- | --- |
| AC-003 知识库重命名 | T016 | 列表 API 返回 ownerUserKey/canDelete，控制器+服务层测试覆盖 |
| AC-004 删除二次确认 | T016 | DELETE 仅创建者可删，不匹配抛 WorkspaceAccessDeniedException→403，软删除 deleted=1 |
| AC-005 非创建者无删除入口 | T016 | canDelete = ownerUserId.equals(currentUserId)，服务端二次校验 |
| AC-026 /model-catalog 接口 | T026 | GET /model-catalog 返回 {providers, typeCounts}，10min 本地缓存（volatile+双重检查锁） |
| AC-027 7 枚举白名单 + CHAT 别名 | T026 | ModelType 7 值白名单 + isValid 拒 CHAT→400；normalize("CHAT")→"LLM" 在 Response DTO 层统一转换 |
| AC-028 PATCH /users/me 局部合并 | T027 | mergePreferenceJson 浅合并：patch 顶层 key 替换 existing 同名 key，theme/language 保留；null/空/`{}` 不清除 |
| AC-029 默认模型偏好 | T027 | defaultModels 子节点局部合并不覆盖其他偏好，两次连续合并不丢旧键 |
| AC-031 Provider 行内操作 + binding 校验 | T026 | binding usage_type=CHAT 因 ModelUsageType 枚举反序列化失败返回 400；ModelUsagePolicy 读取 normalize 兼容旧 CHAT |
| AC-032 API Key 安全 | T027 | 审计日志仅记变更 key 列表，不记录 API Key 等敏感原值 |

### A.4 Review 与人工 Gate

| Review 类型 | 状态 | 说明 |
| --- | --- | --- |
| Spec Review | 通过（自检） | T016/T026/T027 实现符合技术设计说明书 §3.1/§3.2/§4.2/§4.5/§4.8 设计；T027「3 层截断」澄清为 Jackson 64 层解析器防御（非业务截断），技术设计 §4.8.2 伪代码只要求浅合并，实现合理 |
| Code Review | 待执行 | 三个任务代码已完成自检（DDD-lite 分层、显式 import、合并逻辑下沉 Service 层、审计日志不记敏感值），但未经独立 Code Review |
| 人工 Gate | 需要但未通过 | T016 涉及删除权限（403 映射+软删除落库需 T017 real 联调确认）；T027 涉及用户偏好数据安全（合并容错边界需人工复核）；T026 binding 400 映射依赖统一异常端点 |

### A.5 DDL / DML 状态

- T016：`kb_knowledge_base.owner_user_id` 字段已在 `init-schema.sql` 中存在；DDL 变更脚本 `sql/ddl-changes/20260807-kb-owner-user-id.sql` 已存在（存量环境迁移用），未执行生产 DDL。
- T026：无 DDL 变更。`kb_model_profile.model_type VARCHAR(24)` 足以容纳 7 枚举，注释仍为旧值（CHAT/EMBEDDING）但不影响功能。
- T027：无 DDL 变更。`kb_user.preference_json` JSON 列已存在，T027 仅修改应用层合并逻辑。

### A.6 长期知识影响

否。T016/T026/T027 均为后端 API 实现与加固，不产生新的长期知识影响。

### A.7 风险

1. **403/400 HTTP 状态码映射**仅由代码核实+单测覆盖，未经集成测试验证（中风险，T017/T031 real 联调覆盖）。
2. **DEEPSEEK provider 仅含 LLM 无 EMBEDDING**（厂商实际无 embedding 产品）：与 Quality"每个 provider 至少 1 LLM + 1 EMBEDDING"字面有偏差，但满足 Done"3+ 常见厂商"标准；CUSTOM 为空按钮入口；ALIYUN/OPENAI 均含 LLM+EMBEDDING。保持厂商真实性，未虚构模型。
3. **端到端 HTTP 验证**（PATCH /users/me→GET 回显、DELETE→403、POST profile CHAT→400）未执行，留待 T029/T031 联调。
4. **环境注意**：Maven 默认 JAVA_HOME 指向 JDK 8，项目需 JDK 21，运行时须 `JAVA_HOME=C:\Program Files\Java\jdk-21`。

---

## 附录 B：T017-T019 / T031-T033 收尾实施记录（2026-08-10）

> 实现确认依据：用户 2026-08-10 消息“执行未完成的任务，废弃的任务不要执行。前后端服务均已启动”。
> 本轮只执行仍为未完成且未标记停止/废弃的 T017、T018、T019、T031、T032、T033；T013、T014、T015、T020、T021 等停止任务未执行。
> SDD 等级：S2。

### B.1 实施摘要

- 用户重启前后端后，已用真实浏览器和测试账号重新联调。前端 `5173`、后端 `8080` 与健康检查均正常，登录、知识库、模型设置均已走真实后端。
- 已完成的实现工作：修复登录提交竞态与认证页样式、注册页移除条款勾选；补齐知识库 binding 字段适配及默认模型预填；加固 preferenceJson 部分更新；关闭普通连接 PATCH 的 API Key 替换旁路；补充 SSRF 行内错误；修复 375px 顶部导航和设置中心响应式；修复上传 `PARSE` 未编排解析及手动解析误用 `DEFAULT`；补充 Controller/Service/安全/视觉回归测试。
- 已清理本轮创建的临时连接、档案、知识库和上传文档，未留下测试引用；保留用户原有“火山方舟”连接与档案。
- 未勾选任务：T017、T018、T019、T031、T032、T033。
- 实施结果：部分完成。代码与自动化回归为绿色，real 模式核心模型链路通过；文档解析根因与实现已修复，但运行进程尚未加载新二进制；完整 10 接口联调、HTTPS、安全/DBA Gate 尚未关闭。
- 是否可交付完成：否。T017/T018 的旧运行态真实复测仍停留“尚未解析”，最新修复需重启后端后再次复测；T032 存在 binding 语义规格冲突且未做预发 DML；T031/T033 当前浏览器到后端仍为 HTTP。
- 是否发布就绪：否。应用代码候选可构建，但安全传输、生产模型主密钥、预发脚本和人工权限 Gate 仍是发布门禁。
- UI 设计确认状态：已通过（T001 已完成）；本轮未跳过设计确认。

### B.2 变更文件

- `fons4ai-rag2okf-ui/src/__tests__/real-integration.spec.ts`：新增真实 API 请求路径、PARSE/SKIP 上传、解析/发布/重分块与 demo/real 隔离契约测试。
- `fons4ai-rag2okf-ui/src/__tests__/visual-regression.spec.ts`：新增三主题 token、焦点、减少动画、响应式断点及关键页面源码契约测试。
- `fons4ai-rag2okf-ui/src/styles/tokens.css`、`SettingsCenterView.vue`：修复 375px 顶部导航拥挤和设置侧栏挤占首屏，保持无横向溢出。
- `fons4ai-rag2okf-ui/src/api/models.ts`、`api/mock/models.ts`：普通连接更新使用不含 apiKey 的独立类型并在序列化层白名单过滤；密钥只走 `/api-key`。
- `fons4ai-rag2okf-service/src/main/java/.../UpdateModelConnectionRequest.java`、`ModelConfigurationApplicationService.java`：从普通 PATCH DTO/Service 删除 API Key 替换能力。
- `fons4ai-rag2okf-service/src/main/java/.../UserProfileApplicationService.java`：资料字段按 PATCH 语义部分更新；首次 preferenceJson 写入也必须是非空 JSON object。
- `fons4ai-rag2okf-ui/src/api/knowledge-bases.ts`：适配后端 `modelProfileKey` 与前端 `profileKey`；设置页空 binding 预填个人默认模型，偏好读取失败降级为空默认值。
- `fons4ai-rag2okf-ui/src/views/settings/ModelSettingsTab.vue`：SSRF/400 错误归属到 Base URL 行内；编辑连接时普通 PATCH 与 API Key 替换严格拆分。
- `fons4ai-rag2okf-ui/src/views/documents/__tests__/DocumentDetailView.spec.ts`：新增解析/发布失败、重分块 revision、失败重试不串联发布等状态机测试。
- `fons4ai-rag2okf-ui/src/api/documents.ts`：手动解析显式提交 `parseMode=PARSE`，不再被知识库 `autoParse=false` 的默认逻辑静默跳过。
- `fons4ai-rag2okf-service/src/main/java/.../DocumentApplicationService.java`、`DocumentParseController.java`：文件事务提交后按上传 parseMode 编排解析并回传 taskKey/QUEUED；手动接口缺省值改为 PARSE。
- `fons4ai-rag2okf-service/src/test/java/.../DocumentApplicationServiceTest.java`、`DocumentParseControllerTest.java`：覆盖 PARSE 上传创建任务、SKIP 不触发任务和手动缺参仍解析。
- `fons4ai-rag2okf-service/src/test/java/com/fons/cloud/ai/rag2okf/model/ChatToLlmOptionalScriptTest.java`：新增 CHAT→LLM 别名与可选脚本审计性测试。
- `fons4ai-rag2okf-service/src/test/java/com/fons/cloud/ai/rag2okf/user/UserProfilePreferenceMergeSafetyTest.java`：新增连续浅合并、空/非法 patch、主题/语言保留测试。
- `fons4ai-rag2okf-service/src/test/java/com/fons/cloud/ai/rag2okf/model/ApiKeySecurityComplianceTest.java`：新增 HTTPS、AES-GCM、mask-only、日志脱敏、独立替换入口、SSRF 边界测试。
- `fons4ai-rag2okf-service/src/test/java/.../controller/T033ControllerIntegrationTest.java`：新增 Spring MockMvc preference PATCH 与非 owner 删除 403 分层集成测试。
- `spec/features/20260807/ddl-changes/CR-001-rag2okf-kb_model_profile-chat-to-llm.sql`：保留可审计的 profile CHAT→LLM 可选转换；删除错误的 binding `CHAT→LLM` DML，并明确语义待确认。本轮未执行任何 DML。

### B.3 TDD 与聚焦验证

| 任务 | RED / 现状证据 | GREEN 证据 | REFACTOR |
| --- | --- | --- | --- |
| T017 | 真实上传成功，但立即解析与手动“解析文档”后均保持“尚未解析”；定位为上传服务忽略 parseMode 且手动请求未显式传 PARSE | 上传后解析编排、手动默认 PARSE 与前端真实请求契约测试通过 | 解析失败不进入对象存储补偿；保留旧运行态失败证据，等待新后端重启后 real 复测 |
| T018 | 原测试缺少发布前置、发布失败保持、重分块 revision 与失败重试隔离场景 | 文档视图聚焦测试通过 | 清理 mock，补充可读断言 |
| T019 | 375px 实测顶部导航文字竖排、面包屑重叠、设置侧栏过高 | 修复后顶部只保留有效导航，侧栏高度由约 325px 降为 216px；三主题循环与无横向溢出实测通过 | 新增 600px 导航降级和设置中心紧凑导航契约 |
| T031 | real 模式曾存在 binding 字段不一致、默认模型不继承、SSRF 只有全局 toast | 核心 7 步真实链路通过；binding/save/default/profile/catalog/preference 与 SSRF 行内反馈修复并复测 | API 契约测试明确标注 mock transport，不冒充真实 HTTP |
| T032 | 设计把 `kb_model_binding.usage_type` 与模型类型混用，错误写成 LLM | profile CHAT→LLM 别名与脚本审计测试通过；不安全 binding DML 已移除 | 等待明确 CHAT binding 是否应映射 ANSWER_GENERATION |
| T033 | 普通 PATCH 可换 Key；首次偏好可持久化 `[]/null/坏 JSON` | 三层关闭 API Key 旁路；首次/连续偏好边界、MockMvc 403、SSRF/AES/mask 测试通过 | 普通 PATCH 前端序列化白名单 + 后端 DTO 双保险 |

聚焦验证命令与结果：

```text
npm run test:unit
结果：28 个测试文件、335 个测试全部通过。

npm run build
结果：typecheck + Vite 生产构建通过，3633 modules transformed；只有既有的大 chunk 警告。

mvn -Dtest=ApiKeySecurityComplianceTest,UserProfilePreferenceMergeSafetyTest,ChatToLlmOptionalScriptTest,T033ControllerIntegrationTest -Dsurefire.failIfNoSpecifiedTests=false test
环境：JAVA_HOME=C:\hongqy\C\Java\jdk21
结果：BUILD SUCCESS，15 个测试全部通过。

mvn -Dtest=DocumentApplicationServiceTest,DocumentParseControllerTest,ParseApplicationServiceTest test
环境：JAVA_HOME=C:\hongqy\C\Java\jdk21
结果：BUILD SUCCESS，18 个测试全部通过。

mvn test
环境：JAVA_HOME=C:\hongqy\C\Java\jdk21
结果：BUILD SUCCESS，243 个测试全部通过。
```

### B.4 回归与运行态 Evidence Matrix

| 证据项 | 状态 | 证据来源 | 是否阻挡发布 |
| --- | --- | --- | --- |
| 前端编译 | 通过 | `npm run build`，3633 modules transformed | 否 |
| 完整前端单测 | 通过 | 28 files / 335 tests passed | 否 |
| 聚焦后端单测 | 通过 | 安全/偏好/脚本 15 tests + 文档解析 18 tests passed，BUILD SUCCESS | 否 |
| 完整后端单测 | 通过 | 243 tests passed，BUILD SUCCESS | 否 |
| 服务启动 | 通过 | 用户确认已启动；`5173/` 与 `8080/actuator/health` 均返回 200 | 否 |
| 健康检查 | 通过 | `8080/actuator/health` 为 200，`application/vnd.spring-boot.actuator.v3+json` | 否 |
| 服务发现/路由 | 通过 | `.env.development` real 模式直连 `http://127.0.0.1:8080`，真实登录和模型 API 可用 | 否（仅本地） |
| T031 核心 HTTP | 部分通过 | 连接创建、catalog、档案创建/测试、偏好合并、默认模型继承、KB binding 已真实验证并清理 | 是：仍缺完整 10 接口及新后端二进制重启复测 |
| T017/T018 文档状态机 | 代码修复、运行态待复测 | 旧进程中上传成功但自动/手动解析均为“尚未解析”；已修复 parseMode 两处断点，最新二进制尚未加载 | 是 |
| T019 视觉 | 部分通过 | 三主题循环、375px 模型设置、无横向溢出与响应式修复已实测；未覆盖全部页面与完整键盘路径 | 是 |
| 外部依赖 | 只读通过 | 后端健康与端点存在性已只读确认；未对外部模型供应商发起写入 | 否 |
| 运行初始化 DML | 未执行且已封险 | profile 转换脚本默认 ROLLBACK；binding DML 因语义冲突已删除 | 是：T032 完成仍需规格与预发证据 |
| API Key 日志 | 部分通过 | 已用固定无效样本扫描运行日志，0 命中；响应只见 mask | 是：仍需 HTTPS 与生产安全 Gate |
| 人工 Gate | 待执行 | HTTPS、模型主密钥、跨用户权限、可选数据脚本 | 是 |

### B.5 AC 覆盖

| AC | 任务 | 当前证据与状态 |
| --- | --- | --- |
| AC-003～AC-005 | T017/T018/T033 | Controller 403 + Service 权限测试通过；浏览器 real 文档解析需在新后端重启后复测 |
| AC-015 | T017 | real 上传与 demo/real 隔离通过；解析编排代码已修，完整生命周期运行态未复测 |
| AC-016～AC-017 | T019 | 三主题循环、375px 响应式、焦点/减少动画源码契约通过；全页面键盘验收待补 |
| AC-018 | T017/T018 | 自动化状态机规则及 parseMode 编排通过；real 解析状态需新后端重启后复测 |
| AC-026、AC-029、AC-031 | T031 | catalog、级联、偏好、binding 核心真实链路通过；10 接口与 HTTPS 未闭环 |
| AC-027 | T032 | profile CHAT→LLM 别名与拒写通过；binding 历史值目标语义未定义，预发未执行 |
| AC-028、AC-032 | T033 | preferenceJson 首次/连续合并、API Key 独立更新、mask/AES/SSRF/403 自动化通过；安全人工 Gate 待补 |

### B.6 Review、S2 门禁与任务状态

- Implementer 自检：已完成；临时数据按 binding → defaultModels → profile → connection 顺序清理并复核。
- Spec Review：已独立执行。发现普通 PATCH API Key 旁路、首次非法偏好、SSRF 行内反馈三个 P1；均已修复并由自动化/浏览器复验。T032 binding 语义冲突、HTTPS、完整 10 接口仍未关闭。
- Code Review：已独立执行。上述三个 P1 已关闭；生产主密钥、日志集成、悬空引用清理与 preference 读取降级风险已记录，其中 preference 读取降级已实现。
- Critical/Important 问题：文档解析断点已修但新后端源代码尚需重启后二次 real 复测；T032 规格冲突；浏览器到后端 HTTP。
- 人工 Gate：适用且待执行，原因是 T032/T033 涉及数据转换、凭证传输、生产密钥和权限安全。
- Checklist：自动化门禁已绿；外部/人工门禁未关闭。
- 任务规划勾选：本轮不修改 T017/T018/T019/T031/T032/T033 的 `[ ]`，因为 fresh L3 与依赖门禁尚未满足；所有停止/废弃任务保持不执行。

### B.7 数据与长期知识影响

- DDL：不涉及。
- DML：未执行。profile 脚本默认回滚并记录精确主键范围；binding 转换因语义未确认而明确不执行。
- 字段/枚举验证：普通连接 PATCH 不再含 apiKey；知识库 binding 线缆统一适配 `modelProfileKey/profileKey`；偏好根节点固定为 object。
- 敏感数据：只使用固定无效测试样本；API Key mask、AES-GCM、日志不记原值、独立替换入口和 SSRF 边界已自动化验证；未使用真实模型密钥。
- 长期知识影响：是。确认 `kb_model_binding.usage_type` 是 `ANSWER_GENERATION/EMBEDDING` 的业务用途，而不是 7 类模型类型；T032 设计文字需要后续 CR 修正。

### B.8 问题、风险与后续事项

1. 重启后端加载文档解析修复，再执行“立即解析”与手动解析 real 复测；确认 QUEUED → RUNNING → SUCCEEDED 后继续发布/重分块验收。
2. 后端需再次重启以加载本轮 DTO/Service 加固，再补齐连接更新、API Key 独立替换、档案更新等 T031 10 接口 real 证据。
3. T032 需要规格明确：历史 binding `CHAT` 是否映射为 `ANSWER_GENERATION`。在该决策和预发样本/回滚证据前，不得执行 binding DML，也不得勾选完成。
4. 部署环境必须提供浏览器→后端 HTTPS，并强制配置独立 `SYS_MODEL_CREDENTIAL_KEY`；本地 HTTP 和默认 salt 派生不能作为 API Key 安全 Gate 通过证据。
5. 删除 profile/connection 前需校验或清理 KB bindings/defaultModels，避免悬空引用；本轮临时数据已按安全顺序清理。

---

## 附录 C：CR-002 T034-T036 关闭审计与新进程复测（2026-08-10）

### C.1 本次结论

- 正式关闭：**T034**。
- 继续保持未完成：T017、T018、T019、T031、T032、T033、T035、T036。
- 停止/废弃任务未执行。
- 用户要求停止本轮临时数据清理；临时知识库及文档已在该要求前删除，临时默认 LLM 已解除，临时 DeepSeek 连接与 `qwen-plus` 档案保留。

### C.2 T034 关闭证据

- 后端 main 代码扫描 `ModelCatalog|model-catalog` 为 0；原 Controller、ApplicationService、Response、YAML 已删除。
- `ModelProviderTemplate` 为 7 个值，包含 DEEPSEEK、OPENAI；响应仅含 `code/providerName/defaultBaseUrl/officialUrl`，CUSTOM 的 officialUrl 为 null。
- 新进程真实模型设置页成功加载 7 个 Provider 模板及官方链接，DeepSeek 连接弹窗正确预填 `https://api.deepseek.com/v1`。
- JDK 21 执行 `mvn test`：243 tests passed，BUILD SUCCESS；编译与全量单测均通过。
- 未认证直接访问旧路径先被统一认证过滤器返回 401；鉴权后的业务端点已无 Controller 映射。端点删除同时由 main 零残留、Controller 契约测试和全量构建交叉确认。

### C.3 未关闭任务及门禁

| 任务 | 当前已通过 | 尚未满足的关闭条件 |
| --- | --- | --- |
| T017 | 创建 KB、上传、自动解析、源文件内容、1 个分块、重新分块、删除 KB 均在新进程真实通过 | 发布进入 `PUBLISH · RETRY_WAIT`，错误为 `PUBLISH_ARTIFACT_ERROR: OSS operation failed` |
| T018 | 文档状态机自动化通过；真实解析与重分块成功 | 依赖 T017；真实发布未成功，不能完成业务状态回归 |
| T019 | 视觉/响应式源码回归随 333 个前端单测通过 | 本次未完成三主题全页面、窄屏和完整键盘人工验收 |
| T031 | 真实完成连接创建/更新/API Key 独立替换、档案创建/更新/失败反馈、默认模型保存、新 KB 默认绑定预填与保存 | 浏览器到后端仍为 HTTP；完整 10 接口关闭与安全 Gate 未全部满足 |
| T032 | 错误的 binding `CHAT -> LLM` DML 已移除；脚本含计数、回滚、分批和时间预估；别名单测通过 | 依赖 T031；未取得预发样本验证证据 |
| T033 | preference 首次/连续合并、普通 PATCH 关闭 API Key 旁路、AES/mask/SSRF/403 自动化均通过 | 本地传输为 HTTP；生产独立主密钥与真实日志通道审计未关闭 |
| T035 | catalog 零残留；模板搜索、官方链接隔离、三字段预填、模型名手填及 ≤160 校验通过；333 个前端单测和生产构建通过 | `package.json` 没有 lint 脚本，且未安装 ESLint，任务明确要求的 lint Gate 无法计为通过 |
| T036 | demo 集成已覆盖 DeepSeek 搜索 → 三字段连接 → 手填 `deepseek-chat` → 默认模型；typecheck 通过 | 同样缺少任务明确要求的 lint Gate |

### C.4 本次验证结果

```text
npm run test:unit -- --reporter=dot
结果：28 个测试文件、333 个测试全部通过。

npm run build
结果：typecheck + Vite 生产构建通过，3632 modules transformed；仅有大 chunk 警告。

mvn test
环境：JAVA_HOME=C:\hongqy\C\Java\jdk21
结果：BUILD SUCCESS，243 个测试全部通过。

真实浏览器：
- 登录成功并进入 /knowledge-bases；
- 模型模板、搜索、DeepSeek 预填、模型名手填、默认偏好和 KB binding 通过；
- 文档 source-content 正常展示，content_hash 写入不再报截断；
- 发布因 OSS operation failed 进入 RETRY_WAIT。
```
